# Plume — CI (hardened, reproducible; no offensive network I/O in CI).
# =============================================================================
#
# Jobs:
#   - test     : `cargo test --locked` for the Plume daemon (crate in `daemon/`),
#                default features only → SqlcipherStore-only SMB profile. The test COUNT is
#                ASSERTED, not commented: the step sums the `test result: ok. N passed` lines
#                and fails on any drift from EXPECTED_TESTS — that is what makes
#                CONTRIBUTING's "constant test count" invariant an actual gate,
#                plus `cargo build --release --locked`. (The `duckdb` feature-check that used to
#                live here now runs in the `features` job, with the other gated features.)
#   - cold-tier: `cargo test --locked --features cold_tier` on its OWN runner, with its OWN
#                asserted count (EXPECTED_COLD_TESTS). WHY THIS JOB EXISTS — read this before
#                deleting it: since the hot/cold split (hot window 7 days, retention 365 days)
#                `daemon/src/cold_store/` IS THE READ PATH FOR 358 OF THE 365 DAYS of production
#                data, and until this job existed NOTHING in CI so much as COMPILED it. The
#                default job cannot: without `--features cold_tier` the module does not exist.
#                MEASURED, not argued: with a deliberate type error appended to
#                `cold_store/writer.rs`, `cargo test --locked` finished GREEN (exit 0) while
#                `cargo test --locked --features cold_tier` failed to compile (exit 101).
#                The feared failure mode here is NOT a crash — it is SILENT HISTORY LOSS: a
#                query spanning >7 days that returns a wrong count without saying so.
#   - shell    : `bash -n` on every tracked shell script. MEASURED today: 43 tracked scripts,
#                3929 lines, and `grep -rE "shellcheck|shfmt|bash -n" .github/` returned ZERO
#                hits — a production ingest path with no automated checking at all. Plus a
#                CONSULTATIVE shellcheck. See the job for what is and is not guaranteed.
#                This job also carries the COLLECTOR HONESTY gates, shell AND PowerShell: a sensor
#                that cannot collect must SAY so, and the Windows collector must not advance its
#                watermark before the central has acknowledged the batch (measured loss: 42/42).
#   - features : cheap `cargo check` per OFF-by-default cargo feature, so that gated code at
#                least still COMPILES. `check` only — these prove compilation, NOT behaviour.
#                Only `cold_tier` gets a real test run (above); see the job for the rest.
#   - security : supply-chain + secret hygiene — `cargo audit --deny warnings` on the
#                daemon's locked dep tree, plus a gitleaks secret scan of tree + history.
#   - lint     : `cargo fmt --all --check` + `cargo clippy --locked --all-targets`, both
#                CONSULTATIVE (continue-on-error) until the tree is proven warning-free —
#                then drop the continue-on-error / add `-D warnings` to make them blocking.
#
# NOTE features — the full list, from `[features]` in daemon/Cargo.toml (all OFF by default):
#   `duckdb`, `clickhouse`, `clickhouse-ha`, `cold_tier`, `ldap`, `saml`, `ai`.
# (An earlier version of this comment claimed `duckdb` was "the daemon's ONLY compile feature"
# and that `clickhouse` was "NOT (yet) a feature". Both statements were false: the feature table
# lists all seven. The comment had drifted because nothing executed it.)
# COVERAGE, stated honestly — see the `features` job for the per-feature detail:
#   cold_tier ......... COMPILED **and TESTED** (own job, own asserted count).
#   duckdb/clickhouse/clickhouse-ha/ldap/saml/ai ... COMPILE-CHECKED ONLY (`cargo check`).
#     Their code paths are NEVER EXECUTED by CI. A `saml`/`ldap`/`ai`/ClickHouse behaviour
#     regression that still type-checks WILL pass this pipeline. Do not read a green CI as
#     "these backends work"; it means "these backends still compile".
#
# guatx-core (the shared SOQL compiler) is a GIT dependency of the daemon, pinned to a tag in
# daemon/Cargo.toml and NOWHERE ELSE — a recopied label rots, and it did: nine lines of this repo
# (four of them here) still announced an older tag than the manifest. Read the live one with
# `grep -n 'guatx-core' daemon/Cargo.toml`; check_the_core_pin_is_never_recopied.py refuses a
# second copy. `guatxlabs/core` must be reachable by the runner for cargo to resolve the tag
# (anonymous fetch) — CI needs no sibling checkout and no CI-only `[patch]`. The committed
# daemon/Cargo.lock pins guatx-core to that git source+rev, so `cargo test --locked`
# is reproducible against exactly what public users get. (Local dev may still keep a
# git-ignored `.cargo/config.toml` `[patch]` -> sibling checkout for build speed; it
# is never committed and never present in CI.)
#
# All actions and the Rust toolchain are SHA-PINNED for reproducibility.

name: ci

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same ref (runner economy).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# SOURCE UNIQUE du compteur de la suite par DÉFAUT. Il était écrit deux fois (job `test` et job
# `cold-tier`, qui s'en sert pour son garde-fou anti-no-op) : deux sources de vérité qui pouvaient
# diverger en silence — bumper l'une laissait l'autre comparer à un nombre périmé. Une seule ici,
# consommée par les deux jobs.
env:
  # 954 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, 12 cœurs), sur DEUX exécutions
  # de l'arbre final. 949 datait du 08-08 ; +5 viennent du lot P8.6-b / P4.1-p de ce jour (3 tests de
  # KDF scrypt dans `tests/backup_streaming.rs`, 2 de plafond d'ingestion dans `tests/ingest.rs`).
  # DURÉE, dite honnêtement : 219 s puis 187 s, contre 170 s pour l'arbre d'avant — l'écart entre deux
  # exécutions du MÊME arbre (32 s) est du même ordre que l'écart avec l'arbre d'avant, donc cette
  # machine ne permet pas d'attribuer la différence au contenu. Ce qui EST attribuable, parce que
  # mesuré isolément : borner le facteur scrypt à log_n=12 fait payer 555 ms au lieu de 974 à 1998 ms
  # à chaque KDF par passphrase, et il y en a une trentaine dans la suite.
  #
  # ⚠ RECTIFICATION D'UNE ALLÉGATION FAUSSE QUI A VÉCU ICI. Cette ligne a porté, du 2026-08-08 au
  # 2026-08-09 : « CE GARDE NE S'EXÉCUTE QU'À L'OUVERTURE D'UNE PR, et on pousse directement sur
  # main ». C'est FAUX, et le fichier lui-même le contredit vingt lignes plus haut : `on:` porte
  # `push: branches: ["**"]`, et ce bloc n'a JAMAIS été modifié depuis la création du fichier
  # (`git log -p --follow .github/workflows/ci.yml` : une seule apparition, dans le commit initial).
  # `git rev-list --count github/main..main` vaut 0 : rien n'est resté local. La garde a donc bien
  # tourné sur chaque push, et bien vu la dérive 937 -> 945 du 2026-08-07.
  # LE TROU RÉEL est la BOUCLE DE RETOUR, pas le déclencheur : le verdict s'affiche sur une page
  # qu'on n'ouvre pas au moment de commiter. Aucun déclencheur supplémentaire ne ferme ça — un
  # `push` de plus produirait le même rouge, au même endroit, ignoré pareil. Fermé par un contrôle
  # LOCAL, `.github/scripts/compter-les-tests.sh` (2 s : il ÉNUMÈRE les tests, il ne les exécute
  # pas), installable en hook via `git config core.hooksPath .githooks`. Cf. P8.5-a dans
  # docs/ROADMAP.md.
  #
  # 961 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, 166 s, 12 cœurs, TMPDIR sur
  # disque). 954 datait du même jour ; +7 viennent du lot P8.7-a — `tests/partition_config.rs` : le
  # scanner qui ferme la partition de lecture de la configuration (2 tests dérivés) et l'annonce de
  # bascule des réglages de sauvegarde (5 tests PURS, aucun ne touche l'environnement du processus).
  #
  # 968 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, **167 s**, 12 cœurs, TMPDIR sur
  # disque, machine partagée avec d'autres travaux — cf. la note de durée ci-dessus, cette machine ne
  # permet pas d'attribuer un écart de durée au contenu). 961 datait du même jour ; +7 viennent du lot
  # P8.7-b — `tests/cle_at_rest_voie_unique.rs` : la preuve SUR LES OCTETS qu'une clé écrite dans le
  # fichier de configuration chiffre désormais la base chaude (1 test, avec son témoin négatif dans le
  # même corps) et la précédence/annonce (6 tests PURS). Le 8e test du lot est sous `cold_tier`
  # (`cold_store/tests.rs`) : c'est la moitié FROIDE, elle n'existe pas dans le build par défaut.
  #
  # ⚠ CE COMPTE A ÉTÉ MESURÉ DEUX FOIS, ET LA PREMIÈRE ÉTAIT ROUGE. 966 passés / 2 ÉCHOUÉS
  # (`p3a_advanced_rule_captures_src_ip_and_wizard_prefills_ban_ip`,
  # `p3a_ambiguous_groupby_leaves_structured_null`, tous deux sur `file is not a database`) : la
  # première version du lot faisait lire la clé at-rest dans la configuration AMBIANTE, et le test qui
  # posait `PLUME_CONFIG` faisait appliquer un `PRAGMA key` aux bases EN CLAIR de tests voisins. Un
  # verrou local n'aurait rien fermé (le partage est entre CELUI qui mute et TOUS ceux qui ouvrent une
  # base) : `ensure_encrypted` prend désormais la `conf` de son appelant. C'est la suite complète qui
  # a trouvé ça — pas le filtre `-- p87b`, qui était vert.
  #
  # 979 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, **175 s** de bout en bout dont
  # 158,85 s de suite, 12 cœurs, TMPDIR sur disque). 968 datait du même jour ; +11 viennent du lot
  # P10.7-a — `tests/barre_fts_vide_silencieux.rs` : la barre `/api/search` envoyait les tokens BRUTS à
  # `MATCH` et AVALAIT l'erreur du moteur, donc `10.0.0.1` (comme quatre autres saisies d'analyste
  # ordinaires) rendait `{"results": []}` — « rien ne correspond » là où le moteur disait « je refuse ».
  # Les 11 : le CONSTAT (le moteur refuse bien les cinq saisies, avec son témoin positif), leur
  # FERMETURE, un balayage DÉRIVÉ des 95 ASCII imprimables sous 4 formes (380 cas, refait à chaque
  # exécution contre le moteur LIÉ — c'est lui qui a trouvé `a^b`, accepté sans erreur et pourtant
  # introuvable), la règle d'échappement vérifiée contre le moteur, le terme sans token d'index (dit,
  # jamais servi), la non-régression des opérateurs, la requête de phrase RENDUE, le miroir qui suit le
  # tokenizer et les colonnes de la base vivante, l'abstention quand il n'y a pas d'oracle, le filet
  # d'erreur, et un bout-en-bout à travers le routeur RÉEL.
  #
  # ⚠ CE LOT A FAIT ROUGIR UN TEST QU'IL N'A PAS ÉCRIT, ET C'ÉTAIT JUSTE. `day2_search_metric_updates`
  # (`tests/misc.rs`) assertait le p50 du réservoir de latences COMMUN au processus depuis un test
  # PARALLÈLE : toute requête `/api/search` y verse ses millisecondes, et le bout-en-bout ci-dessus en
  # émet sept. Mesuré : « p50 dans la plage observée: 2 ». Le couplage existait déjà (les balayages de
  # routeur passent par la même route) ; il n'était pas visible. La mesure est désormais rendue
  # INSENSIBLE à l'interférence en saturant le réservoir borné — pas de test retiré, pas de seuil relâché.
  #
  # 989 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, **181,93 s** de suite / 3 min 15 s
  # de bout en bout, 12 cœurs). 979 datait du même jour ; **+10 viennent du lot P10.7-b**
  # (`tests/compactage_fts.rs`) — la compaction de l'index plein-texte. Le lot ferme un défaut que
  # personne ne regardait : **une purge de rétention fait GROSSIR `event_fts_data`** (mesuré sur le
  # schéma réel : 135,27 → 185,69 Mio en supprimant 58,4 % des documents), parce qu'une table FTS5 à
  # contenu externe ne retire pas un posting — elle en écrit un de SUPPRESSION — et que plume ne
  # fusionnait JAMAIS ses segments.
  # Les 10 : le DÉFAUT lui-même (le test échoue si l'index rétrécissait tout seul — sans quoi tout le
  # reste serait du poids mort ajouté pour rien) et sa fermeture nommée en octets ; la MUTATION du
  # kill-switch (`PLUME_FTS_COMPACT=0` ⇒ l'index reste gonflé AU MÊME OCTET) ; le SIGNE du budget
  # (`merge` à N POSITIF ne rend rien sur cet index — c'est le piège, et c'est la garde qui attrape la
  # « simplification » consistant à retirer le moins) ; la propriété portée par le TYPE (aucune issue
  # hors `Rendue` ne peut annoncer d'octets) ; le budget de tick épuisé DIT comme tel et la reprise qui
  # converge au même plancher que la compaction d'un seul tenant ; une erreur SQLite qui ne se déguise
  # PAS en budget épuisé ; les index DÉRIVÉS du schéma (une vtable FTS5 inventée demain est trouvée) ;
  # le compte de segments lu dans l'enregistrement de structure de FTS5 (et non dans `%_idx`, qui
  # sous-évalue en silence) ; les bornes du réglage ; la base sans index plein-texte.
  # DURÉE, dite honnêtement : 181,93 s contre 182,69 s pour le même arbre sans ces 10 tests, mesurés à
  # 20 min d'intervalle sur la même machine — l'écart est du bruit, le lot ne prétend rien accélérer.
  #
  # 1007 : MESURÉ le 2026-08-09 (`cargo test --offline --locked`, rc=0, **245,80 s** de suite, 12 cœurs).
  # 989 datait du même jour ; **+18 viennent du lot « série du budget »** — 16 dans
  # `tests/ventilation_serie.rs`, 2 dans `db_ventilation.rs`. Le lot ferme le défaut RACINE derrière
  # trois autres : la ventilation par poste n'existait qu'en RELEVÉ PONCTUEL (`db-stats --par-objet`,
  # une vingtaine de secondes par Gio sur une base réelle le 2026-08-09), donc aucune question de tendance —
  # « la part FTS5 a-t-elle baissé depuis la compaction `4ca6339` ? » — n'était répondable sans
  # relever à la main. La MÊME mesure est désormais écrite par un tick lent dans `metric`, que la
  # commande GXQL `metric` unionne avec `metric_rollup` (90 j).
  # Les 18 : le REFUS de publier se PROPAGE (comptabilité qui ne ferme pas ⇒ TROU dans la série et
  # `plume_db_ventilation_ok=0` avec sa cause, JAMAIS un zéro d'octets — prouvé sur les trois surfaces :
  # points écrits, exposition Prometheus, JSON) ; les TROIS états distincts (jamais mesuré ≠ refusé ≠
  # mesuré — au démarrage, `ok 0` accuserait une panne qui n'a pas eu lieu) ; la SOMME des seaux publiés
  # fait le fichier (c'est ce qui rend une PART calculable depuis la série SEULE, sans second chiffre) ;
  # le parcours n'attend PAS le verrou d'écriture (vérifié verrou tenu, sur un fil séparé pour que la
  # régression rougisse en 5 s au lieu de bloquer la suite) et `tour` ne verrouille pas lui-même (garde
  # STRUCTURELLE : la « simplification » à un seul verrou tiendrait l'ingestion 35 s par heure) ; les
  # DEUX bornes, mesurées et non estimées — CPU (le tick s'espace tout seul pour rester sous 5 % d'un
  # cœur) et DISQUE (**2,16 Mio pour la rétention COMPLÈTE de la série**, schéma réel, après VACUUM,
  # soit 0,105 % du budget 2 Gio) ; les clés d'étiquette stables et distinctes ; et `TOUS`/`rang()` qui
  # ne peuvent pas diverger (un poste ajouté demain est publié sans que personne y pense).
  # DURÉE, dite honnêtement : les 18 tests s'exécutent en 4,10 s quand on les tire seuls. Les 245,80 s
  # ci-dessus NE SONT PAS comparables aux 181,93 s de la ligne précédente : la machine était partagée
  # avec d'autres travaux pendant la mesure, et je n'ai pas contrôlé cette charge. La CI, elle, mesure
  # sur son propre runner.
  #
  # 1012 : MESURÉ le 2026-08-10 (`cargo test --offline --locked`, rc=0, 12 cœurs), sur DEUX
  # exécutions de l'arbre final : **217,84 s** puis **225,72 s** de suite (3 min 39 s puis 3 min 47 s
  # de bout en bout). L'écart entre deux exécutions du MÊME arbre est de 7,9 s : c'est l'ordre de
  # grandeur du bruit de cette machine, et aucune comparaison plus fine n'est justifiable avec elle.
  # 1007 datait du 08-09 ; **+5 viennent du lot « usage des
  # index de `event` »** (`tests/index_usage_event.rs`). Le lot ne corrige aucun défaut : il MESURE
  # celui que `docs/DESIGN-P10-echelle-2go.md` §5.4 avait interdit de toucher sans mesure — « qui se
  # sert des index b-tree de `event` ? » — sur un poste qui pèse **plus du quart du fichier** sur une
  # base réelle (relevé du 2026-08-09), dont plus de la moitié hors de l'index de dédup.
  # Les 5 : le LECTEUR DE PLAN validé par témoin positif DÉRIVÉ (chaque index du catalogue, forcé par
  # `INDEXED BY`, doit être relu — l'index ajouté demain est couvert le jour où il est ajouté) ET par
  # témoin négatif (`NOT INDEXED` doit rendre VIDE, sinon un vide ne serait pas une mesure) ; le REFUS
  # DE PUBLIER si la base d'épreuve ne porte pas les index que la production porte (mutation : retirer
  # un seul appel de réconciliation de fond nomme l'index absent) ; la CLÔTURE DE LA PARTITION du
  # corpus, dérivée du module `server`/`tenants.rs` et du catalogue de colonnes porteuses — deux mutations
  # la font rougir, dont celle qui a révélé que l'extracteur ne lisait pas les lignes à commentaire de
  # fin ; la MESURE elle-même (230 objets livrés, rejoués en `EXPLAIN QUERY PLAN` sous trois régimes de
  # statistiques) ; et l'ABLATION qui chiffre l'autre moitié du devis, le coût d'ÉCRITURE par index.
  # DURÉE, dite honnêtement : les 5 tests s'exécutent en 47,4 s quand on les tire seuls, dont 42,6 s
  # pour la seule ablation (9 bras × 20 000 insertions par la porte d'ingest réelle). C'est le lot le
  # plus cher au test unitaire de ce fichier, et il l'est parce qu'il ÉCRIT vraiment.
  #
  # +1 le 2026-08-19 (lot « destination objet de la sauvegarde », clé P8.22-a), MESURÉ par ÉNUMÉRATION
  # (`cargo test --offline --locked -- --list`). UN SEUL test entre dans la suite PAR DÉFAUT, et c'est
  # voulu : le sink objet est derrière la feature `s3_backup`, donc ses douze tests à lui sont hors de
  # ce profil (leur compteur vit dans le job `features`). Celui qui entre ici est le seul qui parle du
  # profil par défaut — une destination `s3://` que ce binaire ne peut pas honorer ne devient JAMAIS
  # une écriture locale. Il n'a aucun `cfg` et passe donc dans les DEUX profils, ce qui est
  # exactement ce qu'on veut d'une garde de mode 0 : elle ne peut pas être vraie « seulement quand la
  # feature est éteinte ».
  # CONTRÔLE DE L'ÉCART, fait le même jour et par la même énumération : la feature ajoute EXACTEMENT
  # douze noms, tous ceux de `daemon/src/tests/sink_s3.rs`, et pas un de plus.
  # 1060 : MESURÉ le 2026-08-20 par ÉNUMÉRATION (`cargo test -- --list`, deux lectures concordantes),
  # arbre de travail complet. +6 pour le lot « exercice de restauration » (clé P8.3-a,
  # `daemon/src/tests/exercice_de_restauration.rs`) : une restauration menée de bout en bout sur le
  # chemin symétrique avec comparaison du contenu table par table, l'attestation, l'état daté du
  # dernier exercice et son vieillissement, les refus d'enregistrement, le composant de santé et ses
  # jauges, le signal SOC. Le reste de l'écart avec 1048 vient des autres lots du même arbre — ce
  # nombre est celui de l'arbre, pas d'un lot.
  # Six de ces « autres tests » sont ceux de la borne interactive (clé P7.8-a,
  # `daemon/src/tests/semaphore_interactif.rs`) : le décompte DÉRIVÉ des routes qui consomment le
  # sémaphore interactif, l'interdiction du permit nu hors du module qui mesure sa détention, la file
  # VUE et la file ABSENTE (les deux sens), le plafond de cardinalité qu'on fait mordre, et
  # l'étiquette prise sur le GABARIT de route et non sur l'URL. Contre-mesuré le 2026-08-20 par la
  # même énumération, sur le même arbre : 1060 par défaut, 1280 avec `cold_tier`.
  # +11 pour le lot « imputation d'une alerte à ses sources » (clé S7,
  # `daemon/src/tests/imputation_de_source.rs`) : l'ampleur DÉRIVÉE du contenu livré (combien de règles
  # livrées le texte d'une règle sait nommer, et le fait qu'AUCUN des capteurs ne l'était), la mutation
  # dans les deux sens (une source muette -> SA pastille et aucune autre ; deux muettes -> deux
  # pastilles), le même couple pour le chemin RÈGLE, l'alerte globale prouvée intacte champ par champ,
  # le repli des alertes antérieures à la migration, l'inconnu NOMMÉ, la forme stockée, et la garde
  # DÉRIVÉE qui refuse qu'un producteur d'alerte de plus rejoigne la liste sans dire lequel des deux
  # chemins d'imputation il prend. RE-MESURÉ le
  # 2026-08-20 par la MÊME énumération, sur l'arbre courant : 1079 par défaut, 1299 avec `cold_tier`
  # (l'arbre a reçu d'autres lots pendant la mesure ; ces deux nombres sont ceux de l'arbre, pas d'un
  # lot — c'est la même règle que pour l'écart avec 1048 plus haut, et c'est la raison pour laquelle
  # une contre-mesure est refaite ici plutôt qu'un chiffre reporté d'un rapport).
  # +9 pour le lot « observatoire d'usage des index » (clé P10.9-a,
  # `daemon/src/tests/index_usage_observatoire.rs`) : les DEUX témoins de mutation (un énoncé forcé sur
  # un index fait bouger SON compteur et aucun autre ; un balayage non indexé n'en bouge AUCUN — sans
  # ce second, un compteur qui monte toujours ne prouverait rien), l'extinction par défaut prouvée par
  # une exposition VIDE, le coût mesuré et imprimé, le pas d'échantillonnage, le plafond de cardinalité
  # qu'on fait mordre, la déduction de la classe de consommateur, les trois crans du régime de
  # statistiques, et la garde qui exige que la portée de la série voyage dans son `# HELP`. Ces neuf
  # noms sont SANS `cfg` : ils entrent dans les DEUX profils, d'où le même +9 sur les deux compteurs.
  # RE-MESURÉ le 2026-08-20 par la MÊME énumération, sur l'arbre courant (qui porte aussi les lots
  # ci-dessus) : 1079 par défaut, 1299 avec `cold_tier` — les valeurs déjà posées.
  # RE-MESURÉ une dernière fois le 2026-08-20, par la MÊME énumération et sur le MÊME arbre, après la
  # garde dérivée des producteurs d'alerte (S7) et les lots arrivés entre-temps : 1086 par défaut,
  # 1306 avec `cold_tier`. Trois énumérations successives du même jour ont rendu 1070, 1079 puis 1086 :
  # ces nombres sont ceux de l'ARBRE, jamais d'un lot — un lot qui atterrit après celui-ci doit donc
  # RÉ-ÉNUMÉRER, et surtout pas additionner son propre écart à la valeur trouvée ici.
  # DONT +6 pour le lot « empreinte du journal d'écriture » (clé P10.16-a,
  # `daemon/src/tests/wal_empreinte.rs`), inclus dans le 1086 ci-dessus : la formule de la borne
  # exercée sur chacune de ses entrées (plafond d'événements, taille de page, en-tête de trame), les
  # trois états de la borne distingués (dérivée / imposée / désactivée) avec l'exigence que chaque
  # phrase AVOUE ce qu'elle ne borne pas, la couverture d'une VRAIE transaction d'ingest mesurée sur
  # `db/schema.sql` plus la chaîne de migrations — avec le témoin qui fait porter le terme du lot (la
  # réserve d'auto-checkpoint SEULE ne couvre pas cette transaction) —, le résidu mesuré dans les DEUX
  # bras (borné et témoin sans borne : sans le second on aurait mesuré un fichier petit sans savoir
  # pourquoi), le verdict de la voie unique de troncature exercé SOUS la borne dans ses deux états
  # (refusé quand un lecteur tient le journal, tronqué après relâche), et la garde dérivée qui
  # n'autorise qu'un seul auteur au seuil d'auto-checkpoint. Ces six noms sont SANS `cfg` de feature :
  # ils entrent dans les DEUX profils, donc ils ne déplacent pas l'écart cold−défaut de 220. La suite
  # par défaut a été EXÉCUTÉE sur l'arbre courant, pas seulement énumérée : 1086/1086, rc=0, 181,60 s.
  # +3 pour le lot « la clé d'un tenant ne se fabrique pas à partir d'une horloge » (clé S4,
  # `daemon/src/tests/tenants.rs`) : la mutation (aucune matière d'entropie -> AUCUNE clé) avec son
  # témoin inverse (la source réelle de l'hôte rend de la matière, et la clé EST cette matière), le
  # contrôle de structure sur un lot de clés avec le témoin qui prouve qu'il DÉNONCE un générateur à
  # compteur, et la garde DÉRIVÉE dont la partition est une PROPRIÉTÉ — toute fonction de production qui
  # puise à une source d'entropie de l'OS et rend de la matière — plutôt qu'une liste de noms. Ces trois
  # noms sont SANS `cfg` de feature : ils entrent dans les DEUX profils et ne déplacent pas l'écart
  # cold−défaut de 220. RÉ-ÉNUMÉRÉ le 2026-08-20 sur l'arbre courant, qui porte aussi les lots arrivés
  # entre-temps : **1109** par défaut, **1329** avec `cold_tier`, écart **220** inchangé. Ces nombres
  # sont ceux de l'ARBRE, jamais d'un lot : le lot suivant RÉ-ÉNUMÈRE, il n'additionne pas son écart à
  # la valeur posée ici. Deux énumérations du même jour, à quelques minutes d'écart, ont rendu 1098
  # puis 1109 — c'est exactement pourquoi la règle est de ré-énumérer plutôt que de reporter.
  # RÉ-ÉNUMÉRÉ le 2026-08-20 sur l'arbre courant, qui porte aussi les lots arrivés entre-temps :
  # **1125** par défaut, **1345** avec `cold_tier`, écart **220** inchangé. Le lot S28 — la lecture de
  # la limite mémoire du groupe de contrôle, cinq tests SANS `cfg` de feature, donc présents dans les
  # DEUX profils — est compris dedans. L'INSTRUMENT A ÉTÉ VALIDÉ avant d'être cru, par un témoin
  # positif et un témoin négatif : le compte des lignes `: test` et le total que cargo imprime
  # lui-même concordent, et `-- --list --ignored` rend **0 test**. Le total énuméré est donc bien le
  # total qui PASSE, et non un total dont il faudrait retrancher des tests ignorés.
  # ÉCART DÉPLACÉ le 2026-08-25, et c'est la PREMIÈRE fois : il passe de **220** à **221**. Tous les récits
  # ci-dessous parlent d'un écart de 220 — ils décrivent l'arbre de leur date, ils ne sont pas faux, mais
  # l'invariant qu'ils énoncent a cessé de valoir. RAISON MESURÉE : le lot `P8.7-c` ajoute UN test qui ne
  # peut EXISTER que sous la feature (il vérifie que le secret du tier froid suit la provenance de la clé
  # d'ouverture, et la fonction qu'il exerce n'est visible que là). Les deux autres tests du même lot
  # (`P11.18-v`, la machine d'une alerte enfin SERVIE) sont sans `cfg` et entrent dans les deux profils.
  # Énumération du 2026-08-25 : **1331** par défaut, **1552** avec `cold_tier`, écart **221**. Un écart qui
  # bouge n'est pas une alerte en soi ; un écart qui bouge SANS RAISON ÉCRITE en serait une.
  # CONFIRMÉ le 2026-08-20 par une ÉNUMÉRATION INDÉPENDANTE du même arbre — **1125** par défaut,
  # **1345** avec `cold_tier`, écart **220**, `--list --ignored` toujours **0** — donc AUCUN chiffre
  # n'est déplacé ici. Cette énumération porte le lot P5.7-b (le SOC s'alerte sur sa propre mise à
  # jour : `daemon/src/tests/maj_corroboree.rs`), cinq tests SANS `cfg` de feature, présents dans les
  # DEUX profils, déjà compris dans le total ci-dessus : la table d'empreintes re-hachée depuis
  # `systemd/`, la couverture des unités que les installeurs déposent, la mise à jour légitime
  # reclassée avec son témoin négatif, le dépôt hostile sous six formes qui reste détecté, et le fait
  # de déploiement daté-audité. Aucun n'est un producteur d'alerte : la garde d'imputation
  # (`imputation_tout_producteur_d_alerte_declare_son_choix`) compte toujours les mêmes sites.
  # RÉ-ÉNUMÉRÉ le 2026-08-20 par une TROISIÈME lecture indépendante du même arbre — **1125** par
  # défaut, **1345** avec `cold_tier`, écart **220** — donc aucun chiffre n'est déplacé ici. Cette
  # énumération porte le lot S26 (clé S26 : ce que le moteur fait d'un tri est LU et non supposé —
  # `daemon/src/tests/tri_en_memoire_voie_unique.rs`), sept tests SANS `cfg` de feature, présents dans
  # les DEUX profils et déjà compris dans le total ci-dessus. Le lot RETIRE aussi une garde de
  # PROXIMITÉ devenue redondante (`aucune_lecture_sur_fichier_sans_plafond`, bornée aux ouvertures en
  # lecture seule) que la garde de VOIE UNIQUE couvre désormais en entier : +7 −1 sur ce module, et le
  # total reste celui de l'ARBRE, jamais une somme de lots.
  # RÉ-ÉNUMÉRÉ le 2026-08-20 (`cargo test -- --list`, jamais une somme de lots) sur l'arbre qui porte
  # le lot S32 — une lecture d'environnement qui échoue ne rend plus la valeur la plus rassurante
  # (`daemon/src/mesure_environnement.rs`, `daemon/src/tests/mesure_environnement.rs`) : dix-huit tests
  # SANS `cfg` de feature, présents dans les DEUX profils. L'arbre énuméré porte aussi des tests
  # d'autres lots en vol ; le total est celui de l'ARBRE, pas la somme d'un lot et d'une valeur d'avant.
  # RÉ-ÉNUMÉRÉ (`cargo test -- --list`, JAMAIS une somme) sur l'arbre qui porte le lot S33 — les
  # mesures d'HÔTE, la contre-pression et l'identité qui décide des actions locales
  # (`daemon/src/mesure_environnement.rs`, `daemon/src/handlers/actions.rs`,
  # `daemon/src/tests/mesure_environnement.rs`) : QUATRE tests de plus, sans `cfg` de feature, donc
  # présents dans les DEUX profils. La valeur d'avant cette ré-énumération avait elle-même bougé sous
  # un autre lot en vol : c'est précisément pourquoi le total est celui de l'ARBRE et jamais une somme.
  # RÉ-ÉNUMÉRÉ le 2026-08-23 (`cargo test --bin plume-daemon -- --list | grep -c ': test$'`) sur
  # l'arbre qui porte le lot P11.5-c — l'inventaire des comptes qui ACCÈDENT (un compte d'annuaire
  # externe administrait la console sans figurer dans aucune liste) et ce qu'une modification de
  # contenu d'overlay dit d'elle-même (`daemon/src/tests/comptes_qui_accedent.rs`) : douze tests SANS
  # `cfg` de feature, présents dans les DEUX profils. Le total est celui de l'ARBRE, jamais une somme
  # de lots.
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte le lot d'intégrité P11.5-d — la case
  # « Activée » du formulaire d'édition aussi durable que l'interrupteur de la ligne, la réimposition
  # d'un fichier config.d scopée à ce qu'elle possède, et le renommage d'un contenu adossé refusé :
  # six tests SANS `cfg` de feature, présents dans les DEUX profils.
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte la garde P6.8-d — les index
  # d'expression des champs chauds CONSTATÉS au catalogue d'une base migrée, l'attente DÉRIVÉE de
  # la déclaration et non énumérée à côté d'elle, dans les deux sens du levier `PLUME_EXPRINDEX` :
  # quatre tests SANS `cfg` de feature, présents dans les DEUX profils.
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte le correctif P6.8-f — le compte
  # rendu de la passe d'index CONSTATÉ au catalogue au lieu d'être récité depuis la déclaration :
  # trois tests SANS `cfg` de feature, présents dans les DEUX profils (la mutation qui nomme le
  # compte annoncé, son témoin inverse qui interdit un verdict accusant toujours, et la validation
  # de l'instrument — un nom déjà porté par un objet d'un autre type fait échouer la création).
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte le correctif P6.8-e — un champ
  # retiré de la liste chaude ne laisse plus son index à vie, le retrait de fond dérivant son
  # périmètre du PRÉFIXE de la famille et non de la liste des champs : cinq tests SANS `cfg` de
  # feature, présents dans les DEUX profils (la mutation par le vrai point d'entrée, le témoin
  # inverse qui interdit la purge trop large, la prudence d'un geste inconditionnel, l'ordre qui
  # tient la décision sur le levier, et la confrontation du préfixe à ce que le produit crée).
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte le correctif P6.8-g — le constat
  # de présence d'un index compare la DÉFINITION et non l'étiquette (un homonyme posé sur une autre
  # table, ou sur une autre expression, était compté « présent » et le court-circuit sortait sans rien
  # faire) : cinq tests SANS `cfg` de feature, présents dans les DEUX profils (deux mutations qui
  # nomment le compte annoncé, une qui nomme le TRAVAIL de la passe, le témoin inverse qui vaut
  # confrontation anti-no-op, et la validation de l'instrument — la création du produit ne déloge pas
  # un nom déjà porté par un index, donc l'état est stable).
  # RÉ-ÉNUMÉRÉ le 2026-08-24 (MÊME commande) sur l'arbre qui porte le correctif P10.1-b — la taille du
  # déversement des tris est BORNÉE par un quota déclaré, ou l'absence de borne est DITE : cinq tests
  # SANS `cfg` de feature, présents dans les DEUX profils (la mesure qui voit un temporaire délié que le
  # listage du répertoire ne voit pas, une mesure impraticable qui rend un verdict et pas un zéro, le
  # contrôle positif du démarrage, les quatre verdicts du quota, et la mutation dans les deux sens sur
  # un tri qui déverse pour de vrai).
  # LA MARCHE QUI MANQUAIT — RETROUVÉE PAR MESURE LE 2026-08-26, et c'est une dérive de JOURNAL, pas de
  # compteur. Entre la ligne ci-dessus (lot P10.1-b, 1286 par défaut) et la valeur que ce fichier portait
  # en entrant dans le lot d'aujourd'hui (1331), ce journal ne dit RIEN : `git log -S` sur les deux paires
  # rend le MÊME commit, `ed99dbf`, qui a posé 1286 -> 1331 et 1506 -> 1552 d'un seul geste sans ajouter
  # de ligne ici. Ce sont donc **45** tests entrés dans les DEUX profils, plus **un** test froid seul —
  # celui-là est nommé sur `EXPECTED_COLD_TESTS`, parce que c'est LUI qui a fait passer l'écart de 220 à
  # 221 avant que ce lot n'y ajoute le sien. Un compteur juste avec un journal muet reste une dérive : le
  # chiffre disait vrai, la phrase qui l'explique n'existait pas.
  # RÉ-MESURÉ le 2026-08-26 par `.github/scripts/compter-les-tests.sh` sur l'arbre qui prend `axum`
  # 0.8.9 : 1331 -> **1332**. Le +1 est UN test et un seul,
  # `router_json_refuse_les_octets_parasites_apres_le_document` : il traverse le routeur RÉEL pour
  # épingler un changement de CODE DE REFUS que la montée de version apporte — `axum::Json` appelle
  # `Deserializer::end()` depuis 0.8.5, si bien qu'un document JSON valide SUIVI d'octets parasites
  # passe de ACCEPTÉ (le reste ignoré) à **400**. Mesuré en liant les deux piles dans un même binaire
  # le 2026-08-26 : `{"user":"x","pass":"y"} parasite` -> accepté par 0.7.9, refusé 400 par 0.8.9.
  # Sans `cfg` de feature -> il entre AUSSI dans le profil `cold_tier` (voir EXPECTED_COLD_TESTS).
  # RÉ-MESURÉ le 2026-08-26 (MÊME commande) sur l'arbre qui NOMME les deux autres changements servis
  # par la montée `axum` : 1332 -> **1334**. Les +2 sont DEUX tests et deux seuls, tous deux sans
  # `cfg` de feature (donc +2 aussi sur EXPECTED_COLD_TESTS) :
  #   - `router_path_i64_ecrit_la_valeur_de_refus_sans_guillemets` — le CORPS d'un refus de `Path<i64>`
  #     passe de « Cannot parse `"abc"` » à « Cannot parse `abc` » (`{value:?}` -> `{value}` dans
  #     `src/extract/path/mod.rs`) SANS que le code change : 400 des deux côtés, donc aucune garde de
  #     code de refus ne pouvait le voir, sur 101 sites `Path<i64>` mesurés le 2026-08-26 ;
  #   - `router_un_segment_statique_gagne_sur_son_parametre_frere` — le moteur d'appariement a été
  #     REMPLACÉ (matchit 0.7.3 -> 0.8.4) et la précédence « statique avant paramètre » n'était tenue
  #     par aucun test, seulement par un commentaire qui en donnait un mécanisme FAUX.
  # RÉ-MESURÉ le 2026-08-27 par `.github/scripts/compter-les-tests.sh` (jamais par addition : une somme
  # juste par hasard masquerait un test perdu en chemin) sur l'arbre du lot P9 : 1334 -> **1349**, soit
  # +15, et le MÊME +15 sur `EXPECTED_COLD_TESTS` — l'écart entre les deux profils reste 222, INCHANGÉ,
  # ce qui est la seule chose qui dise qu'aucun test n'a été gagné ni perdu du côté de la feature.
  # Aucun des quinze n'est derrière un `cfg(feature)`, ce qui est cohérent avec ce déplacement conjoint :
  #   - 12 pour le magasin de secrets arrêté (`P9.8-a`, `tests/magasin_de_secrets.rs`), dont DEUX portent
  #     le dénominateur : « zéro pas prêt sur ZÉRO déclaré » ne vaut pas une santé — effacer les magasins
  #     pendant l'incident résolvait l'alerte — et sa BORNE, un émetteur qui ne publie pas de total ne
  #     doit pas hériter d'une alerte que rien ne peut plus résoudre ;
  #   - 3 pour la fausse couverture (`P9.5-a`, `tests/detection_aveugle.rs`) : la règle sans producteur
  #     semée éteinte sur une base NEUVE, puis le cas que celle-là ne peut pas voir — l'installation DÉJÀ
  #     déployée, où la ligne est restée active — et la garde qui refuse une seconde lecture des règles
  #     activées hors du point unique.
  # RÉ-MESURÉ le 2026-08-27 par `.github/scripts/compter-les-tests.sh` (les DEUX profils mesurés dans la
  # même exécution, jamais par addition) après la reprise adverse du lot P9 : 1349 -> **1351**, soit +2,
  # et le MÊME +2 sur `EXPECTED_COLD_TESTS` — l'écart entre les deux profils reste **222**, INCHANGÉ, ce
  # qui est la seule chose qui dise qu'aucun test n'a été gagné ni perdu du côté de la feature. Aucun des
  # deux n'est derrière un `cfg(feature)` :
  #   - `un_import_ne_ferme_pas_un_angle_mort_qu_aucun_producteur_ne_nourrit` (`P9.5-a`,
  #     `tests/detection_aveugle.rs`) — les DEUX membres du delta de couverture d'un import Sigma se
  #     comptent de la même manière. Le lot précédent filtrait le « avant » et pas le jeu importé, si bien
  #     qu'un import annonçait fermer un angle mort qu'il ne ferme pas ; trois sens sont éprouvés, dont le
  #     témoin POSITIF (un import que tout peut nourrir ferme bien) ;
  #   - `mds_le_module_avoue_que_son_producteur_n_est_pas_arme` (`P9.8-a`,
  #     `tests/magasin_de_secrets.rs`) — le module disqualifie son plus proche parent au motif qu'« un
  #     mécanisme posé mais non armé n'est pas un signal », et son PROPRE producteur est livré éteint sans
  #     qu'aucun capteur muet ne le couvre. Le témoin DÉRIVE les deux faits de l'arbre (l'amorçage, la
  #     table des capteurs) et exige l'aveu tant qu'ils tiennent — et son RETRAIT le jour où ils cessent.
  # DEUX témoins ont par ailleurs été RÉÉCRITS sans changer le compte : la jambe tautologique de
  # `une_base_deja_deployee_…` (elle affirmait « rendue comme angle mort », ce qui est vrai de TOUTE
  # technique à zéro règle) et le verdict de `aucune_surface_de_couverture_…`, dont les portes admises
  # sont désormais DÉRIVÉES de `detection_aveugle.rs` au lieu d'être un nom recopié.
  #
  # RÉ-MESURÉ le 2026-08-27 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la même
  # exécution, JAMAIS par addition) sur le lot P3→P8 et sa reprise adverse : 1351 -> **1376**, soit
  # +25, et le MÊME +25 sur `EXPECTED_COLD_TESTS` — l'écart entre les deux profils reste **222**,
  # INCHANGÉ. Aucun des vingt-cinq n'est derrière un `cfg(feature)`, et c'est cet écart inchangé qui
  # le dit, pas une lecture des sources. LA MESURE INTERMÉDIAIRE EST ÉCRITE, parce qu'elle réfute une
  # allégation reçue : l'arbre AVANT reprise mesurait 1368 / 1590, donc HEAD mesurait exactement 1351
  # et 1573 — il n'y avait AUCUNE dérive antérieure non reportée. Les +25 se répartissent ainsi
  # (répartition DÉRIVÉE du delta de déclarations par fichier, le TOTAL restant une mesure) :
  #   - 17 pour le lot lui-même : `tests/allowlist_du_responder.rs` (6, neuf, `P4.7-a`),
  #     `tests/entrees_scriptees_bornees.rs` (5, neuf, `P4.6-a`/`P4.6-b`), `tests/query_verify.rs`
  #     (+1, `P7.19-b`), `tests/sauvegarde_preschema.rs` (+4, `P4.4-n`), `tests/sondes_cout.rs`
  #     (+1, `P3.7-b`) ;
  #   - 8 pour la reprise adverse du 2026-08-27 : 7 dans `tests/entrees_scriptees_bornees.rs` (la
  #     dernière ligne sans saut de ligne final et son témoin inverse ; le plafond qui borne encore
  #     l'exécution sans `timeout` et son témoin inverse à compte exact ; le faux aveu de troncature
  #     sur un code 124 hors borne armée ; la clé d'aveu stable sous un compte qui change et son
  #     témoin inverse à deux bornes cumulables) et 1 dans `tests/allowlist_du_responder.rs` (le
  #     miroir, côté démon, du trou de lecture qui était côté shell).
  #
  # CE QUE « L'ÉCART » DIT, ET CE QU'IL NE DIT PAS — la règle, corrigée le 2026-08-28. Les strophes
  # ci-dessus écrivent, cinq fois, que « l'écart reste 222, ce qui est LA SEULE CHOSE qui dise qu'aucun
  # test n'a été gagné ni perdu du côté de la feature ». C'était vrai de CES lots-là, où aucun test
  # ajouté n'était derrière un `cfg(feature)` ; c'est FAUX comme règle, et le premier lot qui ajoute des
  # tests DANS le module froid le montre. La règle juste : un écart qui BOUGE n'est pas suspect en soi
  # — il l'est quand il bouge SANS RAISON ÉCRITE. La raison se dérive : `daemon/src/main.rs` porte
  # `#[cfg(feature = "cold_tier")] mod cold_store;`, donc tout test déclaré sous `daemon/src/cold_store/`
  # ne compte QUE dans le profil froid, et l'écart doit bouger d'EXACTEMENT ce nombre-là. Ce qui reste
  # un signal, inchangé : un écart qui bouge d'un AUTRE nombre que celui des tests ajoutés ou retirés
  # sous la feature.
  #
  # RÉ-MESURÉ le 2026-08-28 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la même
  # exécution, JAMAIS par addition) sur le lot P10 et sa reprise adverse : 1376 -> **1382** (+6) et
  # 1598 -> **1618** (+20). L'écart entre les deux profils passe donc de **222 à 236**, et le +14 est
  # ÉCRIT : ce sont les quatorze tests déclarés dans `daemon/src/cold_store/tests.rs`, module gaté par
  # la feature. Les deux dérivations FERMENT contre la mesure — 1376+6 = 1382 et 1598+6+14 = 1618 —
  # et c'est cette fermeture, pas une lecture des sources, qui dit qu'aucun test n'a été perdu en
  # chemin. Répartition DÉRIVÉE du delta de déclarations par fichier (le TOTAL restant une mesure) :
  #   - LES DEUX PROFILS (+6, aucun `cfg(feature)` dans ces fichiers) : `tests/portillon_avoue.rs`
  #     (3, neuf — l'aveu de portillon unique et sa garde dérivée, `P10.7-c`), `tests/keyset.rs` (+2 :
  #     l'ordre du wrap ancré en clair pour `P10.5-f`, et la garde DÉRIVÉE qui refuse qu'un module de
  #     `web/` RECONSTRUISE un curseur keyset au lieu de renvoyer celui qu'il a reçu),
  #     `tests/index_usage_observatoire.rs` (+1) ;
  #   - PROFIL FROID SEUL (+14, tous dans `daemon/src/cold_store/tests.rs`) : 11 pour le lot lui-même
  #     (browse keyset colonnaire, exactitude froide, provenance) et 3 pour la reprise adverse du
  #     2026-08-28 — la garde mono-env jugée sur la FENÊTRE et non sur la page, l'aveu de provenance
  #     de la voie colonnaire sur une page que le chaud a remplie seule, et le refus qui ne promet
  #     plus « exacte » une voie que le produit publie approximative.
  #
  # RÉ-MESURÉ le 2026-08-28 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la même
  # exécution) ET par une exécution RÉELLE des deux suites, sur la reprise adverse du lot P10 :
  # 1382 -> **1384** (+2) et 1618 -> **1624** (+6). L'écart passe de 236 à **240**, et le +4 est ÉCRIT :
  # QUATRE tests ne comptent que dans le profil froid, et pour DEUX raisons distinctes —
  #   · 2 sont déclarés dans `daemon/src/cold_store/tests.rs`, module que `main.rs` gate par la feature ;
  #   · 2 vivent dans `daemon/src/tests/keyset.rs` (compilé dans les DEUX profils) mais portent
  #     eux-mêmes `#[cfg(feature = "cold_tier")]`, parce qu'ils éprouvent une règle qui n'existe pas
  #     en mode 0 (l'espace d'identifiant d'un curseur froid). C'est le premier lot où l'écart bouge
  #     pour cette seconde raison : « sous `cold_store/` » n'est donc PAS la règle, la règle est
  #     « derrière la feature, où que le test soit écrit ».
  # Les deux dérivations FERMENT contre la mesure — 1382+2 = 1384 et 1618+2+4 = 1624 — et c'est cette
  # fermeture, pas une lecture des sources, qui dit qu'aucun test n'a été perdu en chemin. Répartition
  # DÉRIVÉE du delta de déclarations par fichier (le TOTAL restant une mesure) :
  #   - LES DEUX PROFILS (+2, `tests/keyset.rs`, aucun `cfg`) : la PORTE de la voie colonnaire n'est
  #     écrite qu'une fois et le refus la précède (`P10.5-g`) ; le retrait des colonnes d'aide n'a
  #     qu'un site, parce qu'une page keyset n'a plus qu'une SORTIE ;
  #   - PROFIL FROID SEUL (+4) : 2 dans `tests/keyset.rs` sous `cfg(feature)` — les quatre causes qui
  #     ferment la voie colonnaire, éprouvées une par une, et le refus d'un curseur marqué vu par le
  #     ROUTEUR RÉEL (422 nommé, et servi sans la marque) ; 2 dans `cold_store/tests.rs` — chaque
  #     espace d'identifiant va à son lecteur et aucun autre, et le message d'exactitude ne promet
  #     « exacte » aucune forme qu'une route pré-agrégée intercepte.
  #
  # RÉ-MESURÉ le 2026-08-28 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la même
  # exécution) ET par une exécution RÉELLE des deux suites, sur la SEPTIÈME reprise du même module —
  # celle qui ferme la FAMILLE « on n'infère jamais l'espace d'un curseur » : 1384 -> **1385** (+1) et
  # 1624 -> **1628** (+4). L'écart passe de 240 à **243**, et le +3 est ÉCRIT : trois tests ne comptent
  # que dans le profil froid, pour les DEUX raisons déjà nommées —
  #   · 1 est déclaré dans `daemon/src/cold_store/tests.rs`, module gaté par la feature ;
  #   · 2 vivent dans `daemon/src/tests/keyset.rs` (compilé dans les DEUX profils) mais portent
  #     eux-mêmes `#[cfg(feature = "cold_tier")]`, parce qu'ils éprouvent des règles qui n'existent pas
  #     en mode 0 (l'espace d'identifiant d'un curseur froid, l'aveu de part froide).
  # Les deux dérivations FERMENT contre la mesure — 1384+1 = 1385 et 1624+1+3 = 1628. Répartition
  # DÉRIVÉE du delta de déclarations par fichier (le TOTAL restant une mesure) :
  #   - LES DEUX PROFILS (+1, `tests/keyset.rs`, aucun `cfg`) : la règle d'entrée sur le curseur n'est
  #     pas RECOPIÉE dans le handler, elle y est LUE — et la déduction supprimée (la routabilité d'une
  #     traversée) n'est plus nommée nulle part dans le démon, marche d'arbre à l'appui ;
  #   - PROFIL FROID SEUL (+3) : 2 dans `tests/keyset.rs` sous `cfg(feature)` — la règle d'entrée jugée
  #     cause par cause, jambes NÉGATIVES comprises (une garde qui refuserait tout casserait la
  #     pagination chaude), et l'aveu de part froide qui doit s'accompagner de l'aveu de VOIE
  #     (`stats.served_from`, le champ auquel le refus d'exactitude renvoie le lecteur) ; 1 dans
  #     `cold_store/tests.rs` — l'empreinte de numérotation SUIT ce qui numérote, mesurée sur le rang
  #     d'une même ligne dans deux hydratations qui ne diffèrent que d'UNE SECONDE de borne basse.
  #
  # RÉ-MESURÉ le 2026-08-28 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la MÊME
  # exécution, JAMAIS par addition), sur le lot `P10.5-i` — « un panneau dit jusqu'où il a pu voir, et le
  # nombre ne bouge pas » : **1385 -> 1393** (+8), et le MÊME +8 sur `EXPECTED_COLD_TESTS`
  # (**1628 -> 1636**). LA DÉRIVATION DE L'ÉCART ENTRE PROFILS, qui est ce que ces deux nombres disent
  # ensemble : 1628−1385 = 243 AVANT, 1636−1393 = 243 APRÈS — INCHANGÉ. C'est cet écart inchangé qui
  # établit qu'aucun des huit n'est derrière un `cfg(feature)`, pas une lecture des sources : le module
  # est tiré par un `include!` non gaté, et la seule ligne du lot qui touche au froid est un `assert!`
  # porteur d'un `#[cfg(feature = "cold_tier")]` À L'INTÉRIEUR d'un test qui existe dans les deux profils.
  # UNE ALLÉGATION VOISINE EST RÉFUTÉE PAR LA MÊME MESURE, et elle est laissée telle quelle plus haut
  # faute d'appartenir à ce lot : le paragraphe du 2026-08-27 écrit « l'écart entre les deux profils reste
  # 222 » alors que les deux compteurs qu'il accompagne (1385 / 1628) en donnent 243.
  # Les huit sont TOUS dans `tests/panneau_avoue.rs` (neuf) : l'horizon avoué (positif sur la famille la
  # plus amputée, trois négatifs dont l'anti-fatigue et la marge dérivée du cache), la valeur de l'horizon
  # sous mutation de son levier + le défaut quand le pool de lecture manque, le couple provenance
  # opaque/routée avec le geste naïf rejoué en mutant, l'identité BYTE À BYTE de `columns`/`rows` sur les
  # familles semées, la garde dérivée de la table du cache rendu, la garde dérivée de la table de routage
  # et de la FORME du corps servi, la re-estampe SWR qui ne certifie pas une fenêtre non vue, et la
  # population dérivée des points d'exécution.
  #
  # RÉ-MESURÉ le 2026-08-28 par le MÊME script (les deux profils dans la MÊME exécution, jamais par
  # addition), sur la REPRISE de `P10.5-i` après trois lectures adverses : **1393 -> 1395** (+2), et le
  # MÊME +2 sur `EXPECTED_COLD_TESTS` (**1636 -> 1638**). L'ÉCART ENTRE PROFILS EST DE NOUVEAU DÉRIVÉ :
  # 1636−1393 = 243 AVANT, 1638−1395 = 243 APRÈS — INCHANGÉ, donc aucun des deux n'est derrière un
  # `cfg(feature)`. Les deux nouveaux tiennent les deux constats BLOQUANTS de la reprise : la table des
  # familles de rétention ne peut plus nommer une clé absente de `RETENTION_FIELDS` ni se tromper d'UNITÉ
  # (c'est ce qui faisait annoncer 90 JOURS de portée sur une table coupée à 48 HEURES) ; et les deux
  # portes du coffre compilent le MÊME SQL brut, ce qui est l'égalité — jusque-là tenue par un seul script
  # hors arbre — qui autorise la réduction de trois sites de compilation à deux branches.
  #
  # RÉ-MESURÉ le 2026-08-28 par `.github/scripts/compter-les-tests.sh` (les DEUX profils dans la MÊME
  # exécution, JAMAIS par addition), sur le lot `P4.7-b` — « le prédicat d'adresse annoncé COMMUN aux
  # deux lecteurs divorce sur toute la famille IPv6 » : **1395 -> 1403** (+8), et le MÊME +8 sur
  # `EXPECTED_COLD_TESTS` (**1638 -> 1646**). LA DÉRIVATION DE L'ÉCART ENTRE PROFILS, qui est ce que
  # ces deux nombres disent ensemble : 1638−1395 = 243 AVANT, 1646−1403 = 243 APRÈS — INCHANGÉ. C'est
  # cet écart inchangé qui établit qu'aucun des huit n'est derrière un `cfg(feature)`, pas une lecture
  # des sources : les huit sont dans `daemon/src/tests/allowlist_du_responder.rs`, un module tiré par
  # un `include!` non gaté, et aucun n'écrit `#[cfg(feature = ...)]`.
  # LES HUIT, ET CE QU'ILS TIENNENT. Cinq lisent le CORPUS PARTAGÉ `collectors/predicat-adresse.corpus`
  # — le fichier que le témoin Rust et `.github/scripts/check_enforcer_lists_fail_closed.py` rejouent
  # tous deux, chacun sur SON lecteur, ce qui n'existait pas : la validation de l'instrument (trois
  # combinaisons présentes, la quatrième interdite), le verdict du démon ligne à ligne, la CONTENANCE
  # (tout ce que l'agent lit comme adresse, le démon le refuse), l'absence de SILENCE À DEUX, et le
  # témoin inverse (les noms de service sont encore retenus, sinon « refuser tout » passerait pour une
  # correction). Les trois autres tiennent la borne d'enforcement INCHANGÉE clause pour clause (ce
  # lot n'a pas le droit d'élargir ce qui part vers un pare-feu), la séparation des DEUX questions
  # (classificateur vs borne de ban, dont la fusion redeviendrait le défaut), et le défaut lui-même —
  # une liste d'épargne IPv6, masque et zone compris, refusée en NOMMANT l'autre politique.
  #
  # RÉ-MESURÉ le 2026-08-28 par le MÊME script (les DEUX profils dans la MÊME exécution, JAMAIS par
  # addition), sur la REPRISE de `P4.7-b` après deux lectures adverses : **1403 -> 1410** (+7), et le
  # MÊME +7 sur `EXPECTED_COLD_TESTS` (**1646 -> 1653**). L'ÉCART ENTRE PROFILS EST DE NOUVEAU DÉRIVÉ :
  # 1646−1403 = 243 AVANT, 1653−1410 = 243 APRÈS — INCHANGÉ, donc aucun des sept n'est derrière un
  # `cfg(feature)` ; c'est l'écart qui l'établit, pas une lecture des sources.
  # LES SEPT, ET CE QU'ILS FERMENT — trois constats, tous MESURÉS avant d'être crus :
  #   · (P1) N'ÉTAIT TENUE QUE SUR 30 LIGNES ÉCRITES À LA MAIN, alors que le corpus s'annonçait « la
  #     définition commune » et que les textes servis à l'exploitant énonçaient un UNIVERSEL. DEUX
  #     témoins de BALAYAGE la décomposent : « tout ce qui satisfait la borne structurelle (S) est
  #     refusé par le lecteur » sur toutes les chaînes de ≤ 4 caractères d'un alphabet à un
  #     représentant par classe (avec ses deux planchers de non-dégénérescence : 1225 chaînes sous (S)
  #     mesurées, 17 379 retenues hors (S)), et « le classificateur du démon EST exactement (S) », qui
  #     est ce qui rend la composition avec la moitié CI légitime. La moitié agent vit dans
  #     `check_enforcer_lists_fail_closed.py`, sur un alphabet DÉRIVÉ de la ligne `is_ip` livrée —
  #     mesuré : la forme crochetée `[::1]` échappait à un alphabet écrit à la main, plus maintenant.
  #   · LE RENDU TYPÉ SE DISAIT « JAMAIS PLUS PERMISSIF QUE LA VALIDATION AMONT » ET IL L'ÉTAIT : le
  #     premier jet avait supprimé UNE copie de la borne et laissé les deux autres, dont un plancher
  #     de PID FAUX (`p > 0` contre `p > 300`). DEUX témoins : la grille de 19 couples action/cible où
  #     le rendu natif ne peut pas dépasser la borne amont (avec son témoin inverse — un rendu qui
  #     refuserait tout passerait sinon), et la séparation FORME / POLITIQUE que `run_playbooks` lit.
  #   · UNE RIPOSTE HORS CAPACITÉ ÉTAIT JETÉE SANS UN CHIFFRE (`P4.7-d`) : TROIS témoins — le défaut
  #     (cible IPv6 et PID sous plancher : bilan à 1, aucune action posée) et ses DEUX inverses (une
  #     IP protégée ne gonfle pas le bilan ; une riposte portable est encore posée, bilan à zéro).
  #
  # RÉ-MESURÉ le 2026-08-28 par le MÊME script, LES DEUX PROFILS DANS LA MÊME EXÉCUTION (jamais par
  # addition), sur la fermeture de la FAMILLE DE L'IDENTITÉ D'ADRESSE (`P4.7-g`, `-h`, `-i`, `-j`,
  # `-k`) : **1410 -> 1416** (+6), et le MÊME +6 sur `EXPECTED_COLD_TESTS` (**1653 -> 1659**).
  # L'ÉCART ENTRE PROFILS EST DE NOUVEAU DÉRIVÉ : 1653−1410 = 243 AVANT, 1659−1416 = 243 APRÈS —
  # INCHANGÉ, donc aucun des six n'est derrière un `cfg(feature)` ; c'est l'écart qui l'établit, pas
  # une lecture des sources. Les six vivent dans `daemon/src/tests/identite_d_adresse.rs`, tiré par un
  # `include!` non gaté, et aucun n'écrit `#[cfg(feature = ...)]`.
  # LES SIX, ET CE QU'ILS FERMENT :
  #   · L'IDENTITÉ D'UNE ADRESSE EST SA VALEUR — propriété MÉTAMORPHIQUE ENGENDRÉE depuis le corpus
  #     partagé `collectors/predicat-adresse.corpus` (aucune paire écrite : les réécritures — mappée
  #     pointée, majuscule, expansée, hexadécimale, compressée — sont dérivées de la valeur analysée),
  #     avec son ANTI-VACUITÉ (une fonction constante satisferait la propriété seule : on exige une
  #     adresse protégée ET une non protégée sous matchers non vides).
  #   · LA LARGEUR PROTÉGÉE EST CELLE QUI A ÉTÉ ÉCRITE — les quatre bornes (base, dernière, base−1,
  #     dernière+1) DÉRIVÉES de l'arithmétique du masque DANS le témoin, jamais recopiées ; c'est le
  #     seul qui attrape les DEUX directions (`203.0.113.0/25` protégeait tout le /24 ; `128.0.0.0/1`
  #     ne protégeait qu'UNE adresse et est désormais REFUSÉ).
  #   · UNE NOTATION QUE LE PRODUIT NE SAIT PAS HONORER SE REFUSE, ELLE NE S'ACCEPTE PAS DÉFORMÉE —
  #     joker hors frontière, masque sous plancher, nom d'hôte, faute de frappe : refus NOMMÉ, jamais
  #     matcher inerte ; avec son inverse (un item vide ne signale RIEN, six items légitimes acceptés).
  #   · TOUTE CIBLE DE BAN S'ANALYSE — le contrat qui autorise `ip_is_protected_ctx` à rendre `false`
  #     sur l'inanalysable sans rouvrir de trou : le refus d'analyse est un défaut de FORME, tranché
  #     EN AMONT par la borne d'enforcement (donc COMPTÉ dans `abandonnes`), pas une réponse négative
  #     de la politique. Le défaut de `P4.7-h` (`10.0.0.01` non protégée ET bannissable) est joué.
  #   · LA LEVÉE DE BAN DIT CE QU'ELLE A RETIRÉ — AVANT/APRÈS dans le même corps, sur la valve de
  #     récupération anti-verrouillage elle-même : posé sous la valeur canonique, la saisie BRUTE ne
  #     levait RIEN et l'API répondait `{"ok": true}`.
  #   · LE PAIR MAPPÉ EST LA MÊME MACHINE — et RIEN de plus : l'anti-spoof est joué dans les deux sens
  #     (un pair public replié reste public), et un CIDR dans `PLUME_TRUSTED_PROXIES` n'apparie
  #     toujours rien (l'élargissement de confiance est une décision distincte, hors de ce lot).
  # UN SEPTIÈME TÉMOIN A CHANGÉ DE FORME SANS CHANGER LE COMPTE, et c'est le plus important à lire :
  # `la_borne_de_ban_reste_celle_d_avant_clause_pour_clause` exigeait une ÉGALITÉ avec les quatre
  # clauses de `P4.7-b` — elle aurait INTERDIT la correction de `P4.7-h`. Elle est devenue
  # `la_borne_de_ban_ne_s_elargit_jamais_et_son_resserrement_est_derive` : (E) la borne n'accepte
  # JAMAIS plus qu'avant — la garantie de `P4.7-b` intégralement conservée, rien de neuf ne part vers
  # un pare-feu ; (R) l'écart entre l'ancienne borne et la nouvelle est EXACTEMENT « la chaîne ne
  # s'analyse pas », dérivé et non énuméré, avec non-vacuité dans les deux sens.
  #
  # RÉ-MESURÉ le 2026-08-29 par le MÊME script, LES DEUX PROFILS DANS LA MÊME EXÉCUTION (jamais par
  # addition), sur la REPRISE de la famille de l'identité d'adresse après trois lectures adverses :
  # **1416 -> 1419** (+3), et le MÊME +3 sur `EXPECTED_COLD_TESTS` (**1659 -> 1662**). L'ÉCART ENTRE
  # PROFILS EST DE NOUVEAU DÉRIVÉ : 1659−1416 = 243 AVANT, 1662−1419 = 243 APRÈS — INCHANGÉ, donc
  # aucun des trois n'est derrière un `cfg(feature)` ; c'est l'écart qui l'établit, pas une lecture
  # des sources.
  # LES TROIS, ET CE QU'ILS FERMENT — chacun ferme un défaut que le premier jet du lot avait CRÉÉ ou
  # laissé ouvert, et chacun a été mesuré avant d'être écrit :
  #   · CE QUI EST PUBLIÉ EST CE QUI EST APPLIQUÉ — `etendue_du_reseau` masquait SANS replier la
  #     forme mappée là où `ip_in_cidr`, le DÉCIDEUR, replie des DEUX côtés : `::ffff:203.0.113.0/120`
  #     était PUBLIÉ « 256 adresses protégées » (registre never-ban, journal d'amorçage, refus de
  #     scope) et n'en protégeait qu'UNE. La propriété est DÉRIVÉE — pour tout item accepté, les deux
  #     bornes publiées appartiennent au réseau selon le décideur et les deux voisines immédiates
  #     n'y appartiennent pas — donc un item écrit demain est couvert sans être nommé.
  #   · LES DEUX ANALYSEURS DU MÊME CSV, ET L'ÉCART QUI RESTE — parité d'ACCEPTATION rétablie
  #     (`« 172.16.0.0 /12 »` était HONORÉ par l'affichage et REFUSÉ par la denylist : l'exploitant
  #     voyait son exclusion fonctionner dans les panneaux pendant qu'il n'était plus protégé), et
  #     l'écart de SÉMANTIQUE qui reste — non refermable sans casser le débruitage — est MESURÉ sur
  #     le couple (CACHÉE, BANNISSABLE), le seul sens qui compte : `172.15.0.1` reste masquée par le
  #     préfixe « 172. » ET devient bannissable. Aucun témoin ne jouait ce couple.
  #   · LA BORNE DIT IPv4, ET LA LEVÉE RESTE OUVERTE — la forme IPv4-compatible obsolète
  #     (`::127.0.0.1`) et la forme mixte (`2001:db8::192.0.2.1`, qui est DANS le corpus partagé)
  #     s'analysaient, restaient des cibles de ban ACCEPTÉES et demeuraient INVISIBLES à la
  #     protection : les deux conditions de la fuite, sur une autre écriture. Et la dissymétrie
  #     assumée est tenue par le même témoin : un `unban_ip` sur une notation que le BAN refuse reste
  #     CRÉABLE — une soupape lève PLUS, jamais moins.
  # ET LE TÉMOIN DE LA BORNE A CHANGÉ DE FORME SANS CHANGER LE COMPTE, une seconde fois : (R) disait
  # « l'écart est le refus d'ANALYSE » et dit désormais « l'écart est : la cible ne dénote pas une
  # valeur IPv4 » ; une clause (L) s'y ajoute — la borne de la LEVÉE est celle d'avant le lot, CLAUSE
  # POUR CLAUSE, c'est-à-dire l'égalité de `P4.7-b` conservée là où elle doit l'être : sur la valve.
  #
  # RÉ-MESURÉ le 2026-08-29 par le MÊME script, LES DEUX PROFILS DANS LA MÊME EXÉCUTION (jamais par
  # addition), sur le correctif de `P4.13-a` — sans mandataire, l'écran de login ne pouvait JAMAIS
  # s'afficher : **1419 -> 1425** (+6), et le MÊME +6 sur `EXPECTED_COLD_TESTS` (**1662 -> 1668**).
  # L'ÉCART ENTRE PROFILS EST DE NOUVEAU DÉRIVÉ : 1662−1419 = 243 AVANT, 1668−1425 = 243 APRÈS —
  # INCHANGÉ, donc aucun des six n'est derrière un `cfg(feature)` ; c'est l'écart qui l'établit.
  # LES SIX, ET CE QU'ILS FERMENT — ils vivent dans `daemon/src/tests/fermeture_shell_spa.rs` :
  #   · LA FERMETURE DES IMPORTS ES STATIQUES DEPUIS `/app.js` EST CELLE QUE LA GARDE PORTE, DANS LES
  #     DEUX SENS — atteignable mais non exempté (le login casse), exempté mais plus atteignable
  #     (surface publique morte). L'ensemble est DÉRIVÉ, jamais énuméré : 49 des 50 modules de la
  #     console, le cinquantième (`sw.js`) étant déjà exempté à un autre titre.
  #   · CE QUE LE DOCUMENT D'ENTRÉE ET SA FEUILLE RÉFÉRENCENT DIRECTEMENT EST EXEMPTÉ, ET RIEN DE
  #     PLUS — les trois listes ne se recouvrent pas et chaque chemin désigne un fichier qui existe.
  #   · L'ORDRE EST CE QUE LA DICHOTOMIE EXIGE — listes strictement croissantes (donc sans doublon),
  #     aucun chemin d'interface, aucune collision avec la table de routage.
  #   · L'INSTRUMENT EST VALIDÉ DANS LES DEUX SENS AVANT DE SERVIR — le faux positif est CELUI DU
  #     DÉPÔT (« post-import ( » en prose française dans `sigmaimport.js`, lu comme un import
  #     dynamique par un motif naïf) et le témoin inverse exige qu'un VRAI `import(` soit VU.
  #   · LA PREUVE PAR SERVICE — le routeur RÉEL monté sur le `web/` livré, interrogé en HTTP sans
  #     identité : la racine rend le shell (et non douze octets de texte), et tout ce que le shell
  #     EXIGE (population RECALCULÉE, jamais la liste qu'on vérifie) est rendu BYTE À BYTE.
  #   · LES DEUX TÉMOINS NÉGATIFS — les lectures de la table de routage restent 401 pour un anonyme,
  #     et aucun fichier de `web/` hors fermeture (ni voisin de nom : `/app.js.map`, `/App.js`) ne
  #     devient public ; un élargissement en préfixe ou en suffixe rougit là.
  # CE QUE LA MUTATION A MESURÉ, ET C'EST LA LEÇON DU LOT : le correctif RETIRÉ, les cinq témoins qui
  # LISENT UN ARBRE restent VERTS et seul celui qui SERT rougit. C'est `P8.27-i` en une mesure.
  #
  # `P4.13-a` (REPRISE, 2026-08-29) — SIX TÉMOINS DE PLUS, chacun sur un défaut que DEUX CRITIQUES
  # ADVERSES ont vu et qu'AUCUN témoin du lot ne tenait. MESURÉ par `compter-les-tests.sh`, les deux
  # profils dans la même exécution : **1425 -> 1431** et **1668 -> 1674**, +6 des deux côtés — L'ÉCART
  # ENTRE PROFILS RESTE 243, donc aucun des six n'est derrière un `cfg(feature)`, et c'est l'écart qui
  # l'établit, pas une lecture des sources. Ce qu'ils tiennent :
  #   · LA DÉRIVATION DES ASSETS REFUSE ce qu'elle ne lit pas (guillemets simples, valeur non quotée,
  #     `srcset`/`poster`/`xlink:href`/`formaction`, `url(` du `<style>` EN LIGNE, `@import` des deux
  #     côtés) — elle les IGNORAIT en silence, donc restait verte pendant que l'écran de login se
  #     dégradait ; fixtures jouées dans les deux sens, document livré en contrôle positif.
  #   · LA LICENCE ACCOMPAGNE LA FONTE DISTRIBUÉE — règle DÉRIVÉE (tout texte de licence d'un
  #     répertoire dont une fonte est publique l'est aussi), dans les deux sens. La SIL OFL 1.1 l'exige
  #     et les quatre `.woff2` sont servis à un anonyme depuis le lot.
  #   · LE FILET DE SÉCURITÉ DES 6 s EST AUTORISÉ PAR LA POLITIQUE SERVIE — son empreinte sha256 est
  #     RECALCULÉE depuis `web/index.html` et cherchée dans l'en-tête que le serveur ÉMET. Il ne
  #     s'exécutait dans AUCUN navigateur (`script-src 'self'` sans nonce ni empreinte) : le document
  #     promettait par écrit un mécanisme mort, sur le cas même que ce lot rend atteignable.
  #   · UNE AUTORITÉ NON DÉCLARÉE REÇOIT UNE PHRASE — la preuve n'avait JAMAIS fait varier le `Host`
  #     (ses trois valeurs étaient celles que `host_guard` auto-accepte), et ce second mur rendait
  #     « bad host », huit octets : la forme exacte du défaut qu'on venait de fermer.
  #   · LE BUDGET D'OCTETS DE LA SURFACE PUBLIQUE borne ce que la porte ouverte coûte, sans gêner un
  #     chargement complet ni toucher l'API. MESURÉ AU BANC — poste de développement, démon du dépôt
  #     lancé à la main sur l'arbre, rafales fabriquées ; aucune installation n'est décrite ici — un
  #     anonyme passe de 12 octets / 0,21 ms d'UC à 167 542 octets / 13,28 ms (dont 95 % de `gzip`,
  #     ~6,5 ms par arithmétique en release), soit 63 × un 401 et 92,7 Mo/s au banc pour une seule IP.
  #   · CHAQUE RÉPONSE PUBLIQUE EST FACTURABLE (`content-length` présent ET égal au fichier) : sans
  #     cela, la borne ci-dessus aurait un trou muet.
  # `P11.24-e` (2026-09-02) : **+1**, et un seul — `fraicheur_de_tick_injectee.rs` porte la garde
  # STRUCTURELLE qui interdit à un test d'écrire la fraîcheur ambiante des boucles de fond
  # (`aucun_test_n_ecrit_la_fraicheur_ambiante_des_boucles_de_fond`). Le témoin de santé
  # `day2_component_health_rgy` a CHANGÉ DE FORME sans changer de compte : il décide désormais sur
  # une fraîcheur INJECTÉE (`component_health_avec`) au lieu d'une atomique de processus qu'un autre
  # témoin du même binaire écrasait. `P11.23-e` n'ajoute AUCUN test : sept branches muettes
  # empruntent le canal de refus existant, et le plancher du corpus partagé est une assertion de
  # plus dans un auxiliaire, pas une fonction de test. Le MÊME +1 sur `EXPECTED_COLD_TESTS` — la
  # garde n'est derrière aucun `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils
  # (**245** avant comme après) qui l'établit, pas une lecture des sources.
  # `P11.23-g` (2026-09-02) : **+1**, et un seul — `plancher_des_tables_declarees.rs` porte le
  # témoin de son propre instrument (`le_plancher_des_tables_declarees_accuse_l_instrument_et_
  # nomme_la_table`), qui joue le plancher sur DEUX tables FABRIQUÉES dans le test, l'une pleine et
  # l'autre vide. Les DIX consommateurs, eux, n'ajoutent aucun test : leurs dix-neuf boucles
  # porteuses changent d'expression itérée (`TABLE` -> `table_declaree!(TABLE)`) sans changer de
  # compte. Le MÊME +1 sur `EXPECTED_COLD_TESTS` — le témoin n'est derrière aucun `cfg(feature)`,
  # et c'est l'ÉCART INCHANGÉ entre les deux profils (**245** avant comme après) qui l'établit, pas
  # une lecture des sources.
  # `P7.19-f` et `P7.19-g` (2026-09-03) : **+2**, dérivés du diff et non d'un rapport —
  # `lestimation_de_lignes_ne_retient_jamais_la_ligne_dun_index_partiel` (la jauge ne retient plus
  # la ligne d'un index PARTIEL, dont la première grandeur décrit le SOUS-ENSEMBLE et non la table ;
  # la partialité est DEMANDÉE au catalogue du moteur, aucun nom d'index n'est écrit) et
  # `la_version_du_moteur_de_stockage_est_epinglee_et_son_comportement_danalyse_avec` (deux épingles :
  # la version, constante écrite à la main contre `sqlite_version()` lu sur le moteur lié ; et le
  # COMPORTEMENT dont deux témoins de cet arbre dépendent, mesuré par un `ANALYZE` réellement
  # exécuté). Le cran 0 du régime de statistiques CHANGE DE FORME sans changer de compte : il affirme
  # désormais trois propriétés vraies des DEUX côtés du moteur, sur une population de tables DÉRIVÉE
  # du catalogue au lieu d'une table nommée. Le correctif du verrou d'environnement dans
  # `tests/ingest.rs` n'ajoute AUCUN test : c'est une ligne dans un témoin existant. Le MÊME +2 sur
  # `EXPECTED_COLD_TESTS` — aucun des deux n'est derrière un `cfg(feature)`, et c'est l'ÉCART
  # INCHANGÉ entre les deux profils (**245** avant comme après) qui l'établit, pas une lecture des
  # sources.
  # `P7.19-i` (2026-09-03) : **+3**, dérivés du diff — les trois témoins de
  # `la_ligne_retenue_est_choisie.rs`, un par loi de sélection, chacun FABRIQUANT l'ensemble
  # multiple qu'aucune donnée de banc ne produisait et REJOUANT l'ancienne lecture telle qu'elle
  # était : même dossier, même ligne étrangère, placée devant elle rend le runbook ÉTRANGER,
  # placée derrière elle rend le vrai. Les trois autres fonctions ajoutées par ce passage sont des
  # AUXILIAIRES (une lecture corrigée partagée par deux surfaces, un rejeu de l'ancienne forme, une
  # fabrique de fixture) et n'ajoutent aucun test. `P7.19-j` n'en ajoute AUCUN : ses douze prises de
  # verrou sont une ligne dans des témoins existants, et sa garde a été REFUSÉE avec son chiffre.
  # Le MÊME +3 sur `EXPECTED_COLD_TESTS` — aucun n'est derrière un `cfg(feature)`, et c'est l'ÉCART
  # INCHANGÉ entre les deux profils (**245** avant comme après) qui l'établit, pas une lecture des
  # sources.
  # `P11.20-n` et `P11.20-h` (2026-09-03) : **+14**, comptés par ÉNUMÉRATION et non par motif — huit
  # `#[test]` et six `#[tokio::test]`, forme que le premier relevé avait ratée. Les deux tiers tiennent
  # la CONFIDENTIALITÉ : un objet sans propriétaire cessait d'être un accident de colonne vide, et la
  # portée de lecture donnait à TOUT compte l'autorité de propriétaire sur les objets semés — panneaux
  # privés servis en texte ET en données, figés dans un instantané partageable, plus l'autorité de
  # suppression. Le témoin qui décide joue DEUX IDENTITÉS distinctes, avec son contrôle positif (le
  # propriétaire, lui, voit) et le bien commun préservé. Un second assermente la DÉCISION de
  # resserrement, motif interpolé dans chaque message d'échec pour que le prochain ne la prenne pas
  # pour une régression. Le MÊME +14 sur `EXPECTED_COLD_TESTS` — aucun n'est derrière un
  # `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (**245** avant comme après) qui
  # l'établit, pas une lecture des sources.
  # `P8.10-m` — +7 LE 2026-09-03, APRÈS UNE PANNE DE PRODUCTION DE TROIS HEURES. Le contrat de schéma
  # force la construction des tables virtuelles ; celle de l'index plein-texte a rendu SQLITE_CORRUPT et
  # le démon s'est arrêté — aucune requête, aucune ingestion, aucune surface de santé. Le refus était
  # HONNÊTE (l'index était réellement abîmé) mais son PRIX ne l'était pas : cet index est déclaré à
  # contenu EXTERNE, il ne porte aucune donnée propre, sa source était intacte, et il s'est rebâti en
  # quelques minutes. La porte distingue désormais un objet DÉRIVÉ d'un objet SOURCE — le premier
  # se reconstruit, le second refuse comme avant, et la distinction est DÉRIVÉE de la DDL de référence,
  # pas d'une liste de noms. Les sept témoins tiennent les DEUX sens, et NEUF vérifications de mutation
  # les ont éprouvés : réparation débranchée -> les trois témoins positifs rougissent et les deux négatifs
  # restent verts ; prédicat relâché sur le cas DANGEREUX (`content=''`, où l'index EST la donnée et où
  # le rebâtir la détruirait) -> seul le témoin du prédicat rougit ; contenu non rebâti -> le témoin qui
  # compte les lignes rougit, celui qui ne regarde que la déclaration reste vert. Le MÊME +7 sur
  # `EXPECTED_COLD_TESTS` — aucun n'est derrière un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les
  # deux profils (**245** avant comme après) qui l'établit, pas une lecture des sources. Les deux chiffres
  # viennent de `.github/scripts/compter-les-tests.sh`, jamais d'une addition.
  # `P4.1-v` — +5 LE 2026-09-03, TROUVÉ EN RÉSOLVANT LA PANNE DE `P8.10-m`. Un lot d'ingest que le dépôt
  # durable n'a pas su écrire est ÉCARTÉ vers une quarantaine — bon geste, explicitement « rejouable,
  # aucune perte silencieuse », et le rejeu l'a confirmé (tous les lots écartés
  # sont revenus, sans un seul doublon, parce que l'écart suit un ROLLBACK atomique). LE DÉFAUT N'EST PAS L'ÉCART, C'EST QUE PERSONNE NE LE
  # REGARDE : rien ne lisait ce répertoire, et le seul compte publié était celui des abandons du DERNIER
  # PASSAGE — retombé à zéro dès la reprise pendant que 760 lots dormaient, dont un depuis VINGT-NEUF JOURS.
  # Le commentaire qui justifiait ce compteur énonçait lui-même son angle mort : un fichier abandonné
  # « reste dans la file », or un fichier écarté en SORT. Les deux moitiés livrées sont indissociables et
  # c'est la règle anti-rançon : la profondeur est publiée sur la surface de santé (elle DÉCLASSE le verdict
  # sans effacer ce qu'il disait déjà) ET un geste la vide (`spool-requeue`, inscrit dans l'inventaire des
  # sous-commandes, sans aucune ouverture de base — il sert justement quand la porte refuse de servir).
  # Un cinquième témoin ferme un défaut de la MÊME famille trouvé au même endroit : la branche de dernier
  # recours EFFACE le lot et la ligne annonçait quand même « quarantaine ». SIX vérifications de mutation :
  # répertoire absent redevenu illisible, verdict non déclassé, détail écrasé au lieu d'ajouté, simulation
  # qui déplace, aveu qui redit « quarantaine » dans les trois cas — chacune rougit son témoin et LUI SEUL.
  # Le MÊME +5 sur `EXPECTED_COLD_TESTS`, écart **245** INCHANGÉ, mesuré par le script du dépôt.
  EXPECTED_TESTS: "1559"

jobs:
  # ---------------------------------------------------------------------------
  # test — daemon test suite (default features = SMB profile; count ASSERTED, see below).
  # ---------------------------------------------------------------------------
  test:
    name: test (daemon — cargo test --locked)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Rust toolchain (pinned)
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"

      - name: Cache cargo (registry + target)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            daemon/target
          key: cargo-${{ runner.os }}-${{ hashFiles('daemon/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      # DEFAULT build only: SqlcipherStore alone (rusqlite bundled-sqlcipher-vendored-openssl
      # compiles C + vendored OpenSSL; cc/perl present on ubuntu-24.04).
      # cargo fetches the guatx-core git-dep from GitHub (tag: see daemon/Cargo.toml).
      # NOTE: do NOT add `--features cold_tier` to this job — the cold_tier suite shares
      # fixed temp paths with the default suite and must run on a SEPARATE runner
      # (CONTRIBUTING "Two testing gotchas" #2 says the same). It now HAS one: the
      # `cold-tier` job below. There is a SECOND, independent reason to keep them apart:
      # this job's whole point is to assert the DEFAULT-feature count (EXPECTED_TESTS), which
      # is how CONTRIBUTING's mode-0 invariant is enforced — folding a feature into it would
      # destroy the very measurement. Feature suites get their own job and their own counter.
      #
      # THE COUNT IS ASSERTED HERE. CONTRIBUTING calls the constant test count a NON-NEGOTIABLE
      # invariant (it is how a mode-0/gated change proves it added nothing to the default
      # build); before this step nothing enforced it — the numbers were comments. We sum the
      # `test result: ok. N passed` lines of the run and compare to EXPECTED_TESTS. Summing (rather
      # than matching one line) is what makes the assertion survive a future split into several test
      # targets; measured on the real output of this exact command today, there is ONE such line and
      # ZERO `Doc-tests` sections — `plume-daemon` is a BINARY crate, and cargo only runs doctests for
      # `lib` targets. (An earlier version of this comment described a "0-test doc run": no such line
      # is ever produced. The assertion was right; the reasoning printed next to it was not.)
      # A gated change that leaks a test into the default profile, or a deleted test, now FAILS the
      # build instead of passing unnoticed. `set -o pipefail` so a cargo failure is not masked by tee.
      - name: cargo test (daemon, default features) + assert the test count
        working-directory: daemon
        run: |
          set -euo pipefail
          export TMPDIR="$RUNNER_TEMP/plume-tmp-defaut"
          rm -rf "$TMPDIR"; mkdir -p "$TMPDIR"
          # `P11.23-b` — LE CANAL DU REFUS DE CONCLURE, ARMÉ ICI. Un test qui ne peut PAS voir
          # (plate-forme sans `/proc`, levier de configuration éteint, clé SQLCipher dans
          # l'environnement) a le droit de ne rien prouver ; il n'a pas le droit de se présenter
          # comme un test qui a prouvé. Il ne doit pas non plus ÉCHOUER : aucun geste ne refermerait
          # un rouge posé sur un conteneur durci — ce serait une rançon. Il écrit donc une ligne
          # dans CE fichier, que le pas relit et SERT après la suite. Sans cette variable le canal
          # est INERTE : rien n'est ouvert, rien n'est créé, la suite est identique (mesuré).
          # HORS de `$TMPDIR` À DESSEIN : `aucun-temporaire-laisse.sh` y compterait ce journal comme
          # un résidu de suite. Le nom de la variable n'est PAS recopié dans le lecteur : il le LIT
          # dans `daemon/src/tests/canal_de_refus.rs` (`VARIABLE_DU_CANAL`), donc écrivain et
          # lecteur ne peuvent pas diverger en silence.
          export PLUME_JOURNAL_DES_REFUS="$RUNNER_TEMP/refus-de-conclure-defaut.journal"
          rm -f "$PLUME_JOURNAL_DES_REFUS"
          rc_cargo=0
          cargo test --locked --verbose 2>&1 | tee /tmp/daemon-tests.log || rc_cargo=$?
          # `P7.19-b` — LE CANAL DISTINCT, ARMÉ ICI. Le code de cargo est CAPTURÉ (et non laissé
          # tuer le pas), puis le journal est TRIÉ : si un banc a REFUSÉ DE CONCLURE, sa marque y
          # figure et le tri rend **2** — « je n'ai pas pu mesurer » — là où une propriété violée
          # laisse passer le code de cargo. Le job reste ROUGE dans les deux cas (un banc qui sait
          # ne pas avoir mesuré n'a pas le droit d'être vert) : ce que le 2 achète est un tri
          # MÉCANIQUE — code de sortie du pas + annotation distincte — au lieu d'une lecture
          # d'humain sur un `panic!` indiscernable d'une assertion. La marque n'est écrite NULLE
          # PART ici : le tri la LIT dans `daemon/src/tests/query_verify.rs` (`QB_MARQUE_REFUS`).
          rc_tri=0
          python3 "$GITHUB_WORKSPACE/.github/scripts/check_a_bench_refusal_is_a_distinct_channel.py" \
            --trier /tmp/daemon-tests.log "$rc_cargo" || rc_tri=$?
          [ "$rc_tri" = 0 ] || exit "$rc_tri"
          # LE CANAL EST RELU ET SERVI — mais son code est GARDÉ POUR LA FIN. Un lecteur qui ne
          # peut pas conclure (variable absente, écrivain cassé) rend 2 ; le laisser tuer le pas ici
          # ferait passer le verdict d'« une propriété est violée » à « je n'ai pas pu mesurer »,
          # c'est-à-dire le défaut d'à côté. Le refus de conclure du LECTEUR ne l'emporte donc
          # jamais sur un rouge de cargo, du tri, ou du compte.
          rc_canal=0
          python3 "$GITHUB_WORKSPACE/.github/scripts/check_a_test_that_declines_to_conclude_says_so.py" \
            --lire || rc_canal=$?
          total=$(awk '/^test result: ok\. [0-9]+ passed/{s+=$4} END{print s+0}' /tmp/daemon-tests.log)
          echo "tests passed: $total (expected $EXPECTED_TESTS)"
          if [ "$total" != "$EXPECTED_TESTS" ]; then
            echo "::error::default-suite test count is $total, expected $EXPECTED_TESTS."
            echo "::error::If the change legitimately adds or removes tests, update EXPECTED_TESTS here"
            echo "::error::and the numbers in CONTRIBUTING.md, daemon/.cargo/audit.toml and daemon/src/tests/ingest.rs."
            exit 1
          fi
          "$GITHUB_WORKSPACE/.github/aucun-temporaire-laisse.sh" "suite par défaut"
          [ "$rc_canal" = 0 ] || exit "$rc_canal"

      # Release build of the default profile — proves the daemon links/optimizes clean.
      - name: cargo build --release (daemon, default features)
        working-directory: daemon
        run: cargo build --release --locked --verbose

      # (The `duckdb` feature-check that used to live here moved to the `features` job below,
      # where every OFF-by-default feature is checked in one place. Same command, own runner.)

  # ---------------------------------------------------------------------------
  # cold-tier — THE COLD READ PATH, ON ITS OWN RUNNER.
  #
  # WHY (the risk this job exists to close): with a hot window of 7 days and a retention of
  # 365, `daemon/src/cold_store/` (Parquet cold tier) is what actually answers any query
  # reaching further back than a week — i.e. the overwhelming majority of the retained
  # history. It is gated behind the OFF-by-default `cold_tier` feature, so the default `test`
  # job does not merely skip its tests: IT DOES NOT COMPILE THE MODULE AT ALL. Before this
  # job, a change could delete, break or silently mis-prune the cold reader and the whole
  # pipeline would stay green.
  #
  # This is not a hypothetical. Two facts measured while adding this job:
  #   (a) a deliberate type error appended to `cold_store/writer.rs` left `cargo test --locked`
  #       GREEN (exit 0); only `--features cold_tier` caught it (exit 101, compile error);
  #   (b) the suite was ALREADY RED on `main` when first run here — bumping guatx-core to the
  #       v0.2.1 security release (commit 1a67014) tightened field-name validation in the SOQL
  #       compiler, which broke two `cold_store` extractor-parity tests. That commit's message
  #       records "749 passed / 0 failed" — the DEFAULT suite, the only one CI could run. The
  #       cold tier had been unverified against the compiler it depends on.
  #
  # The dangerous failure mode for this module is NOT a crash. It is a query over >7 days that
  # returns a WRONG COUNT WITHOUT SAYING SO (over-pruning a Parquet file drops rows silently).
  # That is why this job runs the full `cargo test`, not a `cargo check` like the other
  # features: compilation alone would not catch a pruning-logic regression.
  #
  # SEPARATE RUNNER, NOT A STEP IN `test` — two independent reasons, both load-bearing:
  #   1. CONTRIBUTING ("Two testing gotchas" #2): the default and cold_tier suites must never
  #      run concurrently against shared temp fixtures — run them sequentially, on separate
  #      runners. Separate GitHub jobs are separate machines, which satisfies this by
  #      construction.
  #   2. The `test` job's purpose is to assert the DEFAULT-feature count; mixing a feature in
  #      would destroy that measurement (see the note in that job).
  # ---------------------------------------------------------------------------
  cold-tier:
    name: cold-tier (daemon — cargo test --features cold_tier)
    runs-on: ubuntu-24.04
    # WHERE THIS JOB'S TIME GOES — measured 2026-08-20 (12 cores, `-j 4`, TMPDIR on real disk),
    # because the answer was NOT the one everybody guesses. The usual suspect is the cache: enabling
    # a feature is assumed to invalidate it and rebuild the world. REFUTED, by measurement — a build
    # of the ENTIRE graph from an EMPTY target dir (247 crates, `cargo build --tests --features
    # cold_tier`) costs 279 s and produces a 2.2 GB target dir; a warm no-op costs 20 s and a rebuild
    # after touching a source file 27 s. Even a TOTAL cache miss is a small minority of this job.
    # The test RUN is the job: 2392,76 s of the same measurement, ~90 % of the wall clock. It is
    # dominated by a tail — 52 tests of this suite each exceed 60 s, the heaviest (parallel Parquet
    # decode parity) taking 398 s ALONE. Those tests spend their time inside `parquet`/`zstd`/
    # SQLCipher, which `cargo test` used to build at `opt-level = 0`; `[profile.dev.package."*"]`
    # in daemon/Cargo.toml now builds dependencies (and ONLY dependencies) optimized. Same command,
    # EXPECTED_COLD_TESTS passing in both arms, rc=0: 2392,76 s -> 589,91 s. That block carries the A/B and the
    # per-test numbers — read it before reverting the profile to "simplify" the manifest.
    #
    # The ceiling stays deliberately generous so a slow runner reports a REAL failure rather than an
    # ambiguous timeout. It is NOT tightened to track the new duration: a timeout tuned close to the
    # measured time turns runner variance into red builds that say nothing about the code.
    timeout-minutes: 90
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Rust toolchain (pinned)
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"

      # Own cache key ("cold-"): the cold_tier feature set produces DIFFERENT artifacts from the
      # default job (it links `parquet`), so sharing one key would make the two jobs evict each
      # other's target dir on every run.
      - name: Cache cargo (registry + target)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            daemon/target
          key: cargo-cold-${{ runner.os }}-${{ hashFiles('daemon/Cargo.lock') }}
          restore-keys: |
            cargo-cold-${{ runner.os }}-

      # THE COUNT IS ASSERTED, exactly like the default job — same awk, same failure mode, its
      # OWN counter. EXPECTED_COLD_TESTS is the count of the FULL suite under `--features
      # cold_tier` (default tests + the cold_store tests the feature adds), MEASURED on the real
      # output of this exact command, not derived by arithmetic. It must stay strictly greater
      # than EXPECTED_TESTS: if the two ever coincide, the feature stopped adding its tests and
      # this job silently degraded into a duplicate of the default one — the check below catches
      # exactly that, so the job cannot rot into a no-op.
      #
      # TMPDIR: CONTRIBUTING gotcha #1 — these tests spill temp SQLite DBs and Parquet files, so
      # they must land on a REAL DISK, never a RAM-backed tmpfs. $RUNNER_TEMP is disk-backed on
      # GitHub-hosted runners.
      - name: cargo test (daemon, --features cold_tier) + assert the cold test count
        working-directory: daemon
        env:
          # 1149 : MESURÉ le 2026-08-09 (`cargo test --offline --locked --features cold_tier`, 2627 s,
          # rc=0, 12 cœurs, machine autrement au repos), rejoué par moi et non recopié d'un rapport.
          # 1144 datait du 08-08 (2362 s) ; +5 = les mêmes cinq tests que la suite par défaut (lot
          # P8.6-b / P4.1-p) — la feature n'en ajoute aucun, l'écart cold−défaut reste 195.
          # ⚠ UNE PREMIÈRE MESURE A ÉTÉ JETÉE, et c'est pour ça qu'elle est mentionnée : un binaire de
          # test ORPHELIN d'une exécution précédemment interrompue tournait encore (28 min, 215 % CPU,
          # son propre TMPDIR) et se disputait la machine. La durée qu'il a produite ne mesurait pas la
          # suite. Relancée machine vide.
          #
          # 1156 : MESURÉ le 2026-08-09 (`cargo test --offline --locked --features cold_tier`, rc=0,
          # 2317 s, 12 cœurs, TMPDIR sur DISQUE — pas /tmp, qui est un tmpfs sur cette machine).
          # 1149 datait du même jour ; +7 = les mêmes sept tests que la suite par défaut (lot P8.7-a,
          # `tests/partition_config.rs`) — la feature n'en ajoute aucun, l'écart cold−défaut reste 195.
          # DEUX exécutions, et je consigne la SECONDE : la première (2411 s) avait été compilée avant
          # une dernière retouche du scanner, donc elle ne mesurait pas l'arbre livré ; celle-ci, si.
          # NB sur la DURÉE : 2317 s contre 2627 s le 08-09 au matin, mais la première exécution a
          # partagé ses cœurs avec un autre `cargo`. Cette machine ne permet pas d'attribuer l'écart
          # au contenu, et le lot ne prétend rien accélérer.
          #
          # 1164 : MESURÉ le 2026-08-09 (`cargo test --offline --locked --features cold_tier`, rc=0,
          # **2471 s** de bout en bout dont 2455 s de suite, 12 cœurs, TMPDIR sur DISQUE). 1156 datait
          # du même jour ; +8 viennent du lot P8.7-b — les 7 de la suite par défaut
          # (`tests/cle_at_rest_voie_unique.rs`) PLUS UN que la feature ajoute vraiment :
          # `cold_store::tests::p87b_les_deux_moities_at_rest_derivent_du_meme_appel`, qui compare le
          # secret de la moitié FROIDE à celui de l'ouverture — il n'a de sens QUE sous `cold_tier`.
          # L'écart cold−défaut passe donc de 195 à **196**, et c'est la première fois qu'il bouge.
          # DURÉE, dite honnêtement : 2471 s contre 2317 s le matin même, sur une machine qui portait
          # d'autres travaux pendant la mesure (un navigateur à 117 % CPU en continu). Cette machine ne
          # permet pas d'attribuer l'écart au contenu, et le lot ne prétend rien ralentir ni accélérer.
          #
          # 1175 : MESURÉ le 2026-08-09 (`cargo test --offline --locked --features cold_tier`, rc=0,
          # **2019 s** de bout en bout dont 2000,76 s de suite, 12 cœurs, TMPDIR sur disque). 1164
          # datait du même jour ; +11 viennent du lot P10.7-a — les MÊMES 11 que la suite par défaut
          # (`tests/barre_fts_vide_silencieux.rs`, la barre `/api/search` qui rendait « aucun résultat »
          # quand le moteur FTS5 disait « je refuse »). La feature n'en ajoute AUCUN : la garde vit dans
          # le chemin CHAUD (`event_fts`/`event_fields_fts`), que `cold_tier` ne change pas — l'écart
          # cold−défaut reste donc **196**, inchangé depuis P8.7-b.
          # DURÉE, dite honnêtement : 2019 s contre 2471 s le 08-09 pour le lot précédent. Cette machine
          # porte d'autres travaux selon les moments et ne permet pas d'attribuer l'écart au contenu ;
          # le lot ne prétend rien accélérer (il AJOUTE ~0,3 à 0,8 ms par recherche plein-texte, mesuré
          # en `release` — cf. le bloc P10.7-a de `daemon/src/soql_glue.rs`).
          #
          # 1185 : MESURÉ le 2026-08-09 (`cargo test --offline --locked --features cold_tier`, rc=0,
          # **2139,05 s** de suite / 35 min 59 s de bout en bout, 12 cœurs, TMPDIR sur disque ; une
          # première passe du même lot avait rendu 2130,16 s). 1175 datait du même jour ; **+10
          # viennent du lot P10.7-b** — les MÊMES 10 que la suite par défaut
          # (`tests/compactage_fts.rs`, la compaction de l'index plein-texte). La feature n'en ajoute
          # AUCUN, et ce n'est pas un hasard : le défaut vit dans le chemin CHAUD (une purge de
          # rétention fait grossir `event_fts`), que `cold_tier` ne change pas — l'écart cold−défaut
          # reste donc **196**, inchangé depuis P8.7-b.
          # ⚠ CE QUE LE TIER FROID CHANGE QUAND MÊME, ET QUI N'EST PAS MESURÉ ICI : sous
          # `PLUME_COLD_TIER=1`, l'aging SUPPRIME du chaud les lignes archivées — donc il FABRIQUE
          # exactement le poids mort FTS5 que ce lot compacte, et à un rythme bien plus soutenu que la
          # purge de rétention. La compaction s'exécute au même endroit dans les deux cas (fin de
          # `retention_run`, après l'aging), mais son GAIN sous tier froid n'a pas été chiffré.
          #
          # 1203 : MESURÉ le 2026-08-09 par ÉNUMÉRATION (`cargo test --offline --locked --features
          # cold_tier -- --list`, 60 s, 12 cœurs) — pas par exécution. 1185 datait du même jour ; **+18
          # viennent du lot « série du budget »**, les MÊMES 18 que la suite par défaut
          # (`tests/ventilation_serie.rs` + `db_ventilation.rs`). La feature n'en ajoute AUCUN : la
          # ventilation lit `dbstat` sur la base CHAUDE, que `cold_tier` ne change pas — l'écart
          # cold−défaut reste donc **196**, inchangé depuis P8.7-b.
          # ⚠ CE QUE CE CHIFFRE NE PROUVE PAS, ET IL FAUT LE DIRE : il compte des tests DÉCLARÉS, pas
          # des tests VERTS. La suite `cold_tier` n'a PAS été exécutée pour ce lot (≈36 min sur une
          # machine partagée) ; seule la suite par défaut l'a été (rc=0, 1007/1007). La CI, elle,
          # les exécute vraiment — c'est elle qui tranchera.
          #
          # 1208 : MESURÉ le 2026-08-10 par ÉNUMÉRATION (`cargo test --offline --locked --features
          # cold_tier -- --list`, **47,2 s**, 12 cœurs, les DEUX lectures du script de comptage
          # concordant à 1208) — pas par exécution. 1203 datait du 08-09 ; **+5 viennent du lot
          # « usage des index de `event` »**, les MÊMES 5 que la suite par défaut
          # (`tests/index_usage_event.rs`). La feature n'en ajoute AUCUN, et c'est attendu : le banc
          # interroge le catalogue et le planificateur de la base CHAUDE, que `cold_tier` ne change
          # pas — l'écart cold−défaut reste donc **196**, inchangé depuis P8.7-b.
          # ⚠ CE QUE LA MESURE D'USAGE NE COUVRE PAS SOUS TIER FROID : avec `PLUME_COLD_TIER=1`, une
          # part des lectures part vers le lecteur hot∪cold, qui prune par bloom et zone-maps et ne
          # porte AUCUN b-tree. L'usage des index chauds y serait donc MÉCANIQUEMENT plus faible que
          # ce que ce lot mesure. Non chiffré : le froid est OFF en production.
          # ⚠ CE QUE CE CHIFFRE NE PROUVE PAS, ET IL FAUT LE REDIRE : il compte des tests DÉCLARÉS,
          # pas des tests VERTS. La suite `cold_tier` n'a PAS été exécutée pour ce lot ; seule la
          # suite par défaut l'a été (rc=0, 1012/1012, 217,84 s). La CI, elle, les exécute vraiment.
          #
          # 1268 : +1 le 2026-08-19 par ÉNUMÉRATION (lot P8.22-a) — le MÊME que la suite par défaut
          # (`adverse_destination_objet_non_honorable_ne_devient_jamais_une_ecriture_locale`). La
          # feature `cold_tier` n'en ajoute aucun, et c'est attendu : le sink objet dépose une archive
          # déjà produite par le chemin CHAUD, que le tier froid ne change pas — l'écart cold−défaut
          # est donc inchangé.
          #
          # 1280 : MESURÉ le 2026-08-20 par ÉNUMÉRATION (`cargo test --features cold_tier -- --list`,
          # deux lectures concordantes à 1280) — pas par exécution. Les six tests du lot « exercice de
          # restauration » (P8.3-a) sont les MÊMES que ceux de la suite par défaut : la feature n'en
          # ajoute aucun, et c'est attendu — la sauvegarde chaude, sa vérification et le suivi de
          # l'exercice ne dépendent pas du tier froid. L'écart cold−défaut reste donc **220**,
          # inchangé. Même constat pour les six tests de la borne interactive (P7.8-a) : ils portent
          # sur le sémaphore des routes CHAUDES, que le tier froid ne change pas — ils sont donc les
          # mêmes des deux côtés et ne déplacent pas l'écart.
          # +11 pour le lot « imputation d'une alerte à ses sources » (S7) : ces tests n'ont AUCUN `cfg`
          # de feature — ils portent sur le chemin d'alerte CHAUD, que le tier froid ne change pas — donc
          # ils sont les mêmes des deux côtés et l'écart cold−défaut reste **220**. RE-MESURÉ le
          # 2026-08-20 par la MÊME énumération, sur l'arbre courant : 1306 (l'arbre a reçu d'autres lots
          # pendant la mesure ; l'écart cold−défaut vaut toujours 220 — le lot S7 n'a AUCUN `cfg` de
          # feature, il porte sur le chemin d'alerte CHAUD, donc il compte pareil des deux côtés).
          # 1306 CONTRE-MESURÉ le 2026-08-20 par la MÊME énumération (`cargo test --features cold_tier
          # -- --list`, deux lectures concordantes : le compte des lignes `: test` et le total que cargo
          # imprime lui-même), sur l'arbre qui porte aussi les six tests du lot « empreinte du journal
          # d'écriture » (clé P10.16-a). Ces six-là n'ont AUCUN `cfg` de feature : ils portent sur la
          # politique de journal de la base CHAUDE, que le tier froid ne change pas — ils sont donc les
          # mêmes des deux côtés, et l'écart cold−défaut reste **220** (1306 − 1086).
          # RÉ-ÉNUMÉRÉ le 2026-08-20 par la MÊME double lecture, sur l'arbre qui porte aussi le lot S4
          # (clé de tenant sans repli horodaté, trois tests sans `cfg`) : 1329 avec `cold_tier` pour 1109
          # par défaut — l'écart reste **220**.
          # RÉ-ÉNUMÉRÉ le 2026-08-20 par la MÊME double lecture, sur l'arbre qui porte aussi le lot S28
          # (lecture de la limite mémoire du groupe de contrôle, cinq tests sans `cfg`) : 1345 avec
          # `cold_tier` pour 1125 par défaut — l'écart reste **220**.
          # RÉ-ÉNUMÉRÉ le 2026-08-20 par la MÊME double lecture, sur l'arbre qui porte aussi le lot S26
          # (tri en mémoire LU et non supposé, sept tests sans `cfg`, moins une garde de proximité
          # devenue redondante) : 1345 avec `cold_tier` pour 1125 par défaut — l'écart reste **220**.
          # RÉ-ÉNUMÉRÉ le 2026-08-20 par la MÊME double lecture, sur l'arbre qui porte aussi le lot S32
          # (lectures d'environnement séparant « lu » de « pas lisible », dix-huit tests sans `cfg`) :
          # 1368 avec `cold_tier` pour 1148 par défaut — l'écart reste **220**.
          # RÉ-ÉNUMÉRÉ le 2026-08-22 (`cargo test --bin plume-daemon --features cold_tier -- --list`,
          # compte des lignes `: test`), sur l'arbre qui porte aussi le lot P4.4-l (la sauvegarde nommée
          # d'avant un franchissement de schéma, six tests sans `cfg`) : 1452 avec `cold_tier` pour 1232
          # par défaut — l'écart reste **220**.
          # RÉ-MESURÉ le 2026-08-23 par la MÊME commande, sur l'arbre qui porte P11.3-c et P11.3-d (cinq
          # tests sans `cfg`) : 1469 avec `cold_tier` pour 1249 par défaut — l'écart reste **220**.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte le lot d'intégrité
          # P11.5-d (six tests sans `cfg`) : 1484 avec `cold_tier` pour 1264 par défaut — l'écart reste
          # **220**. Ce compteur-ci bouge AVEC l'autre : six tests sans `cfg` entrent dans les deux
          # profils, et le laisser en arrière rendrait ce job rouge sans rien mesurer de neuf.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte la garde P6.8-d
          # (quatre tests sans `cfg` : les index d'expression des champs chauds constatés au
          # catalogue d'une base migrée) : 1488 avec `cold_tier` pour 1268 par défaut — l'écart
          # reste **220**. Ce compteur-ci bouge AVEC l'autre : quatre tests sans `cfg` entrent dans
          # les deux profils, et le laisser en arrière rendrait ce job rouge sans rien mesurer de neuf.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte le correctif P6.8-f
          # (trois tests sans `cfg` : le compte rendu de la passe d'index constaté au catalogue au
          # lieu d'être récité depuis la déclaration) : 1491 avec `cold_tier` pour 1271 par défaut —
          # l'écart reste **220**. Ce compteur-ci bouge AVEC l'autre : trois tests sans `cfg` entrent
          # dans les deux profils, et le laisser en arrière rendrait ce job rouge sans rien mesurer
          # de neuf.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte le correctif P6.8-e
          # (cinq tests sans `cfg` : le retrait de fond des index de champ chaud orphelins, dérivé du
          # préfixe de la famille) : 1496 avec `cold_tier` pour 1276 par défaut — l'écart reste
          # **220**. Ce compteur-ci bouge AVEC l'autre : cinq tests sans `cfg` entrent dans les deux
          # profils, et le laisser en arrière rendrait ce job rouge sans rien mesurer de neuf.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte le correctif P6.8-g
          # (cinq tests sans `cfg` : le constat de présence d'un index comparé à la définition et non
          # à l'étiquette) : 1501 avec `cold_tier` pour 1281 par défaut — l'écart reste **220**. Ce
          # compteur-ci bouge AVEC l'autre : cinq tests sans `cfg` entrent dans les deux profils, et
          # le laisser en arrière rendrait ce job rouge sans rien mesurer de neuf.
          # RÉ-MESURÉ le 2026-08-24 par la MÊME commande, sur l'arbre qui porte le correctif P10.1-b
          # (cinq tests sans `cfg` : le quota qui borne la taille du déversement des tris) : 1506 avec
          # `cold_tier` pour 1286 par défaut — l'écart reste **220**. Ce compteur-ci bouge AVEC
          # l'autre : cinq tests sans `cfg` entrent dans les deux profils, et le laisser en arrière
          # rendrait ce job rouge sans rien mesurer de neuf.
          # LA MARCHE QUI MANQUAIT, ET LE TEST QUI L'A FAITE — MESURÉ LE 2026-08-26. Entre la ligne
          # ci-dessus (1506 / 1286, écart 220) et la valeur que ce fichier portait en entrant dans le lot
          # d'aujourd'hui (1552 / 1331, écart **221**), ce journal ne dit rien : `git log -S` sur les deux
          # paires rend le MÊME commit, `ed99dbf`, qui les a posées d'un coup sans ajouter de ligne ici.
          # LE TEST FROID QUI A FAIT CETTE MARCHE, DÉRIVÉ DES SOURCES aux deux commits (les `#[test]` du
          # module `cold_store`, gaté en entier par la feature, plus ceux qu'un `cfg` cold_tier gâte
          # ailleurs) : `p87c_le_secret_froid_suit_la_provenance_de_la_cle_douverture`, et lui seul — la
          # différence des deux ensembles ne porte AUCUN autre nom.
          # L'INSTRUMENT EST VALIDÉ AVANT D'ÊTRE CRU, sans quoi « il n'a rien trouvé d'autre » et « il est
          # aveugle » se liraient pareil : la même dérivation rend 223 / 224 / 225 noms aux trois points
          # (`ed99dbf~1`, `ed99dbf`, cet arbre) là où les écarts MESURÉS valent 220 / 221 / 222, et ses
          # DEUX faux positifs sont NOMMÉS plutôt que constatés comme un décalage —
          # `sans_la_feature_la_recolte_dit_non_compile` (gaté `cfg(not(feature = "cold_tier"))`) et
          # `purge_refuses_when_the_cold_tier_covers_the_window` (présent dans les DEUX profils). Un
          # décalage constant et expliqué laisse la DIFFÉRENCE exacte.
          #
          # ET « L'ÉCART » N'EST PAS UN COMPTE D'AJOUTS, C'EST UN SOLDE — mesuré le 2026-08-26 en
          # comparant les DEUX listes (`cargo test -- --list`, avec et sans la feature) : le profil froid
          # GAGNE 223 tests et en PERD **1**. Celui qu'il perd est
          # `sans_la_feature_la_recolte_dit_non_compile`, qui n'existe que SANS la feature parce qu'il
          # éprouve exactement ce que la bannière dit quand le tier n'est pas compilé. 223 − 1 = 222.
          # Écrire « l'écart reste 222 » sans cette soustraction laissait croire qu'aucun test ne
          # disparaît d'un profil à l'autre : il y en a un, et c'est par construction.
          # RÉ-MESURÉ le 2026-08-26 par `.github/scripts/compter-les-tests.sh` (1553 avec `cold_tier`
          # pour 1331 par défaut — écart **222**) sur l'arbre qui prend `hkdf` 0.13.0. Le +1 est UN
          # test et un seul, `cold_hkdf_derivation_gelee_rfc5869` : il GÈLE l'octet dérivé par
          # HKDF-SHA256 pour la clé cold, parce que cette montée de version fait cohabiter DEUX
          # générations de `sha2` dans la crate et qu'une clé cold qui change rend illisible tout
          # jour-file froid déjà écrit. Il est derrière `cfg(feature = "cold_tier")` -> `EXPECTED_TESTS`
          # ne bouge PAS, et la mesure du 2026-08-26 le confirme (1331 inchangé).
          # RÉ-MESURÉ le 2026-08-26 (MÊME commande) sur l'arbre qui prend `axum` 0.8.9 : 1554 avec
          # `cold_tier` pour 1332 par défaut — l'écart reste **222**. Le +1 est le MÊME test que
          # celui d'`EXPECTED_TESTS` (`router_json_refuse_les_octets_parasites_apres_le_document`,
          # sans `cfg` de feature) : les deux compteurs bougent ensemble, et l'écart INCHANGÉ est ce
          # qui prouve qu'aucun test n'a été gagné ni perdu du côté de la feature.
          # RÉ-MESURÉ le 2026-08-26 (MÊME commande) sur l'arbre qui NOMME les deux autres changements
          # servis par la montée `axum` : 1556 avec `cold_tier` pour 1334 par défaut — l'écart reste
          # **222**. Les +2 sont les MÊMES deux tests que ceux d'`EXPECTED_TESTS` (refus de `Path<i64>`
          # et précédence du segment statique, tous deux sans `cfg`) : les deux compteurs bougent
          # ensemble, et l'écart INCHANGÉ est ce qui dit qu'aucun test froid n'a été perdu en chemin.
          # RÉ-MESURÉ le 2026-08-27 (MÊME commande, profil `cold_tier` mesuré SÉPARÉMENT — rien ne
          # garantit a priori le même delta, c'est justement ce qu'on vérifie) : 1571 avec `cold_tier`
          # pour 1349 par défaut — l'écart reste **222**. Les +15 sont les MÊMES quinze que ceux
          # d'`EXPECTED_TESTS` (magasin de secrets arrêté, fausse couverture de détection), aucun
          # derrière un `cfg` de feature : les deux compteurs bougent ensemble, et c'est l'écart
          # INCHANGÉ qui dit qu'aucun test froid n'a été perdu en chemin.
          # RÉ-MESURÉ le 2026-08-27 (MÊME commande, profil `cold_tier` mesuré SÉPARÉMENT) après la reprise
          # adverse du lot P9 : 1573 avec `cold_tier` pour 1351 par défaut — l'écart reste **222**. Les +2
          # sont les MÊMES deux tests que ceux d'`EXPECTED_TESTS` (les deux membres du delta d'un import
          # Sigma comptés pareil ; l'aveu dérivé du magasin de secrets sur son propre producteur), aucun
          # derrière un `cfg` de feature : les deux compteurs bougent ensemble, et c'est l'écart INCHANGÉ
          # qui dit qu'aucun test froid n'a été perdu en chemin.
          # RÉ-MESURÉ le 2026-08-27 (MÊME commande, les deux profils dans la même exécution) sur le lot
          # P3→P8 et sa reprise adverse : 1598 avec `cold_tier` pour 1376 par défaut — l'écart reste
          # **222**. Les +25 sont les MÊMES vingt-cinq que ceux d'`EXPECTED_TESTS` (leur répartition
          # par fichier est écrite là-bas, une seule fois), aucun derrière un `cfg` de feature.
          # RÉ-MESURÉ le 2026-08-28 (MÊME commande, les deux profils dans la même exécution) sur le lot
          # P10 et sa reprise adverse : **1618** avec `cold_tier` pour **1382** par défaut — l'écart
          # passe de 222 à **236**, ET C'EST ATTENDU : les quatorze tests neufs de
          # `daemon/src/cold_store/tests.rs` ne comptent QUE dans ce profil (`main.rs` porte
          # `#[cfg(feature = "cold_tier")] mod cold_store;`). La phrase « l'écart reste 222 » des
          # strophes précédentes décrivait CES lots-là, pas une loi : ce qui dit qu'aucun test n'a été
          # perdu, c'est que les DEUX dérivations ferment contre la mesure (1598+6+14 = 1618 ici,
          # 1376+6 = 1382 là-bas). La répartition par fichier est écrite auprès d'`EXPECTED_TESTS`,
          # une seule fois.
          # RÉ-MESURÉ le 2026-08-28 (MÊME commande, les deux profils dans la même exécution) sur la
          # reprise adverse du lot P10 : **1624** avec `cold_tier` pour **1384** par défaut — l'écart
          # passe de 236 à **240**. Les quatre tests du delta sont froids pour DEUX raisons distinctes
          # (deux sous `cold_store/`, deux sous `#[cfg(feature)]` dans `tests/keyset.rs`) ; la
          # répartition est écrite auprès d'`EXPECTED_TESTS`, une seule fois.
          # `P10.5-i` (2026-08-28) : +8, les MÊMES huit que sur `EXPECTED_TESTS` — aucun n'est derrière un
          # `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (243 avant comme après) qui
          # l'établit, pas une lecture des sources. La dérivation complète est écrite auprès
          # d'`EXPECTED_TESTS`, une seule fois.
          # `P4.7-b` (2026-08-28) : +8, les MÊMES huit que sur `EXPECTED_TESTS` — aucun n'est derrière
          # un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (243 avant comme
          # après) qui l'établit, pas une lecture des sources. La dérivation complète et la
          # répartition sont écrites auprès d'`EXPECTED_TESTS`, une seule fois.
          # `P4.13-a` (REPRISE, 2026-08-29) : +6, les MÊMES six que sur `EXPECTED_TESTS` — aucun n'est
          # derrière un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (243 avant
          # comme après) qui l'établit, pas une lecture des sources. La dérivation complète et la
          # répartition sont écrites auprès d'`EXPECTED_TESTS`, une seule fois.
          # `P11.24-e` (2026-09-02) : +1, le MÊME que sur `EXPECTED_TESTS` — il n'est derrière aucun
          # `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (245 avant comme après)
          # qui l'établit, pas une lecture des sources. La dérivation est écrite auprès
          # d'`EXPECTED_TESTS`, une seule fois.
          # `P11.23-g` (2026-09-02) : +1, le MÊME que sur `EXPECTED_TESTS` — il n'est derrière
          # aucun `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (245 avant comme
          # après) qui l'établit, pas une lecture des sources. La dérivation est écrite auprès
          # d'`EXPECTED_TESTS`, une seule fois.
          # `P7.19-f` et `P7.19-g` (2026-09-03) : +2, les MÊMES deux que sur `EXPECTED_TESTS` —
          # aucun n'est derrière un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils
          # (245 avant comme après) qui l'établit, pas une lecture des sources. La dérivation est
          # écrite auprès d'`EXPECTED_TESTS`, une seule fois.
          # `P7.19-i` (2026-09-03) : +3, les MÊMES trois que sur `EXPECTED_TESTS` — aucun n'est
          # derrière un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (245 avant
          # comme après) qui l'établit, pas une lecture des sources. La dérivation est écrite auprès
          # d'`EXPECTED_TESTS`, une seule fois.
          # `P11.20-n` et `P11.20-h` (2026-09-03) : +14, les MÊMES quatorze que sur `EXPECTED_TESTS` —
          # aucun derrière un `cfg(feature)`, et c'est l'ÉCART INCHANGÉ entre les deux profils (245 avant
          # comme après) qui l'établit. La dérivation est écrite auprès d'`EXPECTED_TESTS`, une seule fois.
          EXPECTED_COLD_TESTS: "1804"
          EXPECTED_DEFAULT_TESTS: ${{ env.EXPECTED_TESTS }}
        run: |
          set -euo pipefail
          export TMPDIR="$RUNNER_TEMP/plume-tmp-cold"
          rm -rf "$TMPDIR"; mkdir -p "$TMPDIR"
          # `P11.23-b` — LE CANAL DU REFUS DE CONCLURE, ARMÉ ICI. Un test qui ne peut PAS voir
          # (plate-forme sans `/proc`, levier de configuration éteint, clé SQLCipher dans
          # l'environnement) a le droit de ne rien prouver ; il n'a pas le droit de se présenter
          # comme un test qui a prouvé. Il ne doit pas non plus ÉCHOUER : aucun geste ne refermerait
          # un rouge posé sur un conteneur durci — ce serait une rançon. Il écrit donc une ligne
          # dans CE fichier, que le pas relit et SERT après la suite. Sans cette variable le canal
          # est INERTE : rien n'est ouvert, rien n'est créé, la suite est identique (mesuré).
          # HORS de `$TMPDIR` À DESSEIN : `aucun-temporaire-laisse.sh` y compterait ce journal comme
          # un résidu de suite. Le nom de la variable n'est PAS recopié dans le lecteur : il le LIT
          # dans `daemon/src/tests/canal_de_refus.rs` (`VARIABLE_DU_CANAL`), donc écrivain et
          # lecteur ne peuvent pas diverger en silence.
          export PLUME_JOURNAL_DES_REFUS="$RUNNER_TEMP/refus-de-conclure-cold.journal"
          rm -f "$PLUME_JOURNAL_DES_REFUS"
          rc_cargo=0
          cargo test --locked --features cold_tier --verbose 2>&1 | tee /tmp/cold-tests.log || rc_cargo=$?
          # `P7.19-b` — LE CANAL DISTINCT, ARMÉ ICI. Le code de cargo est CAPTURÉ (et non laissé
          # tuer le pas), puis le journal est TRIÉ : si un banc a REFUSÉ DE CONCLURE, sa marque y
          # figure et le tri rend **2** — « je n'ai pas pu mesurer » — là où une propriété violée
          # laisse passer le code de cargo. Le job reste ROUGE dans les deux cas (un banc qui sait
          # ne pas avoir mesuré n'a pas le droit d'être vert) : ce que le 2 achète est un tri
          # MÉCANIQUE — code de sortie du pas + annotation distincte — au lieu d'une lecture
          # d'humain sur un `panic!` indiscernable d'une assertion. La marque n'est écrite NULLE
          # PART ici : le tri la LIT dans `daemon/src/tests/query_verify.rs` (`QB_MARQUE_REFUS`).
          rc_tri=0
          python3 "$GITHUB_WORKSPACE/.github/scripts/check_a_bench_refusal_is_a_distinct_channel.py" \
            --trier /tmp/cold-tests.log "$rc_cargo" || rc_tri=$?
          [ "$rc_tri" = 0 ] || exit "$rc_tri"
          # LE CANAL EST RELU ET SERVI — mais son code est GARDÉ POUR LA FIN. Un lecteur qui ne
          # peut pas conclure (variable absente, écrivain cassé) rend 2 ; le laisser tuer le pas ici
          # ferait passer le verdict d'« une propriété est violée » à « je n'ai pas pu mesurer »,
          # c'est-à-dire le défaut d'à côté. Le refus de conclure du LECTEUR ne l'emporte donc
          # jamais sur un rouge de cargo, du tri, ou du compte.
          rc_canal=0
          python3 "$GITHUB_WORKSPACE/.github/scripts/check_a_test_that_declines_to_conclude_says_so.py" \
            --lire || rc_canal=$?
          total=$(awk '/^test result: ok\. [0-9]+ passed/{s+=$4} END{print s+0}' /tmp/cold-tests.log)
          echo "cold_tier tests passed: $total (expected $EXPECTED_COLD_TESTS)"
          if [ "$total" != "$EXPECTED_COLD_TESTS" ]; then
            echo "::error::cold_tier suite test count is $total, expected $EXPECTED_COLD_TESTS."
            echo "::error::If the change legitimately adds or removes cold_tier tests, update"
            echo "::error::EXPECTED_COLD_TESTS HERE, from your own measurement — and nowhere else."
            echo "::error::(This message used to send you to a number in CONTRIBUTING.md; there is none,"
            echo "::error:: and CONTRIBUTING says so itself: the live counters live ONLY in this file.)"
            exit 1
          fi
          # Anti-rot: this job must actually be running MORE than the default suite.
          if [ "$EXPECTED_COLD_TESTS" -le "$EXPECTED_DEFAULT_TESTS" ]; then
            echo "::error::EXPECTED_COLD_TESTS ($EXPECTED_COLD_TESTS) <= EXPECTED_TESTS ($EXPECTED_DEFAULT_TESTS):"
            echo "::error::the cold_tier feature is no longer contributing tests — this job has become a no-op."
            exit 1
          fi
          # `P8.9-m` — LE RÉSIDU EST MESURÉ ICI AUSSI, et pas seulement après la suite par défaut.
          # Ce script CITAIT ce job depuis toujours (« le job cold_tier détourne $TMPDIR ») sans y
          # être appelé : mesuré le 2026-08-30, un pas sur six. L'appel est DANS le même `run:`,
          # donc sous le même $TMPDIR isolé exporté plus haut — le mesurer depuis un autre pas ne
          # regarderait pas le bon répertoire. Le câblage est lui-même gardé
          # (`--verifier-le-cablage`, job `gardes`) : il ne peut plus se défaire en silence.
          "$GITHUB_WORKSPACE/.github/aucun-temporaire-laisse.sh" "suite cold_tier"
          [ "$rc_canal" = 0 ] || exit "$rc_canal"

  # ---------------------------------------------------------------------------
  # shell — SYNTAX GATE FOR THE COLLECTORS.
  #
  # WHY: the shell collectors are a production ingest path (43 tracked scripts, ~3.9k lines
  # measured today) and NOTHING in this pipeline looked at them — `grep -rE
  # "shellcheck|shfmt|bash -n" .github/` returned zero hits. A stray `fi`, an unclosed quote or
  # a broken heredoc shipped green and only failed on the host, at collection time, as MISSING
  # DATA — the same silent-loss failure mode the cold-tier job exists for.
  #
  # WHAT THIS DOES AND DOES NOT GUARANTEE — read before trusting it:
  #   `bash -n` is a PARSE check. It proves each script is syntactically valid bash. It proves
  #   NOTHING about behaviour: an unquoted variable, a wrong path, a silently-failing pipeline
  #   all parse fine. This is a floor, not a safety net.
  # ---------------------------------------------------------------------------
  shell:
    name: shell (bash -n — all tracked scripts)
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # `P8.9-f` — la garde « l'index concorde avec ce qu'un commit déclare fermé » compare
          # l'HISTOIRE à l'index. MESURÉ le 2026-08-25 : sur un clone `--depth 1` elle sort en 2,
          # c'est-à-dire qu'elle REFUSE DE CONCLURE au lieu de verdir en étant aveugle — le
          # comportement voulu, mais elle n'atteste alors rien. L'historique complet est ce qui la
          # rend utile ; ce dépôt en compte quelques centaines de commits, le coût est nul.
          fetch-depth: 0

      # Le compteur de la suite est écrit ICI et NULLE PART AILLEURS. Il l'a été dans quatre
      # fichiers, et CONTRIBUTING prescrivait même de mettre les quatre à jour ; relevé le
      # 2026-07-30 alors que ce fichier était juste : CONTRIBUTING disait 758, audit.toml 762,
      # tests/saml.rs et tests/ai.rs 600 — soit 162 de moins que le réel. Un compteur dupliqué
      # ne fait que pourrir, et il pourrit vers le dangereux : un lecteur croit la suite plus
      # petite qu'elle n'est, donc qu'un écart de comptage est normal, alors que la CONSTANCE
      # de ce compte est l'invariant qui prouve qu'une feature OFF ne touche pas le build par
      # défaut. La garde n'énumère aucun fichier : elle lit la valeur vivante ici et exige
      # qu'elle n'apparaisse pas ailleurs, plus que toute affirmation chiffrée sur la taille de
      # la suite porte son ANNÉE — c'est cette seconde jambe qui attrape le nombre PÉRIMÉ, que
      # la première ne peut pas voir. Motif calibré par mesure (une version trop large faisait
      # 30+ faux positifs sur des ID d'événements et des adresses IP) ; résidus écrits dans le
      # script, pas cachés.
      - name: Le compte de tests n'existe qu'à un seul endroit, et toute mesure est datée
        run: python3 .github/scripts/check_no_duplicated_test_count.py

      # `P8.9-m` — LE CÂBLAGE D'UNE GARDE EST LUI-MÊME UNE PROPRIÉTÉ. `aucun-temporaire-laisse.sh`
      # (P7.1-a) décrit une suite qui ne laisse rien derrière elle ; MESURÉ le 2026-08-30, il n'était
      # appelé qu'après UN pas sur les SIX qui lancent `cargo test` ici — et il NOMMAIT le job
      # cold_tier dans son propre corps sans y être branché. Le branchement était un geste à la main
      # dans ce fichier, donc il se défaisait en silence à chaque pas ajouté. Ce contrôle le dérive :
      # les pas de suite sont lus dans les workflows, et les caisses qui DOIVENT prouver leur absence
      # de résidu sont celles qui portent `src/tmp_possede.rs` — l'engagement de possession lui-même.
      # Aucune liste n'est écrite : une caisse qui adopte `tmp_possede` arme ses pas toute seule, et
      # une caisse qui ne l'a pas est nommée hors portée au lieu d'être accusée d'un geste impossible.
      - name: La garde « aucun temporaire laissé » est câblée après chaque suite qui doit le prouver
        run: .github/aucun-temporaire-laisse.sh --verifier-le-cablage

      # MÊME DOCTRINE, AUTRE VALEUR — l'étiquette du cœur. MESURÉ le 2026-08-27 : le manifeste
      # épinglait `guatx-core` sur un tag, et NEUF autres lignes du dépôt (deux dans
      # ARCHITECTURE.md, deux dans CONTRIBUTING.md, une dans deny.toml, QUATRE ici même)
      # réécrivaient au présent un tag ANTÉRIEUR. Un lecteur qui vérifie « quel cœur ce dépôt
      # compile-t-il ? » sur la documentation obtenait la mauvaise réponse, et l'exception
      # `allow-git` de deny.toml se justifiait par une mesure périmée. La correction ligne à ligne
      # avait déjà été faite une fois (Dockerfile, docker-compose.yml) et la dérive est revenue
      # ailleurs : rien n'interdisait la recopie. La garde n'énumère aucun fichier et ne connaît
      # aucun numéro — elle cherche la FORME d'un épinglage (une version accrochée à `tag`/`@`) et
      # la refuse hors des deux fichiers qui font foi. Une version citée SANS cette syntaxe (une
      # mesure DATÉE : « lecture du cœur v0.2.1 ») ne la déclenche pas, et c'est calibré sur
      # l'arbre : sans cette adjacence, sept citations historiques légitimes auraient rougi.
      - name: L'étiquette du cœur ne vit qu'au manifeste (recopiée, elle vieillit)
        run: python3 .github/scripts/check_the_core_pin_is_never_recopied.py

      # Un capteur qui ne peut pas collecter doit le DIRE. MESURÉ le 2026-08-01 sur une VM Ubuntu
      # 24.04 Server fraîche : `auditd.sh` sortait en SUCCÈS sans rien émettre parce que auditd
      # n'est pas installé — le SOC ne pouvait pas distinguer « capteur aveugle » de « rien à
      # signaler ». MESURÉ : 29 des 37 capteurs livrés portaient la même forme, 50 sorties muettes.
      # La garde n'énumère aucun capteur : elle exige que TOUTE sortie anticipée soit classée par
      # l'une des trois primitives de lib.sh, donc un capteur écrit demain est couvert d'office.
      - name: Un capteur qui ne peut pas collecter doit le dire (aucune sortie muette)
        run: python3 .github/scripts/check_collector_exit_is_classified.py

      # MÊME DÉFAUT, AUTRE LANGAGE — et sur la flotte DOMINANTE en entreprise. La garde ci-dessus ne
      # balaie que `collectors/*.sh` : le collecteur Windows était hors périmètre, et il portait les
      # deux défauts. MESURÉ le 2026-08-02 (harnais pwsh, collecteur exécuté TEL QUEL) : (1) central
      # indisponible pendant UN run -> 42 événements éligibles, 0 arrivés, filigrane avancé quand
      # même -> 42 PERDUS DÉFINITIVEMENT ; (2) journal Security en accès refusé -> 12 attendus, 0
      # arrivé, 0 aveu, code retour 0, battement « ok ». Après correctif, mêmes scénarios :
      # 0 perdu / 42 rattrapés, et 5 aveux `collect_status=unavailable`.
      # La garde ne cite aucun capteur : elle exige que tout `catch` classe ou relève, qu'aucune
      # erreur ne soit avalée en silence, et qu'aucun filigrane ne s'écrive avant un POST acquitté.
      # `pwsh` est fourni par l'image ubuntu-24.04 (PowerShell 7.6.3, vérifié dans le README de
      # actions/runner-images) ; s'il manquait, la garde ÉCHOUE au lieu de passer.
      - name: Le collecteur Windows ne peut ni mentir ni oublier (AST PowerShell)
        run: python3 .github/scripts/check_windows_collector_is_honest.py

      # MÊME CAPTEUR, DÉFAUT VOISIN, ET IL SE LIT DANS L'ÉCART ENTRE LES DEUX ÉMETTEURS LIVRÉS.
      # MESURÉ le 2026-08-29 (`P4.12-a`) : Windows nomme `IpAddress` l'adresse d'un échec d'ouverture
      # de session, et les deux lecteurs d'Event Log recopient ce nom VERBATIM dans `fields`. La
      # colonne `event.src_ip`, elle, n'est peuplée que depuis les noms que `promoted_fields` du CIM
      # déclare — `IpAddress` n'en fait pas partie. Le capteur PowerShell faisait la jonction depuis le
      # 2026-08-02 (`-SrcIp $d['IpAddress']`) ; `agent/src/source/windows.rs` ne l'a JAMAIS faite. Le
      # même enregistrement 4625 rendait donc une entité par un capteur et AUCUNE par l'autre, et les
      # deux règles livrées qui détectent par entité (« Brute-force auth par IP » ; « RBA :
      # brute-force d'authentification ») donnaient un verdict qui dépendait du capteur installé.
      #
      # LE TITRE DE CETTE ÉTAPE A ÉTÉ CORRIGÉ LE 2026-08-29, PARCE QU'IL SUR-PROMETTAIT. Il annonçait
      # « la MÊME colonne pour le MÊME enregistrement » ; la garde ne mesurait alors que l'ÉGALITÉ de
      # deux listes de sentinelles, dérivées côté PowerShell du seul chemin d'AUTHENTIFICATION — et le
      # site WFP du même fichier ne portait AUCUNE frontière. Elle était donc VERTE sur exactement la
      # divergence qu'elle nommait (un 5157 en boucle locale : `src_ip=127.0.0.1` d'un côté, rien de
      # l'autre), et son ÉGALITÉ interdisait par surcroît d'élargir la frontière — elle défendait le
      # trou. Elle mesure désormais CHAQUE site de promotion (un site en lecture directe est un ÉCHEC
      # nommé), exige une CONTENANCE et non une égalité, IMPRIME le surplus de l'agent, et lit les
      # deux fichiers SANS leurs lignes de commentaire (une propriété de code ne s'écrit pas en prose).
      # Elle n'énumère toujours aucun nom d'adresse, lit les clés admises dans le contrat CIM, REFUSE
      # de conclure sur un ensemble vide ou sur un corpus qu'aucun témoin n'éprouve, et porte ses
      # témoins positifs ET négatifs. Ce qu'elle NE tient PAS est écrit dans son en-tête et porté par
      # `P4.12-c` : elle compare des frontières, pas des formes canoniques.
      - name: Aucun capteur Windows livré ne laisse passer une adresse que l'autre écarte (P4.12-a)
        run: python3 .github/scripts/check_a_windows_sensor_promotes_the_address_it_carries.py

      # MÊME FAMILLE, AUTRE CAUSE : ici le capteur n'est pas muet, il ANNONCE une couverture que son
      # propre bac à sable lui retire. MESURÉ le 2026-08-20 (systemd 261, sonde différentielle à une
      # seule variable, `collectors/integrity.sh` exécuté TEL QUEL) : sous le `ProtectHome=true` que
      # le drop-in de durcissement partagé posait — un SCALAIRE, donc last-wins sur le
      # `ProtectHome=read-only` que l'unit posait exprès — la baseline FIM perd EXACTEMENT une
      # famille : 86 lignes au lieu de 87, zéro entrée `authkeys` au lieu de 1. Les clés SSH
      # autorisées, vecteur de persistance annoncé par l'en-tête du capteur ET livré comme détection
      # (docs/DETECTION-CATALOG.md, T1098.004), n'étaient donc pas surveillées sur un hôte agent —
      # sans erreur, sans avertissement, et alors qu'un central, lui, les surveillait.
      # La garde n'énumère aucun chemin : elle EXTRAIT la liste annoncée du capteur, recompose le bac
      # à sable effectif (unit + drop-in, scalaires écrasés / listes unionnées) et refuse tout chemin
      # annoncé devenu injoignable. Elle porte ses propres témoins positif et négatif.
      - name: La couverture ANNONCÉE par un capteur doit être ATTEIGNABLE sous son bac à sable
        run: python3 .github/scripts/check_fim_coverage_is_reachable.py
      - { name: "Le capteur d'intégrité voit un drop-in systemd et les types d'unités du chemin de recherche DÉRIVÉ, dit la voie, et avoue un répertoire illisible (P3.8-a)", run: .github/scripts/verifier-fim-couvre-les-unites-systemd.sh }

      # MÊME FAMILLE ENCORE, ET C'EST LA PLUS COÛTEUSE : ici le capteur n'est ni muet ni amputé — il
      # PUBLIE, et ce qu'il publie est la valeur la plus rassurante de sa série. `collectors/resources.sh`
      # alimente QUATRE règles à seuil (mémoire, fuite slab noyau, processeur, occupation du disque
      # racine). Une règle dont l'entrée vaut 0 n'est pas en retard : elle est STRUCTURELLEMENT INERTE,
      # et rien ne le dit. Quatre chemins y menaient — un bloc `END` d'awk qui imprime 0 quand le motif
      # n'a jamais matché, le code de retour de `df` avalé par le tube, un motif de noms d'interface
      # qui ne matche aucune convention usuelle (`ens3`, `eno1`, `wlp3s0`), et une variable vide
      # interpolée DANS un programme awk. La garde exécute le capteur TEL QU'IL EST LIVRÉ contre une
      # arborescence fabriquée et un `df` stubé : rien de la machine de CI n'entre dans le verdict.
      # Elle exige les DEUX témoins et n'énumère aucune mesure (les clés du second sont découvertes
      # dans la sortie du premier) : source lisible valant réellement zéro -> la mesure est PUBLIÉE
      # à 0 ; source non exploitable -> AUCUN nombre, et un aveu qui NOMME la clé et sa cause.
      - name: Une mesure d'hôte qui n'a pas pu être prise n'est pas publiée comme un zéro
        run: python3 .github/scripts/check_host_measures_are_honest.py

      # LA MÊME CHAÎNE, À SON DERNIER MAILLON (`S37`) : le démon publie `<clé>_verdict` / `_cause` /
      # `_detail` à côté de chaque grandeur qu'il n'a pas pu lire (`S32`, `P4.1-r`), et tout s'arrête à
      # la frontière du navigateur si la surface ne LIT pas le verdict — une grandeur illisible s'y
      # rend alors comme un zéro, un tiret ou une case vide. La garde DÉRIVE du code du démon
      # l'ensemble des clés émises (appels à `poser_dans` / `poser_verdict_dans` / `poser_bilan`,
      # gabarit `{boucle}_abandons` résolu sur la table des boucles) et exige qu'un module web lise
      # chacune dans son CODE — une clé citée en commentaire ne compte pas. Elle valide son instrument
      # dans les deux sens, refuse de conclure sous trois clés ou sans sa clé témoin, puis lance le
      # harnais ESM (`web_esm_harnais.mjs`, shim DOM sans réseau) qui lie les modules de `web/` et
      # rend le panneau Système sur des objets fabriqués : état « NON LISIBLE » avec sa cause, abandons
      # visibles, et la valeur — vrai zéro compris — quand le verdict est « lu ».
      - name: La surface web lit chaque verdict que le démon publie, et le rend comme un état
        run: python3 .github/scripts/check_web_reads_every_verdict.py
      - { name: "Toute route sensible du démon (détruit / élève un droit / arme une réponse — DÉRIVÉE du routeur) a ses appelants web derrière la confirmation partagée (P11.5-b)", run: python3 .github/scripts/check_sensitive_routes_are_confirmed.py }

      # `P7.19-d` — LA SYNTAXE D'UN GABARIT DE ROUTE EST CELLE DU ROUTEUR, PARTOUT OÙ ELLE EST ÉCRITE.
      # La montée `axum` 0.7 -> 0.8 fait passer `/api/cases/:id` à `/api/cases/{id}`, et la bibliothèque
      # PANIQUE au démarrage sur un littéral resté en arrière : la table de routage ne pouvait donc pas
      # dériver. Tout le reste, si — mesuré le 2026-08-26 APRÈS la migration de la table, l'ancienne
      # syntaxe survivait dans cinquante-huit fichiers, dont DEUX corps de refus SERVIS (le refus « store
      # de bans plein » renvoyait l'exploitant vers `DELETE /api/netban/:ip`, le refus de purge sous
      # rétention légale vers `/api/legal-holds/:id/release`) et CINQ documents d'intégration publiés.
      # Une garde qui ne juge que les littéraux de `.route(` tient donc le seul endroit que la
      # bibliothèque tenait déjà. Le critère n'est pas « interdire l'ancienne forme » — ce serait rendre
      # inécrivable la seule phrase qui vaille sur cette rupture — c'est : une ligne qui porte l'ANCIENNE
      # doit porter la NOUVELLE sur la MÊME ligne. Prouvée par MUTATION le 2026-08-26 : remettre UN
      # commentaire de `web/` à l'ancienne syntaxe la fait rougir, le restaurer la fait reverdir.
      - { name: "Un gabarit de route s'écrit dans la syntaxe du routeur partout où il est décrit — démon, console, documents publiés (P7.19-d)", run: python3 .github/scripts/check_route_templates_use_the_router_syntax.py }
      - { name: "Aucun bouton de la surface web sans classe partagée ni contexte stylant — contextes et classes DÉRIVÉS de style.css, sites dérivés des modules (P11.4-b)", run: python3 .github/scripts/check_every_button_wears_shared_chrome.py }

      # `P11.14-c` — UN REFUS NE SE REND JAMAIS COMME UNE ABSENCE. Le panneau DLP décidait de son
      # affichage sur UNE condition (`!j || j.error || !Array.isArray(j.rows) || !j.rows.length`) :
      # un refus du démon, une réponse illisible, une panne réseau et un vrai vide y entraient, et
      # « Aucun changement récent (<fenêtre>) — ou capteur inactif » en sortait. D'où la contradiction
      # relevée en usage réel le 2026-08-25 — toute la rétention rendant MOINS que sept jours — alors
      # que le SQL de la fenêtre large est celui de l'étroite MOINS son conjoint `ts >= …` et rend
      # toujours au moins autant : ce que le démon rendait de différent, c'était un REFUS (422 froid
      # tronqué, 400 budget). La garde juge la CONDITION, pas la phrase (une phrase se reformule) :
      # aucune expression de `web/` ne doit décider dans le MÊME test qu'une lecture a échoué et
      # qu'elle est vide. Population DÉCOUVERTE par parcours de `web/`, instrument validé dans les
      # deux sens, refus de conclure sous ses planchers. Ce qu'elle NE tient PAS — la phrase rendue
      # ensuite — est tenu par le harnais ESM (témoin 16), qui exige trois issues distinctes.
      # `P11.23-b` — UN TEST QUI REND LA MAIN SANS CONCLURE LE DIT PAR LE CANAL. Mesuré le
      # 2026-08-31 sur cet arbre : QUATORZE chemins de `daemon/src/tests/` sortaient d'un test sans
      # exercer son assertion (levier de configuration éteint, plate-forme sans `/proc`, clé
      # SQLCipher dans l'environnement) ; NEUF n'imprimaient rien, et les CINQ qui imprimaient ne
      # valaient pas mieux — `libtest` détourne la sortie d'un test qui RÉUSSIT et ne la rend que
      # pour un test qui ÉCHOUE (mesuré sur une caisse d'essai : 0 occurrence sous `cargo test` nu,
      # 3 sous `--nocapture`, 3 sous `--show-output`). L'aveu partait dans le vide et le portail ne
      # lisait que le 0. La garde N'EXIGE PAS qu'un test échoue sur un environnement aveugle : ce
      # serait une rançon. Elle exige que le refus EMPRUNTE LE CANAL. Son critère est POSITIONNEL —
      # l'appel doit être dans LE BLOC du `return` — et jamais lexical : chercher un mot d'aveu dans
      # un corps aurait laissé muette la seconde des DEUX sorties de refus d'un même test de
      # `vieillissement_serie.rs`. Témoins FABRIQUÉS dans la garde (dont ce cas précis), population
      # DÉRIVÉE de l'arbre syntaxique, et refus de conclure (code 2) quand elle ne peut pas voir.
      - { name: "Un test qui rend la main sans conclure le DIT par le canal — aucun refus muet dans daemon/src (P11.23-b)", run: python3 .github/scripts/check_a_test_that_declines_to_conclude_says_so.py }
      # `P11.23-g` — LE JUMEAU SANS SORTIE NI BRANCHE. Un test dont TOUTES les assertions vivent
      # dans une boucle sur une table DÉCLARÉE ailleurs que lui n'a rien à sauter et rien à
      # avouer : le jour où la table perd sa matière, la boucle itère zéro fois et `libtest`
      # compte un test PASSÉ de plus. MESURÉ le 2026-09-02, par propriété : DIX tests, dix-neuf
      # boucles porteuses, DOUZE tables — dont six vivent dans la caisse voisine `guatx_core`.
      # DÉMONTRÉ : `COLLECTORS` vidée sans plancher, le témoin de dédup rend « ok. 1 passed »
      # après ZÉRO assertion. Le remède n'est PAS le canal de refus — il les rendrait verts et
      # muets là où ils doivent ROUGIR : une table vidée est une panne d'INSTRUMENT, pas un
      # environnement aveugle. Le plancher est donc posé DANS LE FOURNISSEUR
      # (`daemon/src/tests/plancher_des_tables_declarees.rs`), même geste que celui qui a fermé
      # deux des neuf sites de `P11.23-e`, et cette garde l'impose aux consommateurs futurs.
      # Verdict PAR BOUCLE (un site routé ne blanchit pas son voisin), analyseur lexical IMPORTÉ
      # de la garde ci-dessus et non recopié, quatorze témoins FABRIQUÉS dans la garde, population
      # DÉRIVÉE comme contrôle positif contre le vert par vacuité, et refus de conclure (code 2)
      # si le plancher n'exige plus rien ou n'accuse plus l'INSTRUMENT.
      - { name: "Un test qui boucle sur une table déclarée passe par le plancher — vider la table ROUGIT (P11.23-g)", run: python3 .github/scripts/check_a_test_that_loops_over_a_declared_table_has_a_floor.py }
      - { name: "Un refus du démon ne se rend jamais comme une absence de données — aucune condition de web/ ne fond l'échec et le vide (P11.14-c)", run: python3 .github/scripts/check_a_refusal_is_not_rendered_as_an_absence.py }
      # `P4.13-b` — UNE ÉCRITURE NUE AU STOCKAGE DU SITE JETTE AU MILIEU D'UN GESTE. Mesuré le
      # 2026-08-30 sous `PLUME_HARNAIS_STOCKAGE_REFUSE=1` : le tri des règles posait son état sans que
      # la liste soit repeinte, et un dépôt de carte laissait l'ordre inchangé. Le geste ne rend pas
      # MOINS, il rend un état INCOHÉRENT — et c'est pire qu'un refus, parce qu'il n'y a rien à lire
      # pour le comprendre. Population DÉRIVÉE du DISQUE (`web/**/*.js`, donc un module neuf est vu
      # avant d'être suivi — l'inverse de la faute à corpus `git ls-files` déjà payée ici), analyseur
      # lexical qui écarte commentaires, chaînes, gabarits et littéraux d'expression régulière, témoins
      # FABRIQUÉS DANS LA GARDE et non lus du dépôt, cliquet à zéro, et refus de conclure (code 2) si le
      # corpus ne porte AUCUNE écriture — contrôle positif contre le vert par vacuité.
      - { name: "Aucune écriture au stockage du site n'est nue dans web/ (P4.13-b)", run: python3 .github/scripts/check_no_naked_site_storage_write.py }

      # `P11.21-k` — UN AVEU SE PROUVE LÀ OÙ IL SE VOIT, PAS LÀ OÙ IL EST ÉCRIT. Le harnais ESM
      # juge le RANG d'un nœud dans le document ; sa propre section 0 PUBLIE qu'il ne tient NI la
      # mise en page NI le style calculé, et écrit la conséquence : « un masquage, une troncature
      # ou une couleur imposés par la feuille de style sont invisibles ici […] la preuve passe
      # alors par un vrai moteur de rendu ». Une règle qui masquerait l'aveu de matrice ATT&CK
      # partiellement lue, le rendrait transparent ou l'INVERSERAIT le laissait donc VERT.
      # UN SUBSTITUT STATIQUE EST REFUSÉ, ET LA RAISON EST DANS CE DÉPÔT : `web/index.html` porte
      # `html:not(.app-ready) main { visibility: hidden; }`, une règle INLINE qui masque toute la
      # surface depuis un ANCÊTRE sans jamais nommer l'aveu — aucune grammaire de sélecteur ne
      # relie ces deux faits. Cette garde ne lit AUCUNE propriété CSS : elle sert la VRAIE page et
      # la VRAIE feuille sur une origine HTTP locale (donc polices et chemins absolus de
      # production), y peint un balisage DÉRIVÉ de `web/attack.js` — ordre des nœuds LU dans la
      # source comprise — et demande au moteur ce qui est PEINT.
      # L'INSTRUMENT SE VALIDE AVANT TOUT VERDICT, ET DANS LES DEUX SENS : 10 mutations injectées
      # doivent TOUTES rougir, sinon la garde REFUSE (code 2) sans rien conclure. Deux passent par
      # un ANCÊTRE en `@media` + `:has()` — dont une par `display`, qui ne s'HÉRITE PAS, de sorte
      # que seule la marche vers les ancêtres peut la voir (elle nomme `section#attack-panel`) — et
      # une inverse l'ordre PEINT (`column-reverse`) à rang de document INCHANGÉ : c'est la preuve
      # que ce banc mesure autre chose que le harnais ESM. Deux autres peignent l'aveu SANS ENCRE
      # (`color:transparent`, puis encre égale au fond) — que ni `display`, ni `visibility`, ni
      # `opacity`, ni la géométrie ne voient.
      # CE QU'ELLE NE TIENT PAS : un seul gabarit de fenêtre (1600x1400) ; aucun test de
      # RECOUVREMENT (un nœud peint par-dessus, un `clip-path`, une encre seulement PÂLE passent) ;
      # et les aveux jumeaux de `web/alerts.js`, qui restent à couvrir.
      # LE MOTEUR EST LÀ, MESURÉ LE 2026-08-30 : zéro installation, `--dump-dom` d'un Chrome sans
      # tête suffit ; l'image `ubuntu-24.04` — sur laquelle TOUS les jobs d'ici tournent, épinglée —
      # publie Google Chrome installé PAR DÉFAUT. Le coût a PLUS QUE DOUBLÉ le 2026-08-31 en couvrant
        # les deux aveux de la liste d'alertes : 11 rendus et une douzaine de secondes sont devenus
        # 22 rendus et une vingtaine — c'est dit ici plutôt que découvert. Le refus de conclure
      # n'est donc pas un bruit permanent : c'est le canal du jour où le moteur quitterait l'image.
      - { name: "Un aveu est PEINT là où il est lu — moteur de rendu, ni masqué ni transparent ni inversé ni rogné, sur la matrice ATT&CK ET les deux aveux de la liste d'alertes (P11.21-k, P11.21-l)", run: python3 .github/scripts/check_an_admission_is_painted_where_it_is_read.py }

      # `P11.21-n` — LA LANGUE DÉCLARÉE EST CELLE QUI EST RENDUE, ET C'EST LA GARDE VOISINE QUI A NOMMÉ
      # LE DÉFAUT SANS LE CORRIGER : « `web/index.html` déclare `lang="fr"` en dur et AUCUN module ne pose
      # jamais `document.documentElement.lang` ». Mesuré le 2026-08-31 dans un vrai moteur : une console
      # dont TOUS les libellés sont peints en anglais annonçait `lang="fr"` — un lecteur d'écran y prononce
      # l'anglais avec la phonétique française. MÊME MOTEUR, MÊME ABSENCE D'INSTALLATION que la garde
      # ci-dessus, et le coût est plus BAS qu'elle : SIX rendus, ~17 s (mesuré sur le poste le 2026-08-31,
      # deux suites cargo tournant en parallèle), parce que les trois cellules de langue — pas de choix en
      # stockage, `fr`, `en` — tiennent dans UN SEUL rendu. Une garde de TEXTE aurait été moins chère et
      # AVEUGLE à deux des cinq mutations que ce fichier joue : un attribut posé sur une CONSTANTE, et un
      # attribut reposé plus tard par un AUTRE module.
      - { name: "La langue DÉCLARÉE par le document est celle qu'il PEINT — moteur de rendu, trois cellules de langue, vocabulaire dérivé du dictionnaire (P11.21-n)", run: python3 .github/scripts/check_the_declared_language_is_the_one_rendered.py }

      # `P11.18-w` — L'ENVIRONNEMENT DU PROCESSUS EST **UNE** RESSOURCE, ET ELLE N'A QU'UN VERROU.
      # `cfg()` résout toute clé dans l'ordre `env > conf > défaut` : un test qui pose une variable ne règle
      # pas SON comportement, il règle celui de tous les tests qui tournent au même instant — et il écrase la
      # `conf` d'un voisin qui croyait décider seul. MESURÉ le 2026-08-25 : un test du tier froid a échoué UNE
      # FOIS SUR DEUX exécutions complètes, sur une assertion qui DIT « tier froid OFF » pendant qu'un test de
      # plafonds posait la variable à 1 — une intégration continue rouge que rien ne distinguait d'une
      # régression réelle. LA CAUSE N'ÉTAIT PAS L'ABSENCE DE VERROU MAIS LEUR NOMBRE : NEUF verrous pour une
      # seule ressource, plus DOUZE tests sans aucun. Deux verrous pour une ressource, c'est zéro verrou.
      # Il n'en reste qu'UN, et cette garde refuse tout test qui mute l'environnement sans le tenir EN
      # ÉCRITURE — le mode qui exclut, un mutateur sous verrou de lecture n'excluant personne. Population et
      # mutateurs DÉRIVÉS des sources, fonctions associées comprises, sans quoi deux utilitaires passaient ;
      # corpus lu sur le SYSTÈME DE FICHIERS et non `git ls-files` (`P11.13-d`) ; témoins positif, négatif, de
      # mode et de fermeture ; refus de conclure (sortie 2) sous ses planchers. Elle DIT ce qu'elle ne tient
      # pas : le côté lecteur, qui n'est pas une propriété syntaxique, et la durée de tenue.
      - { name: "Aucun test du démon ne mute une variable d'environnement du processus sans tenir EN ÉCRITURE le verrou UNIQUE de cette ressource (P11.18-w)", run: python3 .github/scripts/check_no_test_mutates_the_process_env_unlocked.py }

      # `P11.4-d` — AUCUNE RÈGLE DE STYLE SANS CIBLE. Les sélecteurs d'identifiant et de classe sont DÉRIVÉS de
      # `style.css`, le corpus où un nom peut être posé est DÉRIVÉ de `web/` (code sans commentaires) ; un nom
      # construit par concaténation ou gabarit est « indécidable » et nommé, jamais compté orphelin. Mesuré le
      # 2026-08-22 : 521 noms distincts, 15 orphelins (dont `#ack-all`, mort depuis le chrome partagé), tous
      # retirés ; plafond zéro. L'instrument se valide (une règle `#inexistant{}` rougit, `.btn` non).
      - { name: "Chaque identifiant ou classe ciblé par style.css est posé quelque part dans web/ — dérivé, plafond d'orphelins à zéro (P11.4-d)", run: python3 .github/scripts/check_every_style_selector_has_a_target.py }
      # `P11.8-n` — AUCUN PARCOURS D'ARBRE NU DEPUIS UNE RACINE DOMINANTE. `P11.8-m` a écrit le geste
      # d'élagage UNE fois (`NOMS_HORS_ARBRE`, `parcours_des_sources`) ; rien ne rougissait sur un parcours
      # écrit à la main sans élagage, et le trou restait refermable en silence par le prochain qui en
      # écrirait un. La forme a été TRANCHÉE PAR LA MESURE le 2026-08-31, et deux formes plus larges ont été
      # REFUSÉES : accuser TOUT parcours récursif poserait un cliquet à NEUF gardes saines par une propriété
      # des outils (cargo écrit `target/` à côté de `src/`, jamais dedans) — une rançon ; accuser « les
      # racines qui peuvent recevoir un artefact » ferait dépendre le verdict d'un `cargo build`, donc
      # VERT PAR CONSTRUCTION sur la copie neuve de l'intégration. Est accusé le parcours RÉCURSIF dont la
      # racine est DOMINANTE — dérivée de la position du fichier sans descendre, ou désignée — et qui
      # n'exclut aucun artefact ; un parcours PLAT ne l'est jamais, il ne descend rien. Mesuré le
      # 2026-08-31 : 53 instruments, 1 parcours dominant, 0 nu, 1 copie divergente (plafond daté à 1).
      - { name: "Aucun instrument ne parcourt récursivement une racine dominante sans élaguer les artefacts — racine dérivée de la position du fichier, propagée aux paramètres (P11.8-n)", run: python3 .github/scripts/check_no_guard_walks_the_tree_unpruned.py }
      # `P11.4-e` — CHAQUE DÉCLENCHEUR D'AIDE OUVRE UNE SECTION. Déclencheurs DÉRIVÉS de `web/` (`data-help`
      # en HTML et dans les gabarits, `dataset.help`, `openHelp('…')`, entrées du sommaire du guide), sections
      # DÉRIVÉES de l'objet `HELP` du registre d'aide (`web/help_registry.js`, lui-même dérivé de sa définition,
      # pas nommé) ; une section sans déclencheur est une information. Mesuré le
      # 2026-08-22 : deux déclencheurs sans section (« tokens » en HTML, « processors » au sommaire du guide) ;
      # l'ouvreur se taisait (harnais : zéro nœud rendu), il rend désormais un aveu qui nomme la clé.
      - { name: "Chaque déclencheur d'aide de la console (HTML et JS) ouvre une section du registre d'aide — dérivé dans les deux sens, module du registre dérivé (P11.4-e)", run: python3 .github/scripts/check_every_help_trigger_has_a_section.py }

      # `P11.8-a` — LE LEXIQUE FR/EN COUVRE CE QUE CHAQUE MODULE AFFICHE. La console traduit par dictionnaire
      # (`i18n.js`, correspondance EXACTE d'un nœud texte ou d'un attribut affiché) : une chaîne sans clé
      # reste en français, en silence. MESURÉ le 2026-08-22 sur la propriété « chaîne posée dans un puits de
      # rendu » (critère écrit dans la garde, avec ce qu'elle ne voit pas) : 1 568 chaînes statiques affichées,
      # 16 % couvertes avant complément. La garde DÉRIVE la population du code (puits de rendu, littéraux HTML,
      # `index.html`), valide son instrument sur un corpus témoin dans les deux sens, refuse de conclure sous
      # ses planchers, et tient un PLAFOND DE TROUS par module : une chaîne affichée de plus sans entrée rougit.
      - name: Le lexique fr/en couvre les chaînes affichées de chaque module web (aucun trou de plus)
        run: python3 .github/scripts/check_i18n_lexicon_covers_displayed_strings.py

      # LA MÊME FIGURE, PORTÉE À LA SURFACE ENTIÈRE : la garde ci-dessus tient UNE mesure d'hôte sur
      # UN capteur ; celle-ci tient tout capteur shell qui publie des NOMBRES. Sa portée est
      # DÉCOUVERTE (le capteur publie une enveloppe `kind:"metrics"`), jamais listée — un capteur de
      # métriques ajouté demain est contrôlé d'office. Elle exécute chaque capteur TEL QU'IL EST
      # LIVRÉ contre un PATH fabriqué où ses commandes externes sont des stubs, et mute UN SEUL BIT :
      # au témoin (1) tous rendent 0 avec une sortie vide (une source LUE, réellement vide) ; au
      # témoin (2) une seule commande rend 1 (cette lecture-là a échoué). Ce qu'elle exige est que la
      # sortie PUBLIÉE change avec ce bit, que l'aveu NOMME les séries perdues, et qu'il n'en nomme
      # aucune qui soit encore publiée. Les clés ne sont pas énumérées : celles du témoin (2) sont
      # découvertes dans la sortie du témoin (1). Elle valide son propre instrument avant de croire
      # son verdict — fragments témoins pour la part statique, extracteur de commandes contrôlé dans
      # les deux sens, et mutation déclarée INCONCLUANTE plutôt que verte quand une sortie vide n'est
      # pas une lecture nominale. Sous deux capteurs exerçables, elle ÉCHOUE au lieu de conclure.
      - name: Un capteur shell ne publie pas un nombre qu'il n'a pas pu lire
        run: python3 .github/scripts/check_collector_reads_are_honest.py

      # MÊME FAMILLE, PORTÉE SUR UN MÉCANISME DE SÉCURITÉ, ET C'EST LÀ QU'ELLE COÛTE LE PLUS : un
      # enforcer qui ne peut pas lire sa LISTE DE PROTECTION prenait la branche la plus permissive,
      # et rien ne le disait. `respond.sh` lisait la liste des IP à NE JAMAIS bannir par
      # `[ -r "$F" ] && grep -qxF ...` : fichier manquant, accès refusé et recherche en erreur (un
      # RÉPERTOIRE — `-r` le passe, et sous root il passe même un mode 000) tombaient sur la même
      # branche que « lue, l'IP n'y est pas », c'est-à-dire sur « bannis ». `engagement-adapter.sh`
      # faisait pire : son en-tête ÉCRIT l'invariant inverse (« une exemption est une défense
      # BAISSÉE : son mode de panne DOIT être re-arm »), et son compteur d'armement se lisait
      # `"$(cat "$F" 2>/dev/null || echo 0)"` — un compteur illisible ne franchit JAMAIS son seuil,
      # donc le REVERT-ALL promis ne partait plus tant que le central restait injoignable.
      # La garde n'écrit AUCUNE liste d'enforcers : elle la DÉRIVE de la garde ci-dessus (dont le
      # critère est déjà auto-invalidant), et rougit tant qu'un enforcer ajouté là-bas n'a pas ici
      # ses DEUX témoins. Chaque enforcer est exécuté TEL QU'IL EST LIVRÉ, dans un PATH fabriqué où
      # `curl`/`nft` sont des bouchons : liste illisible -> la protection REFUSE et le refus est
      # NOMMÉ (cause de l'ensemble fermé) ; liste lisible et RÉELLEMENT vide -> comportement normal,
      # sans refus. Sans ce second témoin, un enforcer qui refuserait toujours passerait le premier.
      - name: Un enforcer qui ne peut pas lire sa liste de protection REFUSE (fail-closed)
        run: python3 .github/scripts/check_enforcer_lists_fail_closed.py

      # `P4.7-j` — L'IDENTITÉ D'UNE ADRESSE EST SA VALEUR, ET IL N'EN EXISTE QU'UNE LECTURE.
      # MESURÉ le 2026-08-28 : le démon portait TROIS canonicalisations aux verdicts DIFFÉRENTS sur
      # `::ffff:a.b.c.d` — `ssrf_norm_ip` REPLIE (`to_ipv4_mapped`), `parse::<IpAddr>() + to_string()`
      # ne replie PAS, `ipaddress`/`.compressed` côté hôte non plus. La deuxième était employée SIX
      # fois, sur ce qui clé le store `net_ban`, sur l'IP réelle du client et sur le pair TCP : elle
      # CONVERGEAIT les écritures exotiques VERS la forme qui traversait la protection.
      # POPULATION DÉCOUVERTE par parcours (« obtenir une adresse comme valeur, puis la rendre en
      # chaîne »), jamais une liste de fichiers : un site écrit demain est couvert sans être nommé.
      # La garde inclut À DESSEIN les sites CONFORMES, sinon elle n'apparierait plus rien le jour où
      # le dernier fautif disparaît, et son vert cesserait de dire quoi que ce soit ; population
      # vide ou canonicaliseur introuvable -> elle REFUSE DE CONCLURE (code 2) au lieu de rendre vert.
      # La moitié SHELL ne peut pas appeler le canonicaliseur du démon : elle doit porter un AVEU
      # ÉCRIT qui NOMME la divergence, mesurée et inscrite dans `collectors/engagement-adapter.sh`.
      - name: Il n'existe qu'UNE canonicalisation d'adresse, et c'est celle qui replie la forme mappée (P4.7-j)
        run: python3 .github/scripts/check_one_canonical_address_form.py

      # `P4.7-g` — UN NOM, DEUX QUESTIONS : `parse_excl_item` sert le RENDU D'AFFICHAGE (où un préfixe
      # TEXTUEL est la BONNE réponse, la seule qui se rende en `NOT LIKE 'préfixe%'`) ET l'ENFORCEMENT
      # (où c'est la mauvaise, et l'écart va DANS LES DEUX SENS : `172.16.0.0/12` protégeait tout
      # 172/8, `128.0.0.0/1` une SEULE adresse). Le remède n'était donc PAS de remplacer l'analyseur —
      # analyser les deux côtés en valeurs CASSERAIT le débruitage des panneaux, et le témoin
      # `excl_v54_parse_and_clause_generation` le tient sur ses chaînes attendues EXACTES — mais de
      # SÉPARER LES DEUX CONSOMMATEURS. ÉTAT MESURÉ AVANT LE LOT : QUATRE appels d'enforcement
      # (`ledger.rs` x2, `handlers/engagement.rs` x2) -> la garde était ROUGE avant, VERTE après.
      # Le module d'AFFICHAGE est DÉRIVÉ (le fichier qui définit `ExclClauses`), pas écrit.
      - name: L'analyseur du rendu d'affichage n'a QUE des consommateurs d'affichage (P4.7-g)
        run: python3 .github/scripts/check_display_parser_has_only_display_consumers.py

      # `P7.19-b` — « JE N'AI PAS PU MESURER » NE SORT PAS PAR LE MÊME CANAL QUE « LA PROPRIÉTÉ EST
      # VIOLÉE ». Le banc de `daemon/src/tests/query_verify.rs` refuse de conclure hors de sa fenêtre
      # de validité, et son message porte une marque. MESURÉ le 2026-08-27 : `grep -rn "INSTRUMENT
      # NON" .github/` rendait ZÉRO — la marque existait, le TRI n'existait nulle part, et le module
      # affirmait pourtant que « la CI trie sur cette marque et rend le code 2 ». Mécanisme POSÉ,
      # présenté comme ARMÉ. Ce pas garde le tri qui l'arme : il exerce ses témoins POSITIF (marque
      # -> 2) et NÉGATIF (assertion violée -> le code de cargo ; suite verte -> 0) sur la fonction
      # que les pas de test appellent RÉELLEMENT, puis vérifie deux propriétés DÉRIVÉES — la marque
      # est bien émise par la fonction de refus, et TOUT pas qui lance `cargo test` dans le crate du
      # banc passe son journal au tri. La marque n'est recopiée nulle part : elle est lue dans le
      # banc, et sa disparition fait REFUSER la garde (code 2) au lieu de la rendre verte.
      - name: Un banc qui refuse de conclure sort par un canal DISTINCT d'une propriété violée
        run: python3 .github/scripts/check_a_bench_refusal_is_a_distinct_channel.py

      # MÊME FAMILLE QUE LE PAS PRÉCÉDENT, UN CRAN PLUS TÔT : là le CANAL était faux, ici c'est la GRANDEUR.
      # Deux témoins écrits le même jour annonçaient un COMPTE (« quatre lectures de `/proc` »,
      # « six opérations atomiques »), disaient noir sur blanc que leur plafond en était dérivé « pas
      # d'une durée », et assertaient un RAPPORT DE MÉDIANES DE DURÉES. Le 2026-08-30, le portail de
      # déploiement — qui rejoue la suite sur la machine de build PENDANT qu'elle compile — a fait
      # rendre 13,50 au premier là où la composition en vaut 4, sur un arbre INTACT : une accusation
      # FAUSSE, par un témoin vert au repos. Le second se donnait 2,05 de marge, moins que l'excursion
      # de 3,2 déjà constatée sur son jumeau.
      # LA GARDE EXIGE DEUX JAMBES CONJOINTES, et la conjonction CHOISIT le sens de l'erreur : (A) la
      # grandeur comparée dérive d'une HORLOGE, reconnu par flot de données sans aucun lexique ; (B) la
      # prose ATTACHÉE à cette grandeur — le message de l'assertion et la doc des constantes qu'elle
      # nomme, jamais le récit alentour — annonce un COMPTE D'OPÉRATIONS. Sans (B), silence : un budget
      # de latence, une temporisation, un filet, deux durées comparées entre elles sont des propriétés
      # TEMPORELLES légitimes, et une garde qui les accuserait finirait désarmée. Elle NE lit PAS la doc
      # de la fonction : un fichier doit pouvoir DÉCRIRE ce défaut sans être accusé de le commettre.
      # Ses six témoins (un à accuser, cinq à blanchir) sont FABRIQUÉS DANS LA GARDE : aucun plancher
      # sur l'état de l'arbre, qui rougirait le jour où le travail est fini.
      - name: Un témoin qui ANNONCE un compte ne le tient pas avec une HORLOGE
        run: python3 .github/scripts/check_a_counted_property_is_never_asserted_through_a_clock.py

      # MÊME FAMILLE, APPLIQUÉE À LA DOCUMENTATION : ici ce n'est pas une couverture annoncée qui est
      # injoignable, c'est le DOCUMENT lui-même. Un document à jour que rien ne référence est
      # invisible, donc inexistant pour le lecteur. MESURÉ le 2026-08-20 : 11 des 45 documents suivis
      # n'étaient la cible d'AUCUN lien Markdown — dont le document de conception portant l'état le
      # plus frais des travaux d'échelle, l'agent d'endpoint, le récepteur syslog et le harnais de
      # mesure. Aucun n'était brouillon ; chacun était introuvable.
      # La garde n'énumère aucun document : elle découvre les documents suivis par `git ls-files` et
      # exige un CHEMIN DE LIENS depuis `README.md`. Une MENTION en texte brut ne compte pas — c'est
      # exactement sous cette forme que le document d'échelle était « référencé », et une garde qui
      # l'aurait acceptée serait passée au vert sur le défaut qu'elle doit attraper. Elle porte ses
      # deux témoins (formes de liens qu'elle DOIT reconnaître, mentions qu'elle NE DOIT PAS compter)
      # et un plancher de liens résolus sur l'arbre réel, pour ne pas rendre vert en ne voyant rien.
      # UN DOCUMENT AJOUTÉ SANS ENTRÉE D'INDEX FAIT ROUGIR CE PAS, ET C'EST L'OBJET : l'index se tient
      # au moment où le document est écrit, pas lors d'un futur audit.
      - name: Un document suivi doit être ATTEIGNABLE depuis la page d'accueil
        run: python3 .github/scripts/check_every_doc_is_reachable.py

      # `P9.7-b` — CE QU'UN EXPLOITANT DOIT POUVOIR TROUVER EST DOCUMENTÉ. Même famille que le pas
      # précédent, appliquée non plus au FICHIER mais à la SURFACE : le corpus dérive parce que rien
      # ne le lie au code — un levier ajouté, un capteur livré, un onglet posé ne font rougir aucun
      # instrument s'ils ne sont écrits nulle part. MESURÉ SUR L'ARBRE SUIVI le 2026-08-25, avant la
      # campagne `P9.7-a` : sur les 37 onglets que la console DÉCLARE, TROIS avaient une entrée dans
      # le corpus ; les 34 autres n'étaient décrits nulle part. Dans le même temps 39 capteurs sur 39
      # en avaient une — le critère n'est donc pas hors d'atteinte, c'est une part de la surface qui
      # n'avait jamais été relue.
      # LES QUATRE INVENTAIRES SONT DÉRIVÉS, ET LES SOURCES LE SONT AUSSI. Le module de navigation
      # est l'unique fichier de `web/` qui porte la définition `const SPACES = [` ; le script de
      # retrait est l'unique fichier suivi qui porte le `case` validant le mode ; une bibliothèque
      # de capteurs se reconnaît à ce qu'un autre capteur la SOURCE, jamais à son nom. ANCRAGE SUR
      # LA PROPRIÉTÉ, PAS SUR LE NOM (`P11.13-d`) : prouvé par mutation le 2026-08-25 — les trois
      # sources renommées ET déplacées, la garde les retrouve, publie leurs nouveaux chemins, et
      # MORD toujours (37 onglets et 39 capteurs, comptes inchangés) au lieu de verdir en voyant
      # moins de choses à contrôler.
      # DEUX RÉGIMES. Onglets, capteurs et modes : plafond ZÉRO, chacun doit avoir une ENTRÉE — la
      # première cellule d'une ligne de tableau, jamais une simple mention en prose. Leviers
      # `PLUME_*` : plusieurs centaines, dont la majorité n'a d'autre documentation que le code qui
      # les lit — exiger zéro désarmerait la garde, on pose donc un CLIQUET, le nombre non documenté
      # est PUBLIÉ à chaque exécution et ne doit jamais AUGMENTER. Un vert affiche les quatre
      # comptes ; un rouge nomme ce qui manque et où le décrire ; un instrument muet (source de
      # dérivation absente ou dédoublée) REFUSE DE CONCLURE en code 2 au lieu de rendre vert.
      # LE CORPUS QU'ELLE LIT EST CELUI DES FICHIERS SUIVIS, délibérément : un document hors index
      # n'existe pas pour elle comme il n'existe pas pour le lecteur du dépôt.
      - name: Ce qu'un exploitant doit pouvoir trouver est documenté — onglets, capteurs, modes et leviers DÉRIVÉS des sources, cliquet sur ce qui n'est pas couvert (P9.7-b)
        run: python3 .github/scripts/check_operator_surface_is_documented.py

      # `P11.21-c` — ET LE NOM QU'UN DOCUMENT DONNE À UN ONGLET EST CELUI QUE LA CONSOLE SERT. Le pas
      # précédent exige qu'un onglet soit DÉCRIT ; celui-ci exige qu'il soit BIEN NOMMÉ. La distinction
      # n'est pas rhétorique : depuis `P11.18-o` (2026-08-25), un libellé d'onglet n'est plus ÉCRIT dans
      # le module — il est DÉRIVÉ du titre du panneau que l'onglet ouvre, ou du lien de barre latérale
      # quand l'espace n'a qu'un onglet. La console a donc cessé d'avoir un endroit où un nom se recopie ;
      # la DOCUMENTATION en avait un, une colonne de tableau tenue à la main, et elle a cessé d'être tenue
      # LE JOUR MÊME sans que rien ne le dise. MESURÉ SUR L'ARBRE SUIVI le 2026-08-30 : 24 des 37 lignes
      # documentées nommaient un onglet autrement que l'écran — et deux avaient déjà été corrigées à la
      # main la veille, c'est-à-dire que le remède manuel avait été tenté et laissait 24 lignes derrière.
      # LES TROIS SOURCES SONT DÉRIVÉES PAR LEUR PROPRIÉTÉ : le modèle est l'unique fichier suivi de
      # `web/` qui porte `const SPACES = [` ; le document servi, l'unique `.html` suivi qui porte la barre
      # latérale des espaces ; et les LIGNES à juger sont celles dont la première cellule est un
      # identifiant d'onglet DÉCLARÉ PAR LE MODÈLE — la population vient du code, jamais d'un nom de
      # colonne. Aucune des trois ne peut être renommée ou déplacée sans que la garde la retrouve.
      # ELLE REFUSE DE CONCLURE (code 2) PLUTÔT QUE DE DEVINER, et c'est le cœur du geste : réécrire une
      # ligne avec un instrument validé À MOITIÉ remplacerait une documentation PÉRIMÉE par une
      # documentation FAUSSE — la première se soupçonne, la seconde se croit. Sept cas de refus, chacun
      # NOMMÉ : ancre absente ou dédoublée ; modèle lu en corpus DÉGÉNÉRÉ (c'est la panne exacte qu'une
      # autre garde a subie le 2026-08-25, tombée de 37 onglets à 2 quand les libellés écrits ont été
      # ôtés) ; libellé porté par un identifiant non déclaré ; section déclarée absente du document ;
      # repli sur un lien de barre latérale inexistant ; nom dérivé qui serait l'AVEU que la console ne
      # sait pas nommer la destination ; titre portant un commentaire en enfant direct, que le code
      # plierait dans le nom et qu'un lecteur ne verrait pas.
      # L'INSTRUMENT S'AUTO-VALIDE SUR DES ENTRÉES FABRIQUÉES DANS LA GARDE, jamais sur l'état du dépôt :
      # une borne posée sur l'arbre rougirait le jour où le travail est fini, ce qui est une rançon et
      # non une garde. Les quatre chemins de la dérivation, les six refus et la comparaison dans LES DEUX
      # SENS (documentation périmée / page renommée) y sont exercés.
      - name: Le nom qu'un document donne à un onglet est celui que la console SERT — dérivé du titre du panneau, refus de conclure plutôt que devinette (P11.21-c)
        run: python3 .github/scripts/check_a_documented_tab_label_is_the_name_the_console_serves.py
      # `P11.21-d` — UN CHEMIN DE CONSOLE CITÉ EN PROSE MÈNE LÀ OÙ IL DIT. La garde ci-dessus juge une
      # COLONNE de tableau, à position fixe : une citation en PROSE lui échappe PAR CONSTRUCTION, et elle
      # le dit à chaque exécution. Celle-ci prend l'autre moitié. Mesuré le 2026-08-30 : deux citations
      # rangeaient un onglet sous le mauvais espace ET le nommaient par un titre que la console ne sert
      # plus. LE CRITÈRE EST DÉRIVÉ ET IL LE FAUT : un nom PÉRIMÉ ne s'énumère pas — on ne cherche pas ce
      # qu'on ne connaît plus — donc c'est l'autre moitié du couple, celle qui est ENCORE JUSTE, qui
      # déclare la chaîne. Le corpus est lu comme du TEXTE CONTINU et non ligne à ligne : une citation
      # coupée en deux avait échappé au balayage précédent, et c'est exactement la cinquième. La source
      # est CELLE de la garde ci-dessus, importée et non réimplémentée — ce dépôt porte une clé ouverte
      # dont le défaut est « trois lectures d'une même liste, et aucune ne fait foi ». Ce qu'elle ne sait
      # pas trancher, elle le PUBLIE au lieu de le deviner.
      - { name: "Un chemin de console cité en prose mène là où il dit (P11.21-d)", run: python3 .github/scripts/check_a_console_path_cited_in_prose_leads_where_it_says.py }

      # `P7.20-i` — UN CONTRÔLE CITÉ EXISTE DANS CET ARBRE. La garde ci-dessus juge où MÈNE un chemin de
      # console ; celle-ci juge qu'un chemin de CONTRÔLE existe tout court. Mesuré le 2026-08-30 : la
      # recette conteneur et le document de reprise renvoyaient chacun à une garde de capacités comme au
      # contrôle de référence — DEUX NOMS DIFFÉRENTS, tous deux absents, et les deux répertoires qui les
      # porteraient absents aussi. Ce dépôt est PUBLIC : le lecteur extérieur croyait qu'une garde existait
      # là où il n'y en avait aucune, et rien ne le lui disait. L'assertion porte sur l'INDEX et jamais sur
      # le disque — un fichier présent chez l'auteur et non suivi est ABSENT pour ce lecteur, et c'est un
      # piège déjà payé ici (2026-08-22). LE DOMAINE EST RESTREINT AUX EXÉCUTABLES ET AUX FLUX PARCE QUE
      # C'EST MESURÉ : l'appartenance nue de TOUS les chemins cités accuserait 239 fois, 45 fois même après
      # résolution ; restreint à ce qu'un lecteur est invité à JOUER, le compte est 133 cités / 0 absent, et
      # un cliquet à zéro est un cliquet qu'on tient. ÉPROUVÉE PAR MUTATION contre l'arbre `378f29e~1`,
      # AVANT le correctif manuel : elle rougit en nommant le renvoi mort de docs/DR-plume-restore.md. Le
      # nom mort n'est PAS recopié ici, et ce n'est pas de la coquetterie : au premier jet ce bloc le citait
      # entre accents graves, et la garde a rougi sur CE fichier en désignant cette ligne — démentir une
      # phrase fausse en la reproduisant la réintroduit. On nomme le SITE. Elle n'attrape QU'UN des deux
      # noms morts, et le dit dans son propre en-tête : le second était écrit entre PARENTHÈSES et non
      # entre accents graves. Élargir aux parenthèses a été mesuré — deux fausses accusations pour une
      # vraie prise sur la tête — et REFUSÉ. Elle REFUSE DE CONCLURE (sortie 2) plutôt que de verdir en
      # étant aveugle : racine non dérivable, index vide, corpus vide, ou auto-validation démentie.
      - { name: "Un contrôle cité existe dans ce dépôt (P7.20-i)", run: python3 .github/scripts/check_a_cited_control_exists_in_this_repo.py }

      # `P9.4-b` + `P9.6-a` — UN DÉPLOIEMENT N'ARME JAMAIS UNE TÂCHE DONT UNE PRÉCONDITION MANQUE CHEZ
      # LUI. Cette garde était ÉCRITE et volontairement NON CÂBLÉE : elle sortait en refus, et c'était son
      # verdict JUSTE — les deux déploiements de référence armaient l'ordonnanceur de sauvegarde toutes
      # les 6 h en livrant la clé de base VIDE, si bien qu'aucune archive n'était jamais écrite pendant
      # qu'une rétention était annoncée en toutes lettres à côté, et que l'échec était MUET. Son câblage
      # devait atterrir AVEC la décision, pas avant : c'est `P9.6-a` qui satisfait la précondition sur un
      # déploiement NEUF — une base absente engendre sa clé, la base naît chiffrée, le cycle a de quoi
      # produire. RIEN N'EST ÉNUMÉRÉ : les tâches armées sont dérivées de la FORME du gate dans le démon,
      # les préconditions du point fixe des refus atteignables, les équivalences de variables des TABLEAUX
      # DE CONSTANTES du démon, et les déploiements de tout YAML du dépôt qui arme. Chaque lecture est
      # exercée dans les DEUX sens avant tout verdict, et la garde REFUSE DE CONCLURE (sortie 2) sous ses
      # planchers plutôt que de rendre vert en étant aveugle. Éprouvée par mutation le 2026-08-25 :
      # commenter la précondition du manifeste la fait sortir en refus en NOMMANT le fichier.
      # CE QU'ELLE NE TIENT PAS, et qui est écrit dans son verdict : la justesse d'une valeur non vide,
      # l'aboutissement réel du cycle — dit à l'exécution par le signal non purgeable — et ce qu'un
      # fichier d'environnement d'exploitant ajouterait hors du dépôt.
      - name: Un déploiement n'arme jamais une tâche dont une précondition n'est pas satisfaite chez lui (P9.4-b)
        run: python3 .github/scripts/check_a_deployment_never_arms_a_task_it_cannot_run.py

      # `P8.9-c` — MÊME FAMILLE, APPLIQUÉE À L'INDEX DES CLÉS : ici ce n'est ni la couverture ni le
      # document qui manque, c'est ce que la ligne dit d'ELLE-MÊME. Une clé porte un état, et ✅
      # promet qu'il n'y a plus rien à faire. MESURÉ le 2026-08-23, avant réparation : sur 155 lignes
      # ✅ de `docs/ROADMAP.md`, 20 portaient dans leur propre cellule un énoncé « Reste : » /
      # « Reste ouvert : » / « Résidu nommé : », et 17 de ces énoncés n'étaient ni qualifiés
      # d'assumés, ni renvoyés à une clé, ni déclarés fermés. Le document SAVAIT le travail
      # incomplet et l'affichait comme complet ; qui lisait « ce qui est ouvert » ne voyait pas ces
      # dix-sept-là. La garde n'énumère AUCUNE clé — une garde qui nommerait `P3.2-a` serait morte au
      # prochain ajout : elle reconnaît une FORME que le document se donne à lui-même, le substantif
      # en tête d'énoncé, et laisse passer le verbe conjugué en pleine phrase (« le dépôt reste
      # détecté »), qui la noierait sous les faux positifs. Sous ✅, un tel énoncé doit porter un
      # RENVOI vers une autre clé, une QUALIFICATION (assumé, délibéré, à dessein, hors périmètre,
      # hors de portée), ou une CLÔTURE en majuscules — laquelle ferme aussi les restes qui la
      # précèdent, car ces cellules sont écrites dans l'ordre du temps. Elle porte ses deux témoins
      # sur un corpus fabriqué, refuse de conclure sous ses planchers, et échoue si le vocabulaire du
      # reste disparaît du document plutôt que de rester verte en ne gardant plus rien. Ce qu'elle ne
      # tient pas est écrit : la seule famille lexicale du reste, et aucun dépôt voisin.
      - name: Un reste sous une clé fermée appartient à une clé ouverte, ou se déclare
        run: python3 .github/scripts/check_every_residue_belongs_to_an_open_key.py

      # `P10.5-g` — UN REMÈDE MACHINE PUBLIÉ PAR LE DÉMON EST LU PAR LA CONSOLE. Mesuré le 2026-08-28 :
      # les trois refus de curseur froid publiaient `restart_without_cursor` — une CONDUITE, pas une
      # phrase — et AUCUN module de `web/` ne le lisait. Le message s'affichait, la table des curseurs
      # gardait le curseur mort, « Suivant » le rejouait indéfiniment. La population est DÉRIVÉE de la
      # forme que le démon s'est donnée (un corps qui porte `"reason"`, un champ à `true` dedans) et le
      # plafond est ZÉRO : un champ de conduite ajouté sans être branché fait rougir sans qu'aucune liste
      # ne soit tenue à côté. Elle refuse de conclure sous son plancher. Ce qu'elle ne tient pas est écrit
      # dans son en-tête : que le remède soit OBÉI, une autre forme que le booléen nu, un client hors web/.
      - name: Un remède machine publié dans un refus est lu par la console
        run: python3 .github/scripts/check_a_machine_remedy_is_read_by_the_console.py

      # `P8.9-d` — LA FORME DU TABLEAU, LÀ OÙ LES TROIS GARDES VOISINES NE LISENT QUE SON TEXTE. Mesuré
      # le 2026-08-25 : QUINZE lignes de l'index ne se terminaient pas par la barre qui clôt une cellule,
      # et SOIXANTE-DIX lignes réparties sur trois documents portaient plus de cellules que leur tableau
      # n'en déclare — le rendu IGNORE l'excédent. Sur l'index seul, plus de douze mille caractères
      # n'atteignaient pas la page, dont la clôture entière de TROIS clés fermées, que le lecteur voyait
      # donc comme inachevées. Cause unique et vérifiée sur les 70 : une barre verticale dans le texte
      # d'une cellule, que les accents graves ne protègent PAS. Aucune garde ne le voyait : celle des
      # restes, celle des chiffres publiés et celle des collisions de clés lisent le TEXTE des cellules.
      # LE CRITÈRE EST DÉRIVÉ DU DOCUMENT LUI-MÊME : la LIGNE DE DÉLIMITATION d'un tableau est la
      # déclaration que ce tableau fait de sa propre forme — nombre de colonnes, style de barres — et
      # toute ligne de son corps doit la porter. Rien n'est écrit à la main, ni « quatre cellules » ni un
      # nom de fichier (`P11.13-d`). Le corpus est aussi une PROPRIÉTÉ : tout document Markdown qui
      # DÉCLARE un tableau, suivi ou seulement présent, pour qu'un document neuf soit jugé AVANT d'être
      # indexé et non le jour où il est publié. Elle refuse de conclure (code 2) sous ses planchers ou si
      # son découpeur ne se reconnaît plus, et dit ce qu'elle ne tient pas : la forme seulement, jamais le
      # texte, et pas les tableaux montrés dans un bloc de code, qui sont des échantillons.
      - name: Une ligne de tableau porte la forme que son en-tête déclare (P8.9-d)
        run: python3 .github/scripts/check_every_table_row_carries_the_shape_its_header_declares.py

      # `P8.9-g` — UNE CLÉ NEUVE S'AJOUTE À LA FIN DU DOCUMENT, QUI N'EST PAS LA FIN DE SA PHASE. Mesuré
      # le 2026-08-26 : vingt clés sur deux cent quatre-vingts se lisaient sous le titre de la console,
      # dont neuf d'une phase qui n'avait AUCUNE section. Un lecteur cherchant la phase ne trouvait rien,
      # un lecteur lisant la console y trouvait de la sauvegarde. La garde DÉRIVE la section attendue de
      # la clé elle-même — aucune phase n'est énumérée ici, en ajouter une n'oblige à rien toucher — et
      # elle refuse de conclure si son propre instrument échoue à ses témoins ou si le corpus est vide.
      - name: Une clé se lit sous le titre de sa phase (P8.9-g)
        run: python3 .github/scripts/check_every_key_sits_under_the_section_of_its_phase.py

      # `P8.9-h` — UNE CELLULE PORTAIT DEUX MARQUEURS D'ÉTAT COLLÉS, valeur que la légende ne déclare
      # pas. Elle échappait à tout relevé qui lit la colonne — c'est un compte faux qui l'a révélée —
      # et l'un des deux marqueurs contredisait le texte de sa propre cellule. Les états admis sont
      # DÉRIVÉS de la table de légende du document : en ajouter un n'oblige à rien toucher ici, et un
      # document qui porte des clés sans légende fait REFUSER DE CONCLURE plutôt que passer au vert.
      - name: L'état d'une clé est un de ceux que la légende déclare (P8.9-h)
        run: python3 .github/scripts/check_a_key_state_is_one_the_legend_declares.py

      # `P8.9-i` — UNE GARDE ÉCRITE MAIS NON CÂBLÉE NE REFUSE RIEN, et son verdict se lit pourtant comme
      # une garantie. Mesuré le 2026-08-26 : deux gardes neuves vivaient dans `.github/scripts/` sans
      # qu'aucun pas ne les exécute ; le mécanisme qu'elles protégeaient pouvait disparaître sans qu'un
      # seul vert ne bouge. Le corpus est le RÉPERTOIRE et non l'index du dépôt — une garde neuve existe
      # sur le disque avant d'être suivie, et c'est là qu'on oublie de la câbler.
      - name: Une garde écrite est une garde câblée (P8.9-i)
        run: python3 .github/scripts/check_every_guard_written_is_a_guard_wired.py

      # `P8.9-j` — UNE CLÉ CITÉE PAR LE CODE MAIS ABSENTE DE L'INDEX désigne un travail que rien ne suit.
      # Mesuré le 2026-08-26 : quatre clés vivaient dans des modules de la console, dans deux gardes et
      # dans l'intégration continue sans exister dans la feuille de route — dont une, citée quinze fois
      # avec son propre fichier de témoins, qui était FERMÉE depuis le 9 août. Le contrôle des restes ne
      # lit que l'index : il ne pouvait pas les voir. La borne des clés FACTICES des témoins est dérivée
      # de l'index lui-même — une phase que l'index ne titre pas est une phase fabriquée.
      - name: Une clé citée par le code existe dans l'index (P8.9-j)
        run: python3 .github/scripts/check_every_key_cited_in_the_code_exists_in_the_index.py

      # `P8.9-k` — UN INSTRUMENT QUI PORTE LE CHEMIN ABSOLU DE LA MACHINE OÙ IL A ÉTÉ ÉCRIT rend deux
      # verdicts également faux : vert chez son auteur, où le chemin EXISTE et où il balaie le bon
      # arbre sans rien trouver ; jeté partout ailleurs. Mesuré le 2026-08-30, en fermant un rouge
      # d'intégration. Le savoir n'était pas absent, il était MAL PLACÉ : ce dépôt refusait déjà
      # `/home/<compte>` dans un MESSAGE DE COMMIT — où il n'est qu'un bruit de lecture — et nulle
      # part dans les instruments, où le même littéral change un VERDICT.
      # LE BALAYAGE NAÏF EST INUTILISABLE : il rend deux occurrences, toutes deux LÉGITIMES (un motif
      # du système surveillé, et la règle qui refuse ces chemins dans les messages). Le critère est
      # donc le SEGMENT DE COMPTE — concret contre motif — et non le fichier ni la ligne. Les deux
      # resserrements évidents ont été essayés et ÉCARTÉS, mesure à l'appui, dans l'en-tête du script :
      # borner aux instruments exécutables n'écarte PAS le second cas et aveugle 42 instruments sur 56 ;
      # dépouiller les commentaires n'acquitte rien de plus et rétrécit le canal.
      - name: Aucun instrument ne porte le chemin de la machine de son auteur (P8.9-k)
        run: python3 .github/scripts/check_no_instrument_hardcodes_an_author_machine_path.py

      # `P11.19-a` — CE QU'UN CAPTEUR ÉMET EST DÉCLARÉ, et le mot étranger est refusé à l'émission.
      - name: Un producteur déclare les valeurs qu'il émet (P11.19-a)
        run: python3 .github/scripts/check_a_producer_declares_the_values_it_emits.py

      # `P11.18-z` — LA RECHERCHE D'UNE LISTE SURVIT AU RENDU QUI DÉTRUIT SON CHAMP. La garde tient que
      # chaque liste cherchable déclare l'identité stable sans laquelle la mémoire ne peut pas s'armer :
      # sans elle, le mécanisme est posé mais la vue se comporte comme avant, en silence.
      - name: Une liste cherchable se souvient de sa recherche (P11.18-z)
        run: python3 .github/scripts/check_every_searchable_list_remembers_its_search.py

      # `P8.9-f` — L'INDEX ET LES COMMITS POUVAIENT SE CONTREDIRE SANS QUE RIEN NE LE DISE. Deux fois
      # le 2026-08-25, des clés commitées, déployées et VÉRIFIÉES EN PRODUCTION figuraient encore
      # parmi les clés OUVERTES : qui lit « ce qui est ouvert » ne voyait pas ce qui est fait. Cette
      # garde compare ce qu'un commit AFFIRME fermer à ce que l'index DÉCLARE, sans énumérer ni clé ni
      # fichier — le vocabulaire de fermeture est DÉRIVÉ des déclarations datées de l'index, et une
      # famille impure est REJETÉE (« déployé » n'est pas « fermé »). La fermeture doit SUIVRE le
      # groupe de clés : le critère « même ligne » accuserait six clés ouvertes à tort.
      # ELLE NE FERME PAS LA CLÉ QUI LA FONDE, ET ELLE LE DIT LA PREMIÈRE : rejouée sur l'arbre
      # d'avant chacun des deux incidents, elle rend ZÉRO les deux fois. Deux causes MESURÉES, dont
      # une qui vaut au-delà d'ici : un lexique dérivé de l'usage n'admet une famille qu'après
      # plusieurs déclarations déjà posées, donc **la PREMIÈRE fermeture écrite avec un mot neuf est
      # toujours manquée**. Les deux remèdes évidents ont été RÉFUTÉS par la mesure — dériver aussi du
      # vocabulaire des commits ajoute vingt familles dont dix-huit ne ferment rien et deux
      # accusations FAUSSES pour zéro légitime ; exiger la convention en amont refuserait
      # quatre-vingt-neuf commits de fermeture sur cent-dix, et ne dirait rien d'une bascule ABSENTE,
      # qui est justement le défaut. Elle refuse de conclure (code 2) sous ses planchers.
      - name: L'index concorde avec ce qu'un commit déclare fermé — posée sous P8.9-f, qu'elle ne ferme pas
        run: python3 .github/scripts/check_the_index_agrees_with_what_a_commit_closes.py

      # MÊME FAMILLE, APPLIQUÉE AU PLAN DES MODULES : un document que rien ne relit dérive. Le plan
      # (`docs/MODULE-MAP.md`) nommait encore `idp.rs` et `compliance.rs` alors que l'un était devenu
      # un répertoire et l'autre n'avait jamais vécu qu'en handler, et un module de premier niveau sur
      # trois n'y avait aucune entrée (mesuré le 2026-08-22 : 2 chemins absents, 26 modules sans
      # entrée sur 79). La garde n'énumère aucun répertoire : elle lit la ligne `Scope:` du plan, puis
      # relit dans les DEUX sens — tout chemin cité en première cellule d'un tableau du périmètre
      # existe dans l'arbre SUIVI, et tout module de premier niveau du périmètre a une entrée. Elle
      # porte ses témoins, un plancher d'entrées, et refuse de conclure si `git ls-files` se tait.
      # LE PÉRIMÈTRE COUVRE AUSSI LA CONSOLE. Le plan ne tenait que le démon ; `web/`, la moitié du
      # produit et le tronc d'où sont extraits ses modules, n'était relu par rien. Mesuré sur l'arbre
      # suivi le 2026-08-23 : 47 modules `web/*.js`, zéro cité dans le plan. Chaque poste du périmètre
      # DÉCLARE désormais l'extension qui y fait un module (`` `daemon/src/` (`*.rs`), `web/` (`*.js`)
      # ``) : ce qu'est un module dépend du langage de l'arbre, et une garde qui supposait `.rs` ne
      # pouvait tenir que la moitié du produit. Le corpus témoin porte les deux langages.
      - name: Le plan des modules dit vrai sur l'arbre suivi, dans les deux sens — démon ET console (P7.18-a)
        run: python3 .github/scripts/check_module_map_matches_tree.py

      # MÊME FAMILLE, APPLIQUÉE À LA PROMESSE CENTRALE DU PRODUIT : une garantie AFFIRMÉE que le code
      # ne tient que par convention. La livraison au moins une fois repose sur « écrire un temporaire
      # puis renommer ». Ce motif donne l'ATOMICITÉ DU CONTENU et rien d'autre : après une coupure,
      # l'entrée de répertoire peut manquer alors que le fichier existe — les octets sont là, leur NOM
      # n'y est pas, et `ship.sh` parcourt le spool PAR NOM. MESURÉ le 2026-08-20 : aucune
      # synchronisation nulle part, et le motif recopié à la main dans SEPT capteurs en plus de la
      # bibliothèque. Coût de la correction, mesuré le même jour (NVMe, 100 publications) : +12,7 ms
      # par publication en shell — payable pour un capteur cadencé à la minute, et dit comme tel.
      # La garde n'énumère aucun capteur : elle refuse toute publication dans `$SPOOL` écrite hors de
      # `spool_write`/`spool_publish_file`, puis SOURCE la bibliothèque avec un `sync` instrumenté et
      # vérifie l'ORDRE réel (contenu AVANT le renommage, répertoire APRÈS). Elle dit aussi ce qu'elle
      # ne prouve pas : que les appels sont faits, jamais la survie à une coupure d'alimentation.
      - name: Une publication dans le spool doit être DURABLE, pas seulement atomique
        run: python3 .github/scripts/check_publication_is_durable.py

      # LA MÊME FAMILLE, UN CRAN PLUS TÔT : ce qui est publié est durable ; encore faut-il que le
      # produire ne coûte pas DEUX FOIS. MESURÉ le 2026-08-30 (`P6.9-c`) : les cinq surfaces qui
      # décodent un lot avant de le publier (OTLP traces, HEC, audit MinIO, Firehose, Pub/Sub)
      # bâtissaient leur enveloppe par la macro de sérialisation de `serde_json`, dont le bras de repli
      # EMPRUNTE son opérande et le rend par `to_value` — l'arbre du lot était donc RECOPIÉ EN
      # PROFONDEUR, et l'original, jamais consommé, restait vivant à côté de sa copie.
      # CE QUI REND LE DÉFAUT COÛTEUX EST L'INSTANT, PAS LA TAILLE : la recopie tombe là où l'empreinte
      # de la requête est maximale — le corps décompressé et l'arbre décodé sont encore vivants (vérifié
      # par une sonde de compilation qui les LIT après le point de suspension de la publication), donc
      # le second exemplaire s'AJOUTE au pic au lieu de le remplacer. Sur un produit qui doit tenir dans
      # 2 Gio, c'est ce qui compte. Gain mesuré au point de publication (allocateur comptant les octets
      # vifs, corps OTLP synthétique) : le déplacement vaut EXACTEMENT la taille du lot, soit 8,2 % à
      # 25,3 % de l'empreinte selon la forme du corps, et la restitution du lot après sérialisation
      # double ces parts. C'est la propriété qui se transporte, pas ces nombres.
      # La garde n'énumère AUCUN nom de fichier : sa population est DÉRIVÉE du critère « ce fichier
      # appelle le point de publication du spool », de sorte qu'un récepteur poussé neuf y entre le jour
      # où il publie. Si ce critère cesse de désigner quoi que ce soit, elle REFUSE DE CONCLURE au lieu
      # d'afficher un vert vide. Elle s'auto-valide sur dix entrées FABRIQUÉES dans son propre corps
      # (forme visée, trois formes recopiantes, tableau littéral, champ homonyme hors enveloppe, même
      # défaut hors population, défaut présent seulement en commentaire, chaîne contenant deux barres).
      # Elle dit ce qu'elle ne tient pas : elle lit la FORME de la source, jamais une allocation. La
      # propriété elle-même est épinglée côté Rust par l'identité d'ADRESSE du lot entre l'avant et
      # l'enveloppe (`l_enveloppe_deplace_son_lot_et_rend_les_memes_octets`).
      - name: Une enveloppe d'ingestion doit DÉPLACER son lot, jamais le recopier (P6.9-c)
        run: python3 .github/scripts/check_an_ingestion_envelope_never_copies_its_batch.py

      # LE DÉFAUT VOISIN, ET PLUS GRAVE : la garde ci-dessus rend DURABLE ce qui est publié ; celle-ci
      # refuse qu'un capteur ACQUITTE ce qu'il n'a pas encore publié. Un capteur incrémental marque sa
      # progression (filigrane, offset, curseur, repère daté, registre « déjà signalé », base de
      # référence) ; marquer AVANT de publier rend le marqueur menteur — une coupure entre les deux
      # perd les événements, et rien ne le signale, puisque le marqueur affirme qu'ils sont acquittés.
      # Aucune synchronisation n'y remédie : c'est l'ORDRE. `S27` annonçait CINQ capteurs fautifs ; le
      # compte venait d'un `grep state_write`, pas du motif. Recherche refaite sur le MOTIF le
      # 2026-08-20 : DIX-SEPT — les douze manquants marquaient par redirection brute, par `touch` d'un
      # repère relu en `find -newer`, par ajout dans un registre, par renommage d'une base de
      # référence, ou via un point de reprise avancé par un outil externe au moment de la lecture.
      # La garde n'énumère aucun capteur : elle refuse toute écriture directe de marqueur hors de la
      # bibliothèque (un capteur MET EN ATTENTE, il n'a pas le geste d'écrire), puis SOURCE la
      # bibliothèque et vérifie l'ORDRE réel, et enfin SIMULE une coupure entre les deux gestes :
      # l'enveloppe publiée doit être là et le marqueur absent — donc rejeu, jamais perte. Elle dit
      # aussi ce qu'elle ne prouve pas : que le rejeu soit sans conséquence (sept capteurs n'ont
      # aucune clé de dédoublonnage, leurs doublons seront VISIBLES) ni la survie à une coupure
      # d'alimentation.
      - name: Un filigrane doit SUIVRE la publication qu'il acquitte, jamais la précéder
        run: python3 .github/scripts/check_watermark_follows_publication.py

      # LA MOITIÉ MANQUANTE DE LA GARDE PRÉCÉDENTE. Publier avant d'acquitter transforme une perte
      # SILENCIEUSE en rejeu VISIBLE — c'est un progrès, et il est incomplet : le central n'absorbe ce
      # rejeu que si l'événement PORTE de quoi être reconnu. Le mécanisme est unique et il se dérive :
      # `event.dedup` porte un index UNIQUE, toute écriture est un `INSERT OR IGNORE`, la clé est
      # cloisonnée par l'hôte au point d'écriture, et une clé ABSENTE vaut NULL — or SQLite tient deux
      # NULL pour DISTINCTS, donc sans clé il n'y a pas de dédoublonnage du tout. Le décompte annoncé
      # (six / quatre / sept) portait sur DIX-SEPT capteurs parce qu'il avait hérité de la liste de
      # `S30`, celle des capteurs dont l'ORDRE était fautif ; la population qui REJOUE en compte
      # VINGT-DEUX (mesuré le 2026-08-21) — `web`, `mail`, `dataaccess` et `resources` avaient déjà le
      # bon ordre et rejouent tout autant. La garde n'énumère aucun capteur : elle refuse tout objet
      # événement sans clé chez un capteur qui publie-puis-acquitte, puis LANCE VRAIMENT sept capteurs
      # avec une coupure simulée juste après la publication et exige trois choses — la tranche
      # republiée rend les MÊMES clés, des enregistrements DIFFÉRENTS n'en partagent aucune (sans ce
      # témoin-là, une clé constante passerait le premier en effaçant toutes les données), et un
      # enregistrement neuf ne réemploie jamais une clé déjà vue. L'horloge est décalée entre les
      # passes, faute de quoi elles tomberaient dans la même seconde et une clé bâtie sur l'instant de
      # publication paraîtrait stable.
      - name: Un rejeu doit être ABSORBÉ par le central, pas seulement visible
        run: python3 .github/scripts/check_replay_is_absorbable.py

      # LA MÊME FAMILLE, PAR LA PORTE DE SORTIE — et c'est un défaut de CORRECTION, pas un oubli.
      # `S30` a posé « publier d'abord, acquitter ensuite » en s'appuyant sur une hypothèse ÉCRITE :
      # « sortir par la porte propre veut dire qu'aucune enveloppe n'a été publiée, donc rien n'est
      # acquitté ». Vraie pour un curseur CALCULÉ À PARTIR de ce qui a été lu (il n'existe pas tant
      # que la lecture n'a pas abouti) ; FAUSSE pour un marqueur qui ne doit rien à la lecture — un
      # offset pris sur la TAILLE du fichier, un repère daté, l'instant du passage. La lecture échoue,
      # le capteur n'a effectivement rien à signaler, il sort par la porte propre, et cette sortie
      # ACQUITTE une tranche jamais publiée : la perte silencieuse que `S30` fermait, revenue par la
      # sortie. Ce qui la rend invisible : le mot d'une lecture ratée et celui d'une source calme sont
      # le MÊME mot — la chaîne vide — et un `| grep` derrière la lecture remplace son code de retour
      # par le sien, pour qui « rien trouvé » vaut 1, c'est-à-dire le cas NORMAL.
      # MESURÉ le 2026-08-21 sur la PROPRIÉTÉ, pas sur un symbole : 22 capteurs mettent un marqueur en
      # attente ; NEUF pouvaient acquitter une lecture ratée par la sortie propre (falco, suricata,
      # auditd, kube-audit, audit, clamav, yara, cloudflare-http, integrity) et QUATRE par une
      # publication inconditionnelle (ufw, portscan, origin-drop, pod-logs).
      # La garde n'énumère aucun capteur : elle découvre la population sur le motif, exige de chacun
      # l'une des DEUX preuves (nommer le chemin d'échec de lecture, ou ne mettre en attente que sur
      # le chemin qui publie), puis LANCE de vrais capteurs avec la lecture relayée pour qu'elle
      # échoue. Deux témoins en sens inverse, et le second est le cœur : lecture en échec -> marqueur
      # figé, aveu émis, tranche rejouée ; source LUE sans rien à signaler -> marqueur écrit et aucun
      # aveu, sans quoi on aurait troqué une perte silencieuse contre un rejeu permanent.
      - name: Une lecture qui ÉCHOUE ne doit rien acquitter
        run: python3 .github/scripts/check_read_failure_is_not_acknowledged.py

      # `P10.7-g` — LA MÊME FAMILLE, CÔTÉ ROUTE : la lecture a bien eu lieu ou non, et le corps SERVI
      # doit le dire. Le démon a DEUX voies pour lire des lignes derrière une route — la lecture gardée
      # par chien de garde et l'exécution de requête — et les deux peuvent échouer : connexion de
      # lecture indisponible, budget de 5 s épuisé sous charge, table absente, colonne refusée par
      # l'authorizer. Ce qui partait alors avait la FORME d'un résultat (`controls: []`, `totals: {}`,
      # `rows: []`, `total: 0`) : indiscernable d'une mesure réellement à zéro, et sur une route de
      # conformité cela se lit « aucun contrôle en échec » — la valeur la plus rassurante, servie
      # précisément quand rien n'a été mesuré.
      # `P10.7-e` a fermé DEUX routes le 2026-08-30 et a écrit lui-même ce qui le condamnerait : « son
      # correctif est honnête sur une route et muet ailleurs ». Cette garde est la dérivation qui
      # manquait — elle ne nomme AUCUNE route.
      # LES DEUX VOIES NE SONT PAS SYMÉTRIQUES, et c'est le cœur : mesuré le 2026-08-30, la valeur par
      # DÉFAUT n'est rendue que lorsque la connexion n'a pas pu s'ouvrir — un chemin que la concurrence
      # ne déclenche pas ; quand la garde de budget INTERROMPT, la closure est déjà en cours et c'est SA
      # valeur qui remonte. Habiller le seul défaut rend donc honnête sur le chemin RARE en laissant
      # muet celui que la CHARGE déclenche. D'où trois jambes — le défaut qui s'écoule vers une réponse,
      # la closure qui avale une lecture de lignes, le bras d'erreur d'une exécution de requête — et un
      # REFUS EXPLICITE que la seconde soit satisfaite par la première : l'aveu de la jambe B est
      # cherché dans la région de la CLOSURE seulement, la coupure étant structurelle (l'index du
      # troisième argument) et non une convention.
      # La population est DÉCOUVERTE dans `daemon/src/handlers/` sur le texte DÉPOUILLÉ de ses
      # commentaires — une occurrence citée en commentaire existe sur l'arbre et n'est jamais comptée.
      # Sept mutants fabriqués la valident dans les deux sens à chaque exécution, dont celui qui compte :
      # un défaut qui AVOUE au-dessus d'une closure qui AVALE reste accusé. Le PLANCHER porte sur la
      # POPULATION (sous lui, code 2 : refus de conclure, distinct de la violation) et les CLIQUETS sur
      # les accusations — ils ne montent jamais, et ZÉRO est une valeur atteignable : un témoin qui
      # exigerait que le défaut survive serait une rançon.
      - name: Une lecture qui n'a pas eu lieu n'est jamais servie comme un fait (P10.7-g)
        run: python3 .github/scripts/check_a_read_that_did_not_happen_is_never_served_as_a_fact.py

      # `P4.1-q` — LA MÊME FAMILLE, MAIS RIEN N'EST PUBLIÉ DU TOUT. `S32`/`S33`/`S36` ferment le cas
      # où un chiffre RASSURANT part quand même. Ici la détection s'ÉTEINT : un repli d'erreur du
      # parcours qui pose les watches d'intégrité abandonne la branche courante, un SOUS-ARBRE ENTIER
      # sort de la surveillance — aucun événement, aucun avertissement, aucun aveu — et `fim_mode`
      # continue d'annoncer « realtime » pendant que la couverture annoncée reste celle de la
      # configuration. Le même défaut existe du côté de la SURFACE : la commande d'état enveloppait
      # ses lignes dans des `if let Ok(…)` sans `else`, si bien qu'une configuration illisible faisait
      # DISPARAÎTRE la profondeur de file au lieu de la dire inconnue — et une ligne absente se lit
      # comme une absence de problème.
      # CE QUI REND LE DÉFAUT INVISIBLE À LA RELECTURE : la forme fautive est la forme IDIOMATIQUE de
      # Rust — `Err(_) => continue`, `if let Ok(x) = …` sans `else`, `entries.flatten()`. Aucune de
      # ces lignes ne ressemble à une perte.
      # MESURÉ le 2026-08-21 sur la PROPRIÉTÉ (une branche d'échec qui ABANDONNE sans laisser de
      # trace), pas sur un symbole : 8 fonctions posent ou énumèrent de la surveillance, 5 écrivent la
      # surface d'état, et SEIZE sites étaient muets — onze dans le backend Linux, trois grandeurs
      # effacées dans la commande d'état, deux dans le backend Windows. Le constat d'origine en
      # annonçait TROIS et ne voyait pas Windows : il est réfuté par cette mesure. Hors du crate
      # `agent`, le même critère en mesure 32 de plus : ils ne sont pas exigés ici, et c'est écrit.
      # La garde n'énumère ni fichier ni symbole : la population de couverture est dérivée du CONTRAT
      # `FimBackend` (la méthode qui POSE) et de l'énumération de répertoire, celle de la surface de
      # l'écriture sur la sortie standard. Elle valide son propre instrument sur sept témoins en SENS
      # INVERSE — dont la pose DÉGÉNÉRÉE qui ne surveille plus rien, et un scrutin sous bloc `unsafe`
      # qu'une lecture naïve des accolades ne voit pas.
      - name: Une couverture qu'on n'a pas pu poser ne s'éteint pas en silence
        run: python3 .github/scripts/check_coverage_loss_is_never_silent.py

      # DU CODE LIVRÉ QU'AUCUN JOB NE COMPILE (`P8.24-a`). Une fonctionnalité cargo éteinte par défaut
      # met du code derrière un `cfg` que seule une commande `--features` rend vrai ; si aucune
      # commande bloquante de la CI ne l'active, une régression de compilation y passe au VERT. MESURÉ
      # le 2026-08-21 sur les QUATRE manifestes : 9 fonctionnalités déclarées, 8 compilées — l'écart
      # était exactement `fim_windows_native` (agent), prouvé par mutation (`cargo xwin check` : une
      # erreur de type sous ce `cfg` rend 101 avec la fonctionnalité, 0 sans). Le constat tenait pour
      # ce qu'il annonçait : il ne cachait pas un écart plus large. Même famille que `S36`.
      # La garde n'énumère ni fonctionnalité ni job : elle DÉRIVE un côté des manifestes `Cargo.toml`
      # (parseur TOML, dépendances optionnelles implicites comprises) et l'autre des blocs `run:` de
      # TOUS les workflows (recettes `make` suivies ; étapes consultatives ou conditionnelles exclues
      # — elles ne gardent rien), et REFUSE l'écart. Elle refuse aussi de CONCLURE sous un plancher de
      # population ou devant un `--features $VAR` qu'elle ne sait pas résoudre, et elle valide son
      # instrument sur des témoins dans les deux sens avant tout verdict. Une exception doit porter
      # sa raison et sa condition de levée, et pourrit si sa fonctionnalité disparaît ou se compile.
      # `yaml` vient du paquet de la distribution (signé par apt), jamais de pip.
      - name: Toute fonctionnalité cargo déclarée est compilée par un job bloquant
        run: |
          set -euo pipefail
          python3 -c 'import yaml' 2>/dev/null || sudo apt-get install -y python3-yaml
          python3 .github/scripts/check_every_feature_is_compiled_somewhere.py

      # `P7.17-a` — UN CHIFFRE D'EXPLOITATION NE SE PUBLIE PAS (AGENTS.md §4). Un chiffre de CONCEPTION (un
      # plafond, une borne, un ordre de grandeur qui décrit le produit) se publie ; un chiffre RELEVÉ sur
      # une installation (taille de base, débit, compteur, horodatage d'incident, nom d'objet) renseigne
      # sur une cible et n'apprend rien au lecteur du code. La roadmap publique avait été expurgée, pas
      # les commentaires ni la documentation : un filtre grossier en rendait déjà des dizaines. Ce qui
      # sépare les deux classes est L'OBJET MESURÉ — le produit (banc, harnais, arbre, suite) ou une
      # installation — et la garde ne lit pas une liste de mots : elle reconnaît la FORME d'un relevé
      # (une grandeur attribuée à une installation réelle ; un volume daté sans contexte de banc ou
      # d'arbre déclaré ; un hôte sous le domaine de l'éditeur, DÉRIVÉ de l'identité canonique ; un objet
      # Kubernetes nommé). La règle d'écriture qu'elle tient : « tout chiffre daté dit d'où il vient ».
      # Elle valide son instrument dans les deux sens avant tout verdict, refuse de conclure sous ses
      # planchers, et dit ce qu'elle ne voit pas : un relevé sans forme reconnaissable, les fichiers de
      # données, et l'historique déjà poussé — elle tient la version servie, pas le passé.
      - name: Un chiffre d'exploitation ne se publie pas (AGENTS.md §4)
        run: python3 .github/scripts/check_no_operational_figure_is_published.py

      # Discovery is by EXTENSION **and** by first-line SHEBANG, deliberately: today all 43
      # tracked scripts carry a `.sh` extension (measured), and 42 of them a sh/bash shebang —
      # the odd one out is `collectors/lib.sh`, a SOURCED library with no shebang, which the
      # extension glob catches. The shebang sweep is what stops a future extensionless
      # `collectors/foo` from being ingested into production while invisible to this gate.
      # Only the FIRST line counts (`head -1`): matching `^#!` anywhere would drag in Rust
      # files that merely embed a shebang inside a string or heredoc.
      #
      # MIN_SCRIPTS is a FLOOR, not the exact-count invariant used for the Rust suites: adding
      # collectors is routine and must not require touching this file. The floor exists to make
      # the real failure mode impossible — a broken glob or a rename that leaves the loop with
      # ZERO files would otherwise report a cheerful green "all scripts pass".
      - name: bash -n — every tracked shell script must parse
        env:
          MIN_SCRIPTS: "43"
        run: |
          set -euo pipefail
          mapfile -t files < <(
            {
              git ls-files '*.sh' '*.bash'
              git ls-files | while IFS= read -r f; do
                [ -f "$f" ] || continue
                head -1 "$f" 2>/dev/null | grep -qE '^#!.*/(env +)?(ba)?sh( |$)|^#!.*/(ba)?sh$' && printf '%s\n' "$f"
              done
            } | sort -u
          )
          echo "tracked shell scripts discovered: ${#files[@]} (floor: $MIN_SCRIPTS)"
          if [ "${#files[@]}" -lt "$MIN_SCRIPTS" ]; then
            echo "::error::discovered only ${#files[@]} shell scripts, floor is $MIN_SCRIPTS."
            echo "::error::Either discovery broke (this gate would be checking nothing), or scripts were"
            echo "::error::legitimately removed — in that case lower MIN_SCRIPTS here, from your own count."
            exit 1
          fi
          rc=0
          for f in "${files[@]}"; do
            if ! bash -n "$f"; then
              echo "::error file=$f::bash -n: syntax error"
              rc=1
            fi
          done
          # NB: plain `if`, not `[ ... ] && echo` — under `set -e` a false `[ ... ]` at the end of
          # an && list aborts the script, which would exit non-zero for the right reason by pure
          # accident. Be explicit instead.
          if [ "$rc" -eq 0 ]; then echo "all ${#files[@]} scripts parse."; fi
          exit "$rc"

      # shellcheck is CONSULTATIVE (continue-on-error) and stays that way until someone
      # measures the tree clean against it. It is NOT hardcoded as a blocking gate because
      # that claim was never verified: shellcheck is absent from the machine these scripts
      # were checked on, so the number of findings on this tree is NOT MEASURED. Promoting it
      # to blocking on a guess would either wedge CI on day one or, worse, be quietly deleted
      # by the next person it inconveniences. Measure first, then drop continue-on-error.
      # (`shellcheck` ships preinstalled on the ubuntu-24.04 runner image; `|| true` on the
      # install line keeps this step from failing the job if that ever stops being true —
      # consistent with it being advisory.)
      - name: shellcheck (CONSULTATIVE — advisory only, not a gate)
        continue-on-error: true
        run: |
          set -uo pipefail
          command -v shellcheck >/dev/null 2>&1 || sudo apt-get install -y shellcheck || true
          command -v shellcheck >/dev/null 2>&1 || { echo "shellcheck unavailable — skipping (advisory step)"; exit 0; }
          shellcheck --version
          # shellcheck -x follows `source`d files (collectors/lib.sh); -S style keeps output complete.
          git ls-files '*.sh' '*.bash' | xargs -r shellcheck -x -S style || true

  # ---------------------------------------------------------------------------
  # features — COMPILE-CHECK EVERY OFF-BY-DEFAULT FEATURE.
  #
  # WHY: gated code that nothing compiles ROTS. `cold_tier` proved it (see that job: it was
  # already red on main). These features are the rest of the OFF-by-default surface; each gets
  # a `cargo check` so a refactor of shared code cannot silently break a backend nobody builds.
  #
  # WHAT THIS DOES NOT DO — stated plainly so a green tick is not over-read: `cargo check` is
  # TYPE-CHECKING ONLY. It does not link, does not run a single test, and proves NOTHING about
  # behaviour. `saml`, `ldap`, `ai`, `clickhouse` and `clickhouse-ha` have NO executed CI
  # coverage whatsoever. A logic regression in the SAML assertion validator, the LDAP bind, the
  # AI advisory path or the ClickHouse backend WILL pass this pipeline green.
  # Making that real requires test suites runnable without a live IdP / directory / model
  # endpoint / ClickHouse server — that work is NOT done and is not claimed here.
  #
  # Each feature is checked SEPARATELY, not as one combined `--features a,b,c`: features are
  # additive, so a combined check can hide a feature that only fails on its own (e.g. one that
  # relies on an import another feature happens to bring in).
  # ---------------------------------------------------------------------------
  features:
    name: features (cargo check per off-by-default feature)
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Rust toolchain (pinned)
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"

      - name: Cache cargo (registry + target)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            daemon/target
          key: cargo-features-${{ runner.os }}-${{ hashFiles('daemon/Cargo.lock') }}
          restore-keys: |
            cargo-features-${{ runner.os }}-

      # `ai` — pure compile feature (no new crate; enables guatx-core/ai). Cheapest of the set.
      - name: cargo check --features ai (advisory NL->SOQL)
        working-directory: daemon
        run: cargo check --features ai --locked --verbose

      # `ldap` — pulls ldap3 (pure-Rust, tokio-rustls).
      - name: cargo check --features ldap (LDAP/AD bind)
        working-directory: daemon
        run: cargo check --features ldap --locked --verbose

      # `saml` — pulls samlify/saml-rs/bergshamra (pure-Rust, RustCrypto).
      - name: cargo check --features saml (SAML 2.0 SP)
        working-directory: daemon
        run: cargo check --features saml --locked --verbose

      # `clickhouse` — scale-out cold backend (pure-Rust async HTTP client).
      - name: cargo check --features clickhouse (scale-out tier)
        working-directory: daemon
        run: cargo check --features clickhouse --locked --verbose

      # `clickhouse-ha` — INERT SCAFFOLD, superset of `clickhouse`. It is not activated by
      # `--features clickhouse`, so it needs its own check or it would compile nowhere.
      - name: cargo check --features clickhouse-ha (distributed scaffold)
        working-directory: daemon
        run: cargo check --features clickhouse-ha --locked --verbose

      # `duckdb` — EXPERIMENTAL warm tier. Moved here from the `test` job (same command).
      # Un-bundled: `--features duckdb` LINKS a system libduckdb. `cargo check` does not link,
      # which is precisely why this is a `check` and not a `build` — it validates the Rust side
      # without requiring libduckdb on the runner.
      - name: cargo check --features duckdb (warm tier)
        working-directory: daemon
        run: cargo check --features duckdb --locked --verbose

      # `s3_backup` — object-storage sink of the backup scheduler. Adds ZERO crates to the graph
      # (SigV4 is HMAC-SHA256 + SHA-256, both already in `util::hexcrypto`; the transport reuses
      # `util::http_client` and the rustls/ring roots already linked).
      - name: cargo check --features s3_backup (sink objet de sauvegarde)
        working-directory: daemon
        run: cargo check --features s3_backup --locked --verbose

      # ...AND THEN ACTUALLY RUN ITS TESTS. A `cargo check` proves the feature compiles; it proves
      # NOTHING about the property this module exists for — that a failed upload can never come back
      # as a success. Four of its tests script a local fake object service into answering the exact
      # ways an upload can go wrong (refusal, silent close, a re-read that reports a DIFFERENT size,
      # nothing listening), and each asserts the verdict is not a deposit. Those are the tests whose
      # absence would be expensive, so they run here rather than only on a contributor's machine.
      #
      # FILTERED ON PURPOSE. `cargo test --features s3_backup` alone would also re-run the whole
      # default suite on this runner — the same work the `test` job already does, for no added
      # signal. The name filter keeps only what the feature adds; the test-binary build (the
      # expensive part) is shared with the `cargo check` step above via the same target dir.
      #
      # THE COUNT IS ASSERTED, like every other suite in this file, with its OWN counter. Same awk,
      # same failure mode. A feature-gated test that silently stopped being compiled would otherwise
      # leave this step cheerfully green on zero tests — which is exactly the shape of rot the
      # cold-tier job's anti-no-op check was added to catch.
      - name: cargo test --features s3_backup (tests du sink objet) + assert the count
        working-directory: daemon
        env:
          # MESURÉ le 2026-08-19 (`cargo test --offline --locked --features s3_backup sink_s3_`,
          # rc=0, 1,82 s de suite filtrée). Ce compte est celui des tests que la FEATURE ajoute ; il
          # est indépendant de EXPECTED_TESTS et n'a pas à bouger quand la suite par défaut bouge.
          # La feature n'ajoute AUCUN test à la suite par défaut — vérifié par ÉNUMÉRATION le même
          # jour (`-- --list` avec et sans la feature : l'écart vaut exactement ce nombre, et les
          # noms de l'écart sont tous ceux de `tests/sink_s3.rs`).
          EXPECTED_S3_TESTS: "12"
        run: |
          set -euo pipefail
          export TMPDIR="$RUNNER_TEMP/plume-tmp-s3"
          rm -rf "$TMPDIR"; mkdir -p "$TMPDIR"
          rc_cargo=0
          cargo test --features s3_backup --locked --verbose sink_s3_ 2>&1 | tee /tmp/s3-tests.log || rc_cargo=$?
          # `P7.19-b` — LE CANAL DISTINCT, ARMÉ ICI. Le code de cargo est CAPTURÉ (et non laissé
          # tuer le pas), puis le journal est TRIÉ : si un banc a REFUSÉ DE CONCLURE, sa marque y
          # figure et le tri rend **2** — « je n'ai pas pu mesurer » — là où une propriété violée
          # laisse passer le code de cargo. Le job reste ROUGE dans les deux cas (un banc qui sait
          # ne pas avoir mesuré n'a pas le droit d'être vert) : ce que le 2 achète est un tri
          # MÉCANIQUE — code de sortie du pas + annotation distincte — au lieu d'une lecture
          # d'humain sur un `panic!` indiscernable d'une assertion. La marque n'est écrite NULLE
          # PART ici : le tri la LIT dans `daemon/src/tests/query_verify.rs` (`QB_MARQUE_REFUS`).
          rc_tri=0
          python3 "$GITHUB_WORKSPACE/.github/scripts/check_a_bench_refusal_is_a_distinct_channel.py" \
            --trier /tmp/s3-tests.log "$rc_cargo" || rc_tri=$?
          [ "$rc_tri" = 0 ] || exit "$rc_tri"
          total=$(awk '/^test result: ok\. [0-9]+ passed/{s+=$4} END{print s+0}' /tmp/s3-tests.log)
          echo "s3_backup tests passed: $total (expected $EXPECTED_S3_TESTS)"
          if [ "$total" != "$EXPECTED_S3_TESTS" ]; then
            echo "::error::s3_backup suite test count is $total, expected $EXPECTED_S3_TESTS."
            echo "::error::If the change legitimately adds or removes s3_backup tests, update"
            echo "::error::EXPECTED_S3_TESTS HERE, from your own measurement — and nowhere else."
            exit 1
          fi
          if [ "$total" -eq 0 ]; then
            echo "::error::zero test ran: the filter or the feature gate stopped matching — this step is a no-op."
            exit 1
          fi
          # `P8.9-m` — même mesure de résidu, même raison, même $TMPDIR isolé (voir le job cold-tier).
          "$GITHUB_WORKSPACE/.github/aucun-temporaire-laisse.sh" "suite s3_backup"

  # ---------------------------------------------------------------------------
  # security — supply-chain + secret hygiene. Independent gate: runs in parallel with
  # the test job and never gates its logic. Two checks:
  #   1) cargo-audit — RustSec advisory scan of the daemon crate's locked dep tree
  #      (--deny warnings => unmaintained/yanked/vuln advisories FAIL the job). Reads
  #      daemon/Cargo.lock only (the patched path dep guatx-core is skipped — no build,
  #      no core checkout needed).
  #   2) gitleaks   — secret scan of the tree + history. An ephemeral config (written
  #      below, NOT committed) extends the default ruleset and ALLOWLISTS the two
  #      documented unit-test PEM fixtures (daemon/src/idp.rs OIDC test key,
  #      daemon/src/tests/saml.rs SAML test key/cert — non-secret by design) so the scan
  #      flags REAL leaks without false-positiving on the test keys.
  # ---------------------------------------------------------------------------
  security:
    name: security (cargo-audit + gitleaks)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0   # full history so gitleaks can scan past commits, not just the tip.

      # AVANT gitleaks, et pour cause : un octet NUL dans un fichier suivi le fait classer en
      # `data`, et `grep` SANS `-a` le saute EN SILENCE. Mesuré dans le dépôt frère `forge` —
      # un NUL dans un commentaire de code, et grep y trouvait 0 occurrence d'un motif présent
      # 9 fois : un secret déposé là n'aurait été vu par personne. La garde n'énumère aucun
      # fichier : elle exige que TOUT fichier suivi porteur d'un NUL soit déclaré binaire dans
      # `.gitattributes` — donc un fichier ajouté demain est couvert par construction — et que
      # la déclaration reste étroite, pour qu'un `*.rs binary` ne la désarme pas d'une ligne.
      - name: Aucun octet NUL non déclaré (sinon les scans de secrets sautent le fichier)
        run: python3 .github/scripts/check_no_stray_nul.py

      - name: Setup Rust toolchain (pinned)
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"

      # VERSION ÉPINGLÉE — et c'est le point, pas un détail. `cargo install cargo-audit --locked`
      # SANS `--version` installe « la dernière publiée du jour », donc le verdict de cette porte
      # pouvait changer sans qu'une ligne du dépôt ait bougé : une release amont suffisait.
      #
      # Le mode de panne à connaître est un ÉCHEC AU CHARGEMENT, pas un findings de plus. `--locked`
      # fait construire cargo-audit avec SON Cargo.lock, donc avec une version FIGÉE de la crate
      # `cvss` qui analyse les scores de la base d'avis. Mesuré le 2026-08-19 (Cargo.lock des .crate
      # publiés) : cargo-audit 0.21.2 verrouille cvss 2.0.0, dont `src/` n'a QUE le module `v3` ;
      # cvss 2.1.0+ ajoute `v4`, et 0.22.0/0.22.1/0.22.2 verrouillent tous cvss 2.2.0. Face à une
      # advisory notée en CVSS 4.0 — RUSTSEC-2026-0066 (astral-tokio-tar), entrée dans la base le
      # 2026-03-17 — cvss 2.0.0 rend `unsupported CVSS version: 4.0` PENDANT le chargement de la
      # base : l'outil n'atteint alors NI la liste d'ignore de daemon/.cargo/audit.toml NI le scan,
      # et aucun correctif de vulnérabilité ne peut rendre ce job vert. 0.22.2 charge les 1217
      # advisories et scanne les 541 deps (mesuré).
      #
      # POUR RELEVER cette version, deux conditions à VÉRIFIER, pas à supposer :
      #   1. le Cargo.lock de la release visée épingle cvss >= 2.1.0 (sinon retour du même mur) ;
      #   2. son `rust-version` est <= la toolchain épinglée ci-dessus (0.22.2 exige 1.88 ; 1.96.0 OK).
      #
      # La base d'avis (advisory-db), elle, n'est VOLONTAIREMENT pas épinglée : la figer rendrait
      # cette porte aveugle à toute advisory publiée après la révision choisie — ce serait désarmer
      # le contrôle. On épingle l'OUTIL (son comportement doit être reproductible), pas la BASE
      # (son contenu doit être frais).
      - name: Install cargo-audit (version épinglée — lit le CVSS 4.0)
        run: cargo install cargo-audit --locked --version 0.22.2

      # `--deny warnings` = unmaintained/unsound/yanked/vuln advisories FAIL the job. The TLS/crypto
      # advisory cluster (ring 0.16, rustls 0.21, rustls-webpki 0.101, rustls-pemfile 1.0 — RUSTSEC-2025-0009/
      # -0010, -2026-0098/-0099/-0104, -2025-0134) and the anyhow unsound-downcast (RUSTSEC-2026-0190) were
      # FIXED BY UPDATE (ldap3 0.11->0.12 pulls the patched rustls 0.23/ring 0.17 stack; anyhow -> 1.0.104) —
      # NOT ignored; so was the h2 unbounded-empty-DATA-frames advisory (RUSTSEC-2026-0258, h2 0.4.15 ->
      # 0.4.16 via `cargo update -p h2`, transitive through hyper). The accepted advisories with no
      # fix-by-update live in daemon/.cargo/audit.toml
      # (family convention, cf forge console/.cargo/audit.toml) — read automatically by cargo-audit from
      # working-directory: daemon. Each is transitive/build-time or off-by-default and justified there:
      #
      #   RUSTSEC-2023-0071 (rsa 0.9.x, "Marvin" RSA-decryption timing side-channel) — NO patched rsa release
      #     exists. Enters ONLY via the off-by-default `saml` feature (samlify->bergshamra XML-DSig). Plume uses
      #     RSA solely to (a) VERIFY IdP assertion signatures (finish_sso, public-key op — no secret) and
      #     (b) SIGN outbound AuthnRequests (private-key SIGNING, not decryption). It NEVER performs RSA
      #     private-key DECRYPTION of attacker-supplied ciphertext (no EncryptedAssertion handling) -> no
      #     Marvin decryption oracle is exposed. Not applicable.
      #   RUSTSEC-2024-0436 (paste 1.0.15, unmaintained) — build-time proc-macro, transitive via the
      #     off-by-default clickhouse/parquet(cold_tier) deps. No maintained drop-in successor; compile-time
      #     only, no runtime attack surface. No fix available.
      #   RUSTSEC-2026-0173 (proc-macro-error2 2.0.1, unmaintained) — build-time proc-macro, transitive via
      #     age -> i18n-embed-fl. Compile-time only, no runtime surface. No fix available.
      #   RUSTSEC-2024-0320 (yaml-rust 0.4.5, unmaintained) — direct dep, used ONLY by the Sigma-rule importer
      #     front-end (YAML -> serde_json::Value) on operator-supplied rule files. "Unmaintained" != vulnerable;
      #     the successor `yaml-rust2` is a DIFFERENT crate (API break, migration risk to the whole default suite),
      #     so there is no in-place fix-by-update. Revisit if a real yaml-rust CVE lands.
      #   RUSTSEC-2026-0235 (rkyv 0.7.46, insufficient archive validation -> OOB reads on Rc/Arc archives) —
      #     the announced fix is >=0.8.17, which is UNREACHABLE by update: rust_decimal, up to and including
      #     its latest release 1.42.1, declares `rkyv = "^0.7.46"` as its NORMAL optional dep (0.8 is only a
      #     dev-dependency there), and 0.7.46 is the last 0.7.x — so `cargo update -p rkyv --precise 0.8.17`
      #     hard-fails on that requirement. Chain measured 2026-08-19: plume-daemon -> duckdb (off-by-default
      #     feature) -> rust_decimal -> rkyv. Real surface is NIL: nobody enables rust_decimal's `rkyv`
      #     feature (its defaults are serde+std, duckdb takes it with `features = []`), and
      #     `cargo tree -i rkyv --all-features` prints "nothing to print" — rkyv is never compiled or
      #     linked, it exists only as a Cargo.lock entry that cargo-audit scans. Lift when rust_decimal
      #     moves its normal dep to rkyv 0.8.
      # `P8.29-a` — UN PAS QUI ÉCHOUE NE DOIT PAS CACHER CELUI D'APRÈS. Mesuré le 2026-08-28 : le scan de
      # secrets était rouge depuis le 2026-08-27 et DEUX exécutions ne l'ont pas montré, parce que l'audit
      # des dépendances échoue AVANT lui et arrête le travail. Le rouge affiché nommait donc une cause
      # vraie et en TAISAIT une autre — la famille même que ce dépôt poursuit : un résultat incomplet
      # présenté comme complet. Les deux scans sont désormais joués DANS TOUS LES CAS, leurs issues sont
      # RETENUES, et un pas final refuse si l'une d'elles a échoué. Aucun des deux ne devient tolérant :
      # ce qui change est l'ORDRE DE RÉVÉLATION, pas la sévérité.
      - name: cargo audit (daemon crate — deny warnings)
        id: cargo_audit
        continue-on-error: true
        working-directory: daemon
        run: cargo audit --deny warnings

      # Ephemeral gitleaks config (runner-only, never committed) — extends defaults +
      # allowlists the documented unit-test PEM fixtures so they cannot false-positive.
      - name: Write gitleaks config (allowlist documented test PEM fixtures + env-ref/placeholder paths + schema-rebrand SQL)
        if: always()
        run: |
          cat > "$GITHUB_WORKSPACE/.gitleaks-ci.toml" <<'EOF'
          title = "plume CI gitleaks config"
          [extend]
          useDefault = true
          [allowlist]
          description = "Documented unit-test PEM fixtures, shell env-var references, documented test placeholders and the schema-rebrand SQL — non-secret by design."
          # LINE-scoped allowlist. Production source files are NEVER excluded wholesale: a real secret introduced
          # in `main.rs`, in `migrate.rs` or in any collector must still trip the scan. Each regex below suppresses
          # one PRECISE non-secret shape:
          #   1. the rebrand migration SQL — `REPLACE(query,'key=soc_creds','key=plume_creds')` and its siblings —
          #      which the generic-api-key rule reads as a credential; it is a panel-query key rename.
          #   2. shell references to environment variables in the collectors (`-u "${PLUME_USER:?}:${PLUME_PASS:?}"`,
          #      `Authorization: Bearer $PLUME_TOKEN`): the value never appears, only the variable name.
          #   3. two documented test placeholders that live in production files.
          #   4. the FTS-compaction benchmark's own SQLCipher key, which encrypts only the scratch
          #      databases that benchmark builds for itself.
          #   5. the purge test's canary string, which that test asserts is ABSENT from the ledger.
          #   7. the SQLCipher keys of the native-backup posture tests
          #      (`daemon/src/tests/posture_de_sauvegarde_native.rs`): every key there follows the
          #      convention `p8NN-cle-<word>` and opens only the throwaway databases that test BUILDS
          #      itself under a temporary directory. The SHAPE is allowlisted, not the file: any other
          #      string in that module is still scanned. Measured on 2026-08-22: four such keys tripped
          #      `generic-api-key` and turned the public CI red.
          #   6. the shipped-unit digest table in `daemon/src/maj_corroboree.rs` — 88 lines of the exact
          #      shape `("plume-NAME.service", "<64 hex>"),`. These are sha256 digests of unit FILES that
          #      live in this same repository, not credentials. A value allowlist is impossible: each digest
          #      changes whenever its unit does. What makes the SHAPE allowlist safe is that another
          #      mechanism verifies every entry — `les_empreintes_livrees_sont_celles_des_fichiers` re-hashes
          #      the files and fails on a stale digest, a missing name or a name in excess. A foreign value
          #      hidden in that table would therefore turn the suite red, which is a stronger check than the
          #      scan. Anything in that file NOT of this shape is still scanned.
          #   8. the ESM harness's own local-storage key names in `.github/scripts/web_esm_harnais.mjs`.
          #      The shape is `storeKey: "banc_<word>"` — a browser localStorage key handed to the paged-list
          #      component the harness DRIVES, never a credential; the generic-api-key rule reads `key: "value"`
          #      and cannot tell them apart. The SHAPE is allowlisted, not the file: any other `key`-like literal
          #      in that harness — and every `storeKey` that is NOT prefixed `banc_` — is still scanned. Measured
          #      on 2026-08-28: this single line was the ONE public-lineage finding, and it had been red since
          #      2026-08-27 — masked for two runs by the `cargo audit` step, which fails BEFORE this one and
          #      stops the job. A step that hides the step after it is why this went unseen for a day.
          regexTarget = "line"
          regexes = [
            '''storeKey: "banc_[a-z0-9_]+"''',
            '''REPLACE\(query,'key=soc_\w+','key=plume_\w+'\)''',
            '''\$\{PLUME_[A-Z0-9_]+(:\?|:-|\})''',
            '''Bearer \$\{?[A-Z0-9_]+''',
            '''pentest-secret-''',
            '''env-token-xyz''',
            # 4. `bench/fts-compaction/banc_fts.c` line 27 — the literal SQLCipher key of the FTS5 compaction
            #    harness. It is handed to `PRAGMA key=` on the throwaway databases that harness BUILDS itself
            #    (ref.db, purge.db, arme-*.db, all created by `bench/fts-compaction/mesurer.sh` in a scratch
            #    directory); it opens no production database and is read from no configuration. The exact
            #    VALUE is allowlisted, never the file: a real secret added elsewhere in that benchmark — or
            #    anywhere under bench/ — still trips the scan.
            '''banc-fts-cle-sqlcipher-32-octets''',
            # 5. `daemon/src/tests/purge.rs` line 581 — the canary of the test
            #    `purge_ledger_entry_leaks_no_purged_content`. The test INSERTS this string into a throwaway
            #    event, then asserts it is ABSENT from the purge ledger and from the SOC mirror event: it is
            #    the probe of a confidentiality guard, not a credential to anything. The exact VALUE is
            #    allowlisted, never the file — the rest of purge.rs stays scanned (that path is deliberately
            #    NOT in the `paths` list below).
            '''MOTDEPASSE-ULTRA-SENSIBLE-9f3a''',
            # 6. shipped-unit digest table — shape only, see reason 6 above.
            '''\("plume-[a-z0-9-]+\.(service|timer)", "[0-9a-f]{64}"\),''',
            # 7. native-backup posture test keys — shape only, see reason 7 above.
            '''"p8[0-9]{2}-cle-[a-z-]+"'''
          ]
          # PATH-scoped allowlist — TEST FILES ONLY (never a production path). Each holds documented, offline-generated
          # fixtures: the OIDC/SAML unit-test PEM keys live under `daemon/src/idp/` (a directory module) and in
          # `daemon/src/tests/saml.rs`; the remaining two carry test placeholders.
          paths = [
            '''daemon/src/idp/.*\.rs''',
            '''daemon/src/tests/saml\.rs''',
            '''daemon/src/tests/detection\.rs''',
            '''daemon/src/tests/engagement\.rs''',
          ]
          EOF

      # Pinned gitleaks BINARY (v8.24.3) — no repo token, no license required, and behaves
      # identically on push and pull_request. Replaces the former gitleaks GitHub Action, which now
      # requires a repo token on pull_request events (→ failed every PR). The download is
      # checksum-pinned (sha256 from the release's checksums.txt) so a tampered/substituted asset
      # fails the build. The security job checks plume out at the workspace root (no `path:`), so
      # `--source .` == repo root and the config resolves at $GITHUB_WORKSPACE/.gitleaks-ci.toml
      # (written just above). Scans tree + full history (fetch-depth: 0 above). This job keeps only
      # `permissions: contents: read` — the binary needs no token (least privilege).
      - name: Secret scan (gitleaks, pinned binary — no token/license required)
        id: gitleaks
        if: always()
        continue-on-error: true
        run: |
          set -euo pipefail
          GL_VER=8.24.3
          GL_SHA=9991e0b2903da4c8f6122b5c3186448b927a5da4deef1fe45271c3793f4ee29c
          curl -sSfL -o /tmp/gitleaks.tgz "https://github.com/gitleaks/gitleaks/releases/download/v${GL_VER}/gitleaks_${GL_VER}_linux_x64.tar.gz"
          echo "${GL_SHA}  /tmp/gitleaks.tgz" | sha256sum -c -
          tar -xzf /tmp/gitleaks.tgz -C /tmp gitleaks
          /tmp/gitleaks detect --source . --config "$GITHUB_WORKSPACE/.gitleaks-ci.toml" --no-banner --redact --exit-code 1

      # `deny.toml` existait depuis longtemps mais AUCUN job ne l'exécutait : une configuration
      # que personne ne lance n'est pas une garde, c'est de la décoration. Mesuré avant l'ajout —
      # `grep -c "cargo-deny\|cargo deny" ci.yml` = 0.
      #
      # PÉRIMÈTRE : `licenses bans sources`, PAS `advisories`. Les avis sont déjà couverts par
      # `cargo audit --deny warnings` ci-dessus ; les dupliquer n'ajoute rien et importe une
      # fragilité réelle — cargo-deny 0.18.2 échoue à parser un avis portant un score CVSS 4.0
      # (« unsupported CVSS version: 4.0 »), constaté sur la base d'avis locale.
      #
      # Binaire ÉPINGLÉ par version + sha256, comme gitleaks juste au-dessus : `cargo install`
      # recompilerait l'outil à chaque exécution pour le même résultat.
      # P7.7-b — la liste des crates couverts est DÉRIVÉE de l'arborescence, plus ÉNUMÉRÉE.
      # Ce dépôt n'a PAS de workspace Cargo : les crates sont indépendants, et la commande ne
      # nommait que `daemon/Cargo.toml`. `agent` (celui qu'on INSTALLE sur les postes clients),
      # `collector-mail` et `collector-syslog` n'étaient donc vérifiés ni en licences, ni en bans,
      # ni en sources — et un crate ajouté demain ne l'aurait pas été davantage, sans que personne
      # ne le voie. Le `find` découvre les manifestes : couvrir un nouveau crate ne demande plus
      # aucun geste.
      #
      # CE QUE ÇA A TROUVÉ, mesuré le 2026-08-03 en étendant la couverture : `agent` ÉCHOUAIT
      # (code 4) sur `webpki-roots@1.0.8` en `CDLA-Permissive-2.0`. La licence est désormais
      # autorisée NOMMÉMENT et justifiée dans `deny.toml` — pas ajoutée en silence.
      # LE PAS QUI REND LE VERDICT DES DEUX SCANS. Sans lui, `continue-on-error` rendrait le travail vert
      # sur un secret trouvé — exactement le défaut inverse de celui qu'on ferme. Il nomme CHAQUE issue,
      # de sorte qu'un lecteur voit d'un coup ce qui a échoué et ce qui a passé, jamais l'un à la place
      # de l'autre.
      - name: Les DEUX scans de sécurité ont passé
        if: always()
        run: |
          set -u
          echo "cargo-audit : ${{ steps.cargo_audit.outcome }}"
          echo "gitleaks    : ${{ steps.gitleaks.outcome }}"
          rc=0
          [ "${{ steps.cargo_audit.outcome }}" = success ] || { echo "::error::cargo audit a échoué (avis de dépendance ou version retirée)."; rc=1; }
          [ "${{ steps.gitleaks.outcome }}" = success ] || { echo "::error::le scan de secrets a échoué."; rc=1; }
          exit $rc

      - name: Licences, bans et sources des dépendances (cargo-deny, TOUS les crates découverts)
        run: |
          set -euo pipefail
          CD_VER=0.18.2
          CD_SHA=43c4a79c4b9fd1fcb3dddb305a1b4d8f7ac4a72accd61bb50a0b698789ca894c
          CD_TGZ="cargo-deny-${CD_VER}-x86_64-unknown-linux-musl"
          curl -sSfL -o /tmp/cargo-deny.tgz "https://github.com/EmbarkStudios/cargo-deny/releases/download/${CD_VER}/${CD_TGZ}.tar.gz"
          echo "${CD_SHA}  /tmp/cargo-deny.tgz" | sha256sum -c -
          tar -xzf /tmp/cargo-deny.tgz --strip-components=1 -C /tmp "${CD_TGZ}/cargo-deny"

          # Découverte : un crate = un Cargo.toml de premier niveau (pas de workspace ici).
          mapfile -t MANIFESTES < <(find . -mindepth 2 -maxdepth 2 -name Cargo.toml -not -path './target/*' | sort)

          # VALIDER L'INSTRUMENT : un `find` qui ne rend rien passerait au vert en ne vérifiant
          # RIEN. Le seuil est celui des crates réellement présents aujourd'hui.
          echo "crates découverts : ${#MANIFESTES[@]}"
          printf '  %s\n' "${MANIFESTES[@]}"
          if [ "${#MANIFESTES[@]}" -lt 4 ]; then
            echo "::error::découverte des crates suspecte (${#MANIFESTES[@]} < 4) — la garde ne garderait rien"
            exit 1
          fi

          # Tous les crates sont vérifiés AVANT de conclure : on veut la liste COMPLÈTE des
          # violations, pas seulement la première (sinon chaque correctif en révèle une de plus).
          rc=0
          for m in "${MANIFESTES[@]}"; do
            echo "::group::cargo-deny ${m}"
            /tmp/cargo-deny --manifest-path "${m}" check licenses bans sources || rc=1
            echo "::endgroup::"
          done
          exit "${rc}"

  # ---------------------------------------------------------------------------
  # lint — fmt + clippy, CONSULTATIVE (non-blocking until the tree is proven
  # warning-free). cargo resolves the guatx-core git-dep (tag: daemon/Cargo.toml)
  # directly. TODO: make blocking (`-D warnings`, drop continue-on-error) once
  # the tree is clean.
  # ---------------------------------------------------------------------------
  lint:
    name: lint (fmt + clippy — consultative)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Rust toolchain (pinned) + rustfmt/clippy
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"
          components: rustfmt, clippy

      - name: cargo fmt --all --check (consultative)
        working-directory: daemon
        run: cargo fmt --all --check
        continue-on-error: true

      - name: cargo clippy (consultative, no -D warnings)
        working-directory: daemon
        run: cargo clippy --locked --all-targets
        continue-on-error: true

  # ---------------------------------------------------------------------------
  # collectors — `cargo test --locked` pour les DEUX collecteurs compilés.
  #
  # POURQUOI CE JOB EXISTE. Ces deux crates étaient bâtis par leur Dockerfile et
  # vérifiés en licences/bans par le job `security` (qui découvre TOUS les
  # Cargo.toml), mais leur SUITE n'était exécutée nulle part : `test` et
  # `cold-tier` ne connaissent que `daemon/`, `agent-ci.yml` ne connaît que
  # `agent/`. Les gardes que ces crates portent — une liste d'adresses posée et
  # non comprise ne doit pas ouvrir le :514 ; une boîte qu'on ne sait pas
  # examiner ne se conclut pas par « 0 alerte » — n'auraient donc jamais tourné
  # en intégration, et une garde qui ne tourne pas est une garde en nom seulement.
  #
  # LES COMPTES SONT EXACTS, PAS DES PLANCHERS, et c'est la discipline de ce
  # dépôt : la CONSTANCE du compte est ce qui attrape une garde qui aurait cessé
  # d'être énumérée (un `#[cfg]` mal posé, un module de test décroché). Ils sont
  # écrits ICI et nulle part ailleurs.
  # ---------------------------------------------------------------------------
  collectors:
    name: collectors (syslog + mail — cargo test)
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout plume
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Rust toolchain (pinned)
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # @stable (no semver tags; pinned to stable branch commit)
        with:
          toolchain: "1.96.0"

      - name: Cache cargo (registry + target)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            collector-syslog/target
            collector-mail/target
          key: ${{ runner.os }}-cargo-collectors-${{ hashFiles('collector-syslog/Cargo.lock', 'collector-mail/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-collectors-

      # 49 : MESURÉ le 2026-08-21 par ÉNUMÉRATION (`cargo test -- --list`, deux lectures
      # concordantes : le compte des lignes `: test` et le total annoncé par cargo). Le lot `S36` a
      # ajouté à ce crate la garde de la liste d'adresses (une liste POSÉE ne peut jamais produire
      # une liste vide), le refus d'un pair dont l'adresse n'est pas lue, la paire sur l'identité de
      # l'hôte et la parité de vocabulaire avec le module du démon.
      - name: cargo test (collector-syslog) + assert the test count
        working-directory: collector-syslog
        env:
          EXPECTED_SYSLOG_TESTS: "49"
        run: |
          set -euo pipefail
          cargo test --locked --verbose 2>&1 | tee /tmp/syslog-tests.log
          total=$(grep -oP 'test result: ok\. \K[0-9]+' /tmp/syslog-tests.log | paste -sd+ | bc)
          echo "collector-syslog tests passed: $total (expected $EXPECTED_SYSLOG_TESTS)"
          if [ "$total" != "$EXPECTED_SYSLOG_TESTS" ]; then
            echo "::error::collector-syslog test count is $total, expected $EXPECTED_SYSLOG_TESTS."
            echo "::error::If the change legitimately adds or removes tests, update EXPECTED_SYSLOG_TESTS"
            echo "::error::HERE, from your own RE-ENUMERATION (cargo test -- --list) — never from a sum."
            exit 1
          fi

      # 5 : MESURÉ le 2026-08-21 par la MÊME énumération. Ce crate n'avait AUCUN test avant le lot
      # `S36` ; les cinq sont la paire sur l'identité, le contrat d'aveu, la parité de vocabulaire et
      # la garde dérivée qui exécute le collecteur contre une arborescence de courrier fabriquée.
      - name: cargo test (collector-mail) + assert the test count
        working-directory: collector-mail
        env:
          EXPECTED_MAIL_TESTS: "5"
        run: |
          set -euo pipefail
          cargo test --locked --verbose 2>&1 | tee /tmp/mail-tests.log
          total=$(grep -oP 'test result: ok\. \K[0-9]+' /tmp/mail-tests.log | paste -sd+ | bc)
          echo "collector-mail tests passed: $total (expected $EXPECTED_MAIL_TESTS)"
          if [ "$total" != "$EXPECTED_MAIL_TESTS" ]; then
            echo "::error::collector-mail test count is $total, expected $EXPECTED_MAIL_TESTS."
            echo "::error::If the change legitimately adds or removes tests, update EXPECTED_MAIL_TESTS"
            echo "::error::HERE, from your own RE-ENUMERATION (cargo test -- --list) — never from a sum."
            exit 1
          fi

  # ---------------------------------------------------------------------------
  # ci-ok — SINGLE required status-check context for the `main` branch ruleset.
  # The name `CI` is STABLE: it does not depend on any matrix or on the set of
  # upstream jobs, so evolving test/security/lint never breaks the "required
  # check" rule. Without this aggregation job the ruleset would have to require
  # each job name directly — the day one is renamed/removed, the required check
  # would never report again and `main` would become unpushable, including for
  # the very fix that would repair it.
  # ---------------------------------------------------------------------------
  ci-ok:
    name: CI
    if: always() # otherwise a failed upstream job SKIPS this one instead of failing it
    needs: [test, cold-tier, shell, features, security, lint, collectors]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: All required jobs must have SUCCEEDED
        # `skipped` and `cancelled` are treated as failures ON PURPOSE: GitHub
        # counts a required-but-skipped check as satisfied, which would let a
        # job that never ran slip through.
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: |
          echo "::error::a CI job did not succeed: ${{ join(needs.*.result, ', ') }}"
          exit 1
