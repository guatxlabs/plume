{
  "_comment": "Discovery de secrets Kubernetes : >5 `list secrets` par un même sujet = ratissage des Secrets (credentials-in-files). objectRef.resource=secrets + verb=list. Distinct de l'octroi RBAC : ici c'est la LECTURE d'inventaire. Inerte sans audit k8s. || TÉLÉMÉTRIE REQUISE: kube-audit (resource, verb) || Catalogue de détection curé (#22) — managed=1 posé par le loader, enabled=false (bibliothèque : activer via l'UI selon la télémétrie disponible).",
  "name": "Catalogue — K8s: énumération de secrets (list massif)",
  "enabled": false,
  "query": "search source=kube-audit resource=secrets verb=list | stats count by user | where count > 5 | stats count",
  "is_soql": true,
  "op": ">",
  "threshold": 0,
  "severity": 3,
  "interval_s": 300,
  "window_s": 1800,
  "mitre": "T1552.007"
}
