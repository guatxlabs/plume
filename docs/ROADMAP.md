# Feuille de route de plume

plume est un SOC/XDR écrit en Rust, conçu pour tenir dans un budget mémoire de **2 Gio**. Ce
document est l'**index public des chantiers**, pas un journal de développement : chaque ligne dit ce
qu'une clé désigne, dans quel état elle se trouve, et rien de plus. Les messages de commit de ce
dépôt citent ces clés ; l'index existe pour qu'un lecteur puisse retrouver à quoi une clé correspond.
Ce n'est **ni** une liste de fonctionnalités, **ni** un calendrier, **ni** un argumentaire, et il ne
contient aucune mesure prise sur une installation particulière : les chiffres qui restent sont des
propriétés du code (nombre de routes, d'index déclarés, de sondes), jamais l'état d'une machine. Les
limites connues sont nommées, y compris quand elles sont gênantes.

## Comment lire

**La clé** — P*phase*.*chantier*-*constat*, par exemple P10.14-a. Une clé est **stable** : une fois
attribuée elle ne bouge plus, et elle se cite du constat jusqu'au commit. Les numéros de phase de cet
index sont ceux de l'audit interne ; ils ne coïncident pas avec les phases produit du cahier des
charges, où P10 désigne un tout autre sujet.

| État | Signification |
|---|---|
| ✅ | corrigé et vérifié |
| 🔵 | mesuré, décision prise, **pas encore construit** |
| ⬜ | ouvert |
| 🔒 | bloqué : le geste appartient à l'exploitant |
| ❓ | numéro réservé, sans constat attesté |

Une clé peut désigner un **thème** dont plusieurs constats relèvent ; l'index en donne alors une
ligne unique et nomme le résidu dans la phrase. Quelques clés couvrent l'outillage de déploiement,
qui vit hors de ce dépôt ; elles sont conservées parce que des commits les citent. Une clé sans
rapport avec le produit n'est pas listée.

---

## P3 — Ingestion, déduplication, sondes de fraîcheur

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P3.1-a** | Déduplication | ✅ | La clé de déduplication était fabriquée par l'émetteur, qui ne voit que lui-même : deux machines faisant le même travail produisaient la même clé et la seconde ligne était rejetée sans un mot. La clé porte désormais l'hôte. |
| **P3.2-a** | Sondes de fraîcheur | ✅ | Vingt sondes d'événements et une sonde de métriques rendent la donnée la plus **fraîche** du parc : un parc dont une seule machine parlait encore était vu comme sain, et rien ne se levait. Deux affirmations de la version précédente de cette ligne sont **réfutées** — les sondes qui suivent la fraîcheur du pipeline masquent tout autant (cet indicateur est lui aussi un agrégat sans hôte), et l'inventaire d'hôtes ne perd pas une machine devenue muette. La portée de chaque sonde est désormais **déclarée par le type** et lisible dans le panneau, et une sonde de flotte lève un signal nommant les machines entièrement muettes — en rendant un **compte**, jamais une série par hôte. Résidu nommé : un hôte dont un seul collecteur meurt pendant que les autres parlent reste invisible ; repasser les vingt et une sondes par hôte multiplierait la cardinalité par la taille du parc et, mesuré, ferait suivre leur coût au volume. |
| **P3.6-a** | Ingestion Loki | ✅ | Deux documents promettaient une déduplication exactly-once que cette surface ne tient pas. La garantie réelle est at-least-once, et les documents le disent — aucune clé dérivée du contenu n'a été inventée, elle transformerait un doublon visible en perte invisible. |
| **P3.6-b** | Ingestion Loki | ✅ | Cette route insérait directement en base : les processeurs d'ingestion (rejet, masquage de données personnelles, routage) ne s'y appliquaient pas, alors que le code affirmait le contraire en la nommant. |
| **P3.6-c** | Garde d'ingestion | ✅ | La garde voisine ne se déclenchait que si l'ordre SQL mentionnait la déduplication : elle contrôlait la **forme** de la clé, jamais la **présence** du mécanisme. Fermée par le répertoire — tout chemin d'ingestion qui écrit sans passer par le point d'extension échoue. |
| **P3.7-a** | Coût des sondes | ✅ | Le type des sondes figeait la table et la colonne, jamais le **coût** : plusieurs sondes filtraient sur une colonne non indexée, en O(N), sous le verrou d'écriture, à cadence courte. Fermé par un index partiel. Reste ouvert : l'ablation en débit, qui établirait que ce coût était le contributeur dominant, n'est pas faite. |

---

## P4 — Exploitation : CLI, service, collecteurs, plafonds

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P4.1-a** | Collecteur Windows | ✅ | Le tampon était vidé inconditionnellement et le filigrane avancé **avant** l'envoi : une indisponibilité du central perdait définitivement et silencieusement les événements du cycle. |
| **P4.1-b** | CLI | ✅ | L'option d'aide n'imprimait pas que l'aide : elle migrait la base, imprimait un jeton d'installation à usage unique, puis se mettait à écouter. La garde est le complément calculé du flot de contrôle, donc une sous-commande future est couverte sans y penser. |
| **P4.1-c** | Observabilité HTTP | ✅ | Une réponse 5xx ne laissait rien côté serveur : ni couche de trace, ni journal d'accès, ni identifiant de requête. La condition porte sur la classe du code, pas sur une liste. |
| **P4.1-d** | Build Windows | ✅ | La bibliothèque C statique n'était que **documentée** ; sur un Windows sans redistribuable, l'exécutable s'arrêtait avant sa première instruction. |
| **P4.1-e** | Surveillance des capteurs | ✅ | Des capteurs qui n'avaient jamais rien émis étaient déclarés muets par une surface et « inconnus » par une autre : deux verdicts pour la même observation, avec un écart de seuil accidentel et écrit nulle part. |
| **P4.1-f** | Désinstallation | ✅ | La commande de désinstallation annonçait un succès qu'elle n'avait pas obtenu, avec un code de sortie nul et zéro fichier supprimé. Le type de résultat ne permet plus de conclure sans dire ce qui a été retiré. |
| **P4.1-g** … **P4.1-n** | — | ❓ | Lettres réservées : aucun constat ne les porte dans ce dépôt. Le trou n'est pas comblé par déduction. |
| **P4.1-o** | Plafonds d'ingestion | ✅ | Deux plafonds gardaient le même chemin, et c'est **celui qui ne parle pas** qui mordait en premier : le plafond d'octets rendait inatteignable le plafond en nombre d'événements, dont le message était pourtant le seul à nommer compte, limite et levier. |
| **P4.1-p** | Plafonds d'ingestion | ✅ | Trois défauts de la même famille : une route accusait réception de ce qu'elle avait **retenu** et non reçu ; trois sites de rejet ne comptaient rien, et le seul compteur existant nommait mal ce qu'il comptait ; un refus conseillait un levier sans effet dans la configuration par défaut. Le verdict est désormais dérivé de la comparaison des plafonds, donc changer une constante change le message. |
| **P4.1-q** *(clé neuve)* | Moniteur d'integrite | ⬜ | **Une detection qui s'ETEINT, sans trace** — famille distincte de la valeur rassurante publiee. Dans le moniteur d'integrite, l'echec d'une lecture de metadonnees provoque un RETOUR SILENCIEUX (`agent/src/source/fim/linux.rs`, deux sites) : le parcours abandonne la branche courante, si bien qu'un SOUS-ARBRE ENTIER n'est alors JAMAIS surveille — aucun evenement, aucun aveu, et la couverture annoncee reste celle de la configuration. Trouve hors perimetre par le lot des sites d'action. Second site de la meme famille : la commande d'etat de l'agent (`agent/src/main.rs`), ou une configuration ou une file d'attente illisible fait DISPARAITRE la profondeur de file de l'affichage, au lieu de la dire inconnue. |
| **P4.2-a** | Service systemd | ✅ | L'installation annonçait « service installé et démarré » et sortait 0 sur un service **mort**. La garde observe une durée d'activité, un instantané pouvant dire « actif » juste avant l'échec. |
| **P4.2-b** | Build et messages d'erreur | ✅ | Un échec de compilation croisée ne nommait pas le paquet manquant, et un échec TLS sur certificat auto-signé était classé comme une erreur d'entrée-sortie sans son remède. Les remèdes ont été exécutés avant d'être écrits ; l'un était faux et n'est donc pas conseillé. |
| **P4.3-a** | Collecteur Windows | ✅ | En accès refusé sur le journal de sécurité, le collecteur rendait zéro événement, zéro aveu et un code de sortie nul, pendant que son battement de santé annonçait « ok ». Le discriminant retenu est l'identifiant d'erreur, jamais le message, qui est localisé. |

### P4.4 — Porte de déploiement

Ces clés couvrent l'outillage qui décide qu'un déploiement est sain. Il vit hors de ce dépôt ; les
constats sont conservés ici parce que des commits les citent et parce que les défauts sont
génériques.

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P4.4-a** | Porte de déploiement | ✅ | La porte interrogeait la **santé** du service et jamais l'**identité** de ce qui sert : toutes ses conditions restaient vraies sur la version précédente, donc elle ne pouvait pas échouer tant que l'ancien processus vivait. Le premier correctif a lui-même désactivé en silence le contrôle de schéma — corrigé à son tour, la garde qui cesse de mordre étant la même famille de défaut. |
| **P4.4-b** | Porte de déploiement | ✅ | Le script poussait le changement puis rendait un verdict sans jamais constater que l'orchestrateur avait vu et appliqué la nouvelle révision. |
| **P4.4-c** | Retour arrière | ✅ | La porte promettait un retour arrière automatique qu'elle ne pouvait pas tenir : une base déjà migrée est refusée par le binaire précédent. La porte est désormais à **sens unique** quand le schéma monte, le dit, et exige un acquittement explicite. Reste : la table de décision est prouvée par un harnais jetable, pas par une garde permanente. |
| **P4.4-d** | Dérive dépôt / production | ✅ | Un changement de build livré mais jamais construit n'a été crié par personne : la porte vérifie l'identité **au moment** du déploiement, rien ne regardait l'écart entre deux déploiements. |
| **P4.4-e** | Porte de déploiement | ✅ | Le script annonçait un échec après avoir réussi et sautait son propre filet : une étape devenue sans objet rend un code non nul, ce qui court-circuitait la porte de santé et laissait le retour arrière non armé. |
| **P4.4-f** | Fraîcheur de la référence | ✅ | La référence annoncée désignait une copie de travail et non le dépôt de vérité, et la mesure avait lieu avant tout rafraîchissement : un commit poussé pouvait être invisible, le script concluant « rien à construire » puis « succès ». Aucune phrase n'était fausse isolément ; l'ensemble décrivait un état périmé et l'appelait un succès. |
| **P4.4-g** | Sonde de dérive | ✅ | La sonde de dérive était elle-même dérivée : installée une fois, jamais réinstallée après modification. Le périmètre vient désormais d'une table unique, et l'installé est comparé au **rendu**, la plupart des fichiers surveillés étant engendrés et n'ayant aucune source à comparer. |
| **P4.4-h** | Fraîcheur de la référence | ✅ | La fraîcheur de la copie de travail reposait sur un mécanisme absent de tout dépôt, désactivable, et muet quand il refuse. Les seuls fichiers dont le rendu dépend sont désormais certifiés contre le dépôt de vérité, et une comparaison non certifiée est **sautée** plutôt que faussée. |
| **P4.4-i** | Pose de configuration | ✅ | Un fichier de configuration posé sur un hôte avait divergé de sa version au dépôt sans que rien ne le rapporte. L'écart était inerte, mais il établissait que la version du dépôt était une reconstruction jamais confrontée aux octets vivants. |
| **P4.4-j** | Banc de test | ✅ | Un banc qui **exécute** le geste de réparation imprimé par une table exécute ce que la table nomme : une mutation lui a fait lancer un installateur système. Le geste imprimé est désormais confiné avant exécution, sans affaiblir la détection. |
| **P4.4-k** | Porte de déploiement | ✅ | La porte sondait la seule route que les sondes d'orchestration évitent délibérément, parce qu'elle prend un verrou bloquant. Le défaut n'est pas le délai mais l'ambiguïté : « ne répond pas » et « écrivain occupé » rendaient la même phrase. Une phrase les départage désormais, aux deux sites. |

### P4.5 — Ce que l'image contient vraiment

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P4.5-a** | Image et fonctionnalités | ✅ | Une consolidation a retiré le tier froid de l'image, en silence : le déploiement déclarait cinq variables que le binaire ne pouvait pas honorer. |
| **P4.5-b** | Garde d'image | ✅ | La garde comparait des **commits** et non des **capacités** : deux recettes de build pouvaient différer d'un jeu de fonctionnalités sans que rien ne le voie. La garde est désormais dérivée du manifeste jusqu'au symbole présent dans le binaire, et elle accuse l'instrument plutôt que l'image quand son témoin positif tombe. |

---

## P5 — Taxonomie, autorité, portée d'un jeton

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P5.1-a** | Détection | ✅ | Les deux règles de force brute livrées et activées compilent un filtre sur un champ d'action qu'aucun émetteur Windows ne posait : la technique était aveugle sur l'OS où elle est la plus courante. |
| **P5.1-b** | Taxonomie | ✅ | Le contrôle de taxonomie sortait avant tout contrôle sur une catégorie vide, au motif écrit dans le code que le parseur trancherait — ce qu'aucun parseur livré ne faisait pour ces sources. Des événements sans catégorie étaient acceptés, stockés, sans une ligne de journal. Aucune valeur d'office n'a été inventée : c'est le silence qui est fermé. |
| **P5.2-a** | Portée des jetons | ✅ | Le liage « un jeton de machine n'écrit que sous son hôte » existait à trois exemplaires écrits séparément et manquait sur quatre surfaces d'ingestion, alors que la documentation n'annonçait qu'une exception. |
| **P5.2-b** | Portée des jetons | ✅ | La forme la plus courte de la commande de création de jeton — celle de la documentation — produisait un jeton **non lié**, capable d'écrire sous n'importe quel nom d'hôte. La portée est désormais une somme fermée que le compilateur impose, et le point d'écriture est unique pour la CLI comme pour l'interface. |
| **P5.3-a** | Autorité HTTP | ✅ | Le garde d'hôte ne lisait que l'en-tête Host, qui n'existe pas en HTTP/2 où l'autorité est un pseudo-en-tête ; l'absence était traduite en refus. En TLS natif, la quasi-totalité des routes était injoignable depuis un navigateur, tous les émetteurs internes étant en HTTP/1.1. |
| **P5.4-a** | Collecte Windows | ✅ | Les identifiants Kerberos manquaient à la liste du collecteur : une cécité que seul un contrôleur de domaine révèle. La liste est désormais dérivée d'une table identifiant → issue, et chaque exécution recense ce que le canal produit et qu'on ne collecte pas. |
| **P5.4-b** | Collecte Windows | ✅ | Une catégorie d'exécution sans ligne de commande n'est pas vide, elle est **trompeuse** : un tableau de bord voit un chiffre non nul et stable et ne surveille rien. Le collecteur déclare désormais l'amputation ; aucune valeur n'est fabriquée. |
| **P5.5-a** | Secrets | ✅ | Le secret passait par la ligne de commande : les arguments d'un processus sont publics, et le capteur de journal les expédiait au SOC que ce secret protège. Non fermable côté produit — l'audit d'exécution du système capte ce qu'un opérateur tape. |
| **P5.5-b** | Confidentialité de la collecte | ✅ | Trente-deux champs de journal système étaient expédiés alors que neuf sont lus, dont la ligne de commande complète de l'émetteur, sur le réseau et dans le tampon disque. La restriction est sondée dans les deux sens et son absence **dégrade en le disant**. |
| **P5.6** | — | ❓ | Numéro réservé : aucun constat ne le porte. |
| **P5.7-a** | Règles semées | ✅ | Une règle semée, activée et taguée d'une technique ATT&CK portait une requête qui ne contraignait que la sévérité : la technique était annoncée **couverte** alors que rien n'observait l'authentification. La migration ne corrige que les règles restées à leur forme livrée, jamais celles qu'un exploitant a éditées. |
| **P5.7-b** | Auto-surveillance | ✅ | Le collecteur d'intégrité surveille le répertoire d'unités système, et une règle livrée y voit un vecteur de persistance : toute mise à jour de plume levait une alerte sur le SOC lui-même. Aucune exemption par nom n'est faite — elle offrirait un angle mort taillé sur mesure. Un dépôt d'unité est **reclassé** en informationnel quand deux faits tiennent ensemble : son **contenu** est l'une des unités que ce build livre, sous le nom sous lequel elle l'est, **et** un déploiement daté et audité vient d'ouvrir une fenêtre bornée. L'événement n'est jamais effacé : il garde son motif et la sévérité que le capteur avait posée. Hors fenêtre, ou sous un contenu étranger, le dépôt reste détecté — et c'est prouvé par mutation. Reste fermée par ailleurs : la croissance silencieuse du nombre d'unités livrées. |
| **P5.8-a** | Privilèges d'exploitation | 🔵🔒 | Une entrée sudo « bornée » ne vaut pas mieux qu'un octroi total tant que le code élevé appartient à l'utilisateur qui l'invoque : sudo résout le chemin à l'exécution, posséder le répertoire suffit. Le vrai obstacle est la **propriété du code élevé**, pas une ligne de configuration. Garde dérivée, filet et procédure sont livrés ; le geste appartient à l'exploitant. |

---

## P6 — Mémoire, performance, indexation

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P6.1-a** | Diagnostic mémoire | ✅ | Un indicateur de « prise sous swap » était dérivé du swap **système** alors que le service tourne avec le swap interdit : il mesurait les autres processus de la machine et marquait des relevés parfaitement sains. |
| **P6.1-b** | Budget mémoire | ✅ | Le budget mémoire était **publié** et rien ne l'imposait : le paramètre invoqué ne borne que le cache de pages, et le trieur n'a aucun budget. Fermé par une limite de tas dérivée du budget publié, qui vit dans l'allocateur : une agrégation trop large reçoit un refus explicite au lieu de tuer le processus. |
| **P6.2-a** | Généralité des mesures | ✅ | Un chiffre mesuré sur un profil prouve ce profil. La matrice rejouée montre que le nom des sources, le nom des champs, la taille d'événement, la cardinalité et le nombre d'hôtes déplacent les résultats de plusieurs ordres de grandeur — et que la distribution de sévérité, elle, ne les déplace pas. |
| **P6.8-a** | Index adaptatif | ✅ | Un champ indexé à chaud n'est pas compilé comme un champ chaud : comparé à un nombre, la requête porte une conversion que l'index d'expression ne peut pas apparier, et le plan dégénère de recherche en balayage. |
| **P6.8-b** | Index adaptatif | ✅ | Rien dans l'interface ne disait si l'indexation adaptative était active ni quels champs avaient été indexés. Mesuré avec contrôle positif, le mécanisme était actif et n'indexait rien : il a été retiré, une frontière de crate rendant sa chaîne de comptage morte pour toujours. |
| **P6.8-c** | Index et détection | ✅ | Le retrait de l'indexation adaptative a supprimé en fond trois index orphelins, dont deux servaient de la détection **active**. L'erreur n'est pas d'avoir mal mesuré : c'est d'avoir agi comme si un trou nommé était comblé. Deux champs entrent dans la liste chaude, la divergence entre crates devient inexprimable, et le troisième n'est pas restauré (aucun usage livré, cardinalité de 1). Reste à confirmer sur une base réelle que les index correspondants sont bien créés. |

---

## P7 — Hygiène, chaîne d'approvisionnement, surface d'autorisation

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P7.1-a** | Hygiène des tests | ✅ | Les fixtures nettoyaient en **énumérant** le chemin qu'elles avaient nommé, alors que SQLite en crée deux autres à côté. Le correctif possède le contenant et l'efface entièrement, donc y compris sur panique ; une garde de compilation a trouvé les sites que la conversion par motif avait manqués. |
| **P7.3-b** | Export | ✅ | L'export ne reprenait pas la sonde de plafond des agrégats et sortait « non tronqué » alors que le plafond avait mordu ; puis, une fois téléchargé, rien dans le fichier ne l'avouait. La marque est portée par le **nom** du fichier, le corps CSV et le tableau JSON servant des consommateurs qu'une enveloppe casserait. |
| **P7.3-c** | Export | ✅ | La marque de troncature ne disait pas l'**ampleur**. Elle porte désormais le nombre de lignes manquantes, ou « ampleur inconnue » quand la sonde ne l'a pas établi — jamais un repli sur zéro. |
| **P7.4-a** | Tier froid | ✅ | **Réfuté** : le constat annonçait des tranches froides antérieures à une migration qui ne touche pas le tier froid. Le format et la disposition des colonnes sont figés depuis l'origine ; une telle tranche ne peut pas exister. |
| **P7.5-a** | Avertissements de compilation | ✅ | La nature des avertissements résiduels était mal énoncée ; le seul qui signalait vraiment quelque chose a ouvert son propre constat, ci-dessous. Le décompte n'a pas été revérifié depuis. |
| **P7.7-b** | Chaîne d'approvisionnement | ✅ | Le contrôle de licences, de bans et de sources ne couvrait qu'un crate sur quatre — pas celui qui est installé sur les postes clients. La liste est désormais dérivée des manifestes trouvés, avec une garde d'instrument si la découverte en rend moins que prévu. |
| **P7.8-a** | Observabilité des requêtes | ✅ | Le constat annonçait **dix-neuf** routes sans métrique de latence, en avouant que le décompte n'avait pas été refait. **Refait le 2026-08-20 par dérivation depuis la source : dix-huit sites d'acquisition, qui servent vingt et un gabarits de route** — quatre sites vivent dans des fonctions d'aide partagées, d'où l'écart entre les deux nombres. Le sémaphore borne la ressource la plus contrainte du projet ; il est désormais mesuré **au point de passage unique** qui le délivre, donc sans une ligne de mesure dans aucune route, et une route ajoutée demain est comptée sans qu'on y pense. Deux grandeurs **séparées**, parce qu'un total les confond et désigne le mauvais levier : l'**attente** du permit (la file) et le **travail** permit en main. La **saturation** est publiée (acquisitions ayant dû attendre, permis détenus, taille de la borne). Cardinalité **bornée deux fois** — l'étiquette est le *gabarit* de route et jamais l'URL, et le registre est plafonné : au pire quarante-neuf valeurs d'étiquette, six séries chacune. Gardes dérivées prouvées par mutation : une acquisition hors du point de passage et un permit nu hors du module qui mesure sa détention sont refusés, en **nommant le site fautif**. Au passage, la garde d'origine a été **réfutée** : elle exigeait le nom du sémaphore et le mot « acquire » sur la MÊME ligne, et laissait donc passer la même acquisition écrite en deux lignes. |
| **P7.9-a** | SQL brut | ✅ | Un chemin acceptait du SQL brut sans trace là où son jumeau le refuse avec commentaire, audit et test. La décision est désormais **déclarée** et tracée au registre d'audit aux deux sites, et la documentation ne prétend plus l'uniformité que le code contredisait. |
| **P7.13-a** | SQL brut et autorisation | ✅ | La porte « SQL brut réservé aux administrateurs » tenait à l'**écriture** mais pas au **choix de ce qui s'exécute** : un compte éditeur pouvait faire exécuter un panneau d'administrateur et lire des lignes de la table des utilisateurs, puis figer le résultat dans un instantané partageable. Borne vérifiée : l'autorizer SQLite refuse les colonnes de secrets même à un administrateur. |
| **P7.14-a** | Pagination keyset | ✅ | Un commentaire promettait un repli silencieux quand la compilation d'une pagination par curseur échoue. Le drapeau correspondant n'était jamais consulté, le curseur restait armé sur un SQL de repli, et la requête produisait l'erreur que la phrase excluait. |
| **P7.15-a** | Provenance affichée | ✅ | Un panneau déclarait une provenance dont la moitié est une fonction jamais appelée : l'exploitant croyait lire un état d'indexation à l'exécution, il lisait une constante figée à la compilation. Une provenance fausse est pire qu'absente — elle fait cesser de chercher. |
| **P7.16-a** *(clé neuve)* | Ban natif : câblage | ✅ | Le ban d'une adresse est une promesse de **composition** — « toutes les routes d'un coup » tient à une couche du routeur, pas aux fonctions qu'elle appelle. Or toutes ses gardes étaient prouvées à la couture, sur des fonctions pures et des chemins choisis à la main : en retirant la couche, la suite entière restait verte. Une garde **dérivée** de la table de routage interroge désormais chaque route déclarée — plus les chemins servis par la fallback — à travers le routeur réel ; sous la même mutation elle en nomme 328 (route, méthode). Le verdict porte sur ce que la couche **écrit**, pas sur son code : une route d'ingestion rend le même 403 pour une clé de livraison invalide, et c'est le témoin négatif qui l'a montré. Les exemptions — sondes d'orchestrateur, valve de récupération — sont déclarées, justifiées, et vérifiées dans les deux sens. |
| **P7.16-b** *(clé neuve)* | Ban natif : borne mémoire | ✅ | La banlist vivante était une structure en mémoire **sans plafond**, alimentée entre autres par un chemin d'auto-ban optionnel : la croissance devenait pilotable par l'attaquant, à raison d'une entrée par adresse source. Elle est désormais bornée aux deux bouts — lignes lues et entrées construites — avec l'ordre de priorité de l'**enforcement** (bans permanents d'abord, puis les échéances les plus lointaines, départage stable). Au plafond, la pose est **refusée** avec un code explicite plutôt qu'enregistrée sans rien bloquer, et la saturation est publiée en métrique : une borne qui ne se dit pas est indistinguable d'une borne absente. |
| **P7.2 · P7.6 · P7.10 · P7.11 · P7.12** | — | ❓ | Numéros réservés : aucun constat ne les porte. |

---

## P8 — Chaîne de livraison : sauvegarde, restauration, gardes de dépôt

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P8.2-a** | Gardes de dépôt | ✅ | Les dépôts nus n'avaient que des hooks de **post**-réception, qui journalisent et ne peuvent rien refuser — et la ligne imprimée au push ressemblait à une garde ayant laissé passer. Une règle de pré-réception dérivée d'une partition fermée protège tout sauf ce qui est déclaré jetable, avec une sortie de secours auditable exigeant un motif. |
| **P8.2-b** | — | ❓ | Numéro réservé : aucun constat ne le porte. |
| **P8.3-a** | Restauration | ✅ | La vérification automatisée annonçait « déchiffre et ouvre la base » ; elle restaurait vers une base jetable qu'elle **n'ouvrait jamais**. Une archive parfaitement chiffrée, parfaitement rejouée, et **VIDE**, en sortait verte — mesuré par mutation : la garde retirée, la vérification rend `full_decrypt_verified` sur une archive à **0 ligne**. Elle rouvre désormais la base restaurée avec sa clé et en **compte le contenu** (tables dérivées du schéma, jamais énumérées) ; une restauration sans une seule ligne est un ÉCHEC. Reste hors de portée **délibérément** : le mode d'escrow, dont l'identité privée ne doit vivre ni près des sauvegardes, ni dans un test, ni dans l'intégration continue — l'exercice y est donc **hors ligne**, et c'est son ABSENCE qui était invisible. Une vérification complète réussie émet une **attestation** (une ligne de faits, aucun secret) que le nœud enregistre ; l'état daté qui en découle **vieillit**, se lit dans la santé par composant, dans `plume_restore_drill_overdue` et dans un événement SOC non purgeable, et refuse de compter un exercice mené sur le chemin symétrique pour une installation qui séquestre en asymétrique. Ce qu'il ne prouve pas, écrit pour être opposable : il défend contre l'oubli, pas contre une attestation recopiée à la main. |
| **P8.4-a** | Sauvegarde | ✅ | Un ratio de compression publié sans date. La chaîne incriminée avait déjà disparu du code ; ce qui restait vrai est l'écart entre l'archive et le fichier de base, qui se lit comme une perte de données si personne n'explique que la charge est un export **logique** — sans contenu d'index, sans tables d'index plein texte, sans pages libres, tous reconstruits à la restauration. Aucun ratio de référence n'est gravé dans le binaire : il dépend de la composition de chaque installation. |
| **P8.5-a** | Compteurs de tests | ✅ | La garde de compteurs de tests s'exécutait bien à chaque envoi ; ce qui manquait est la **boucle de retour** — son verdict s'affiche sur une page qu'on n'ouvre pas au moment de commiter. Un contrôle local énumère les tests, sans les exécuter, en quelques secondes. Il ne prouve pas qu'ils passent, et il le dit. |
| **P8.5-b** | Compteurs de tests | ✅ | La garde confondait deux mots français par une classe de caractères trop permissive et lisait des « passes » de fusion comme des tests « passés » : l'intégration continue publique était rouge. Témoins des deux sens ajoutés. |
| **P8.6-a** | Tests et mémoire | ✅ | Un test assertait sur la mémoire résidente du **processus** depuis une suite parallèle : vert ici, rouge là. Remplacé par une preuve par construction — un allocateur de test qui tient, par fil, l'écart entre alloué et libéré et son maximum — et par une invariance en volume à largeur constante, qui ne peut ni rater un pic ni voir celui d'un voisin. |
| **P8.6-b** | Sauvegarde et dérivation de clé | ✅ | La dérivation de clé de l'archive s'étalonnait au chronomètre à chaque sauvegarde, avec un coût mémoire dépendant du profil de compilation, et pouvait réserver une part importante du budget. Le plafond de **lecture**, recalculé sur la machine qui déchiffre, rendait en outre la restaurabilité dépendante de cette machine. Facteur fixé, borne de lecture fixe, réglage exposé pour qui utilise une véritable phrase de passe humaine. |
| **P8.7-a** | Configuration | ✅ | Sur le déploiement natif systemd, l'ordonnanceur lisait le fichier de configuration pour trouver la base mais ignorait le destinataire de séquestre écrit deux lignes plus bas : la grande majorité des variables n'étaient lisibles que depuis l'environnement. Router ces lectures est un sur-ensemble strict, jamais une bascule ; l'alternative par fichier d'environnement a été **rejetée** parce qu'elle exporterait des secrets dans l'environnement du processus. Un registre de dette ne peut plus que rétrécir. |
| **P8.7-b** | Chiffrement au repos | ✅ | Une clé écrite dans le fichier de configuration chiffrait le tier froid et laissait la base chaude **en clair**, sans un mot : deux voies de lecture distinctes pour la même clé. Voie unique désormais, fail-closed conservé, bascule annoncée au démarrage — et aucune valeur n'est jamais journalisée. |
| **P8.8-a** | Configuration hors dépôt | 🔒 | Un collecteur d'hôte tire à cadence courte contre des cibles qui n'existent plus, et il le **dit** : le défaut est dans la boucle de retour, pas dans l'instrument. Non corrigé délibérément — sa configuration vit hors de tout contrôle de version, et c'est cela le vrai défaut. |
| **P8.9-a** | Tenue de l'index | ✅ | L'index se trompait sur ses entrées les plus coûteuses, closes et déployées depuis deux jours. Cause structurelle : sans la clé dans le message de commit, aucune vérification ne peut relier un correctif à sa ligne. Le remède est en amont — citer la clé — et non une garde qui ne mordrait que sur les clés déjà citées, c'est-à-dire précisément pas sur celles qui dérivent. |
| **P8.9-b** | Tenue de l'index | ✅ | Le document pouvait se contredire sans que rien ne le dise : une clé réutilisée pour deux constats sans rapport, et une ligne périmée en double. La règle « une clé, une ligne » a été **mesurée fausse** — plusieurs clés nomment légitimement un thème. La règle retenue est celle que le document se donne à lui-même : une ligne qui se déclare neuve doit être la seule définition de sa clé. |
| **P8.10-a** | Reprise après sinistre | ✅ | Une reprise ne savait pas distinguer un échange de fichiers interrompu d'une copie de base périmée : son marqueur était l'existence d'un fichier, qui n'a pas de durée de vie. Le témoin est dérivé de la séquence d'échange, et le conteneur **refuse de démarrer** plutôt que de choisir à la place de l'humain entre « périmé » et « vide ». |
| **P8.11-a** | Déploiement piloté par dépôt | ✅ | Dans un déploiement piloté par dépôt, modifier un manifeste surveillé **est** un déploiement. Un hook de pré-envoi refuse désormais un envoi qui touche ces chemins sans se déclarer ; le périmètre est dérivé des applications déclarées, pas d'une énumération. Limite assumée : la configuration de hooks est locale à un clone, donc à réinstaller après clonage. |
| **P8.12-a** | Verrou de déploiement | ✅ | Un fichier de verrou persistant prend le propriétaire du premier qui l'a créé : une seule exécution privilégiée rendait la voie non privilégiée définitivement inaccessible. Le remède **préserve l'inode** au lieu de supprimer le nom — le verrou porte sur l'inode ouvert, supprimer ne libère rien et ferait croire à un lancement qu'il est seul. |
| **P8.13-a** | Démarrage et volume | ✅ | « Volume neuf » se déduisait de la seule absence du fichier de base : un démon pouvait repartir sur une base **vide**, en silence. Pour un SOC, perdre l'historique sans un mot est pire que ne pas démarrer. Le critère est dérivé de l'état complet du volume, tout résidu prouvant qu'il a servi. |
| **P8.14-a** | Communication publique | ✅ | Une garde automatique confrontant les technologies annoncées publiquement à celles qui tournent réellement a été **refusée**, avec sa raison : une page de présentation a le droit de simplifier, et un automate qui la surveillerait ajouterait une dépendance et une surface d'attaque pour un gain rédactionnel. La correction ponctuelle est faite ; le contrôle reste humain, à la relecture. Écrit pour que la garde ne soit pas reproposée comme un manque. |
| **P8.15-a** | Historique public | ✅ | L'historique et les documents publiés s'adressaient à un lecteur interne et portaient une identité personnelle. Messages réécrits à la voix impersonnelle en conservant mesure, réfutation et clé ; identité de projet posée ; deux gardes versionnées refusent une identité non canonique et les familles objectives de fuite. Le **style** n'est pas mécanisé — une garde qui prétendrait juger le ton ferait du bruit et finirait désarmée. INTEGRATION CONTINUE VERTE SUR LES DEUX DEPOTS, ce qui clot la campagne. SEPT causes distinctes l'en separaient, et aucune n'etait celle annoncee en premier : un verrou epingle sur une revision que la reecriture avait supprimee, avec des tags publies pointant sur des commits orphelins ; un outil d'audit incapable de lire un format d'avis recent, qui echouait AVANT de scanner et rendait donc sans effet les correctifs de vulnerabilite ; deux avis de dependance dont un inatteignable ; deux valeurs-temoins de test prises pour des secrets ; une course sur des reglages partages entre tests, intermittente donc lue comme un alea ; un marqueur de vocabulaire disparu d'un document refondu puis remis sans son accent ; et une garde qui se comptait elle-meme, protegee par un controle positif satisfait du meme faux positif. CINQ hypotheses ont ete REFUTEES par la mesure en cours de route, chaque fois pour moins cher que le correctif qu'elles auraient fait poser. RESTE, comme COUT et non comme defaut : le job de tier froid dure environ 45 minutes. |
| **P8.16-a** | Reecriture et consommateurs | ✅ | Reecrire l'historique d'une bibliotheque deplace tous ses SHA derriere des noms de tags inchanges. Deux consequences ont ete mesurees APRES coup, et c'est le defaut : le verrou du consommateur epinglait la revision par SHA et cargo echouait sur « revision not found », bloquant compilation, tests et tier froid ; et les tags publies, pousses AVANT deux reecritures ulterieures qui n'ont force-pousse que la branche par defaut, designaient des commits ORPHELINS hors de l'histoire publiee. REGLE : une reecriture repousse ses TAGS dans le meme geste, et tout consommateur epinglant un SHA est remis a jour dans la foulee. Correctif verifie par MUTATION : le job de verification par fonctionnalite est passe de ROUGE a VERT. |
| **P8.17-a** | Avis de securite des dependances  | ✅ | Deux avis remontes par l'audit de dependances, aucun lie a une modification du depot. h2 0.4.15 (RUSTSEC-2026-0258, avis du 2026-08-17) se corrige par un bump de patch vers 0.4.16. rkyv 0.7.46 (RUSTSEC-2026-0235) demande un SAUT MAJEUR vers 0.8.17, probablement via le parent du tier froid colonnaire : l'atteignabilite du correctif depend de ce parent, et se mesure avant d'agir. | FERMEE : h2 corrige par bump de patch, la crate etant transitive le verrou suffit. rkyv reste, correctif INATTEIGNABLE — son parent declare une dependance NORMALE compatible 0.7 seulement, et epingler la version corrigee echoue a la resolution ; surface NULLE par ailleurs, la fonctionnalite qui le tire n'etant activee par personne et l'arbre toutes fonctionnalites confondues ne rendant rien. L'ignore porte sa date et sa CONDITION DE LEVEE. Ce correctif ne pouvait a lui seul rendre le job vert : la cause etait le chargement de la base d'avis, traite par la cle suivante.
| **P8.18-a** | Outil et base d'audit non epingles | ✅ | PERIMETRE CORRIGE DEUX FOIS. Il a d'abord ete cantonne a un depot, puis elargi a tous : les DEUX cadrages etaient faux. Le job porte DEUX controles sous un seul nom, et ils echouent pour des raisons DIFFERENTES selon le depot — un nom de job qui recouvre deux controles rend son verdict illisible. L'audit de dependances n'atteint meme pas l'etape de scan : la base d'avis contient une entree en CVSS 4.0 que la version installee ne sait pas lire, et l'outil s'arrete sur une erreur d'analyse. Ce n'est ni une vulnerabilite ni une regression du code — c'est un outil non epingle qui derive de sa base. Ni l'outil ni la base ne sont epingles : l'outil est installe en derniere version publiee, la base est recuperee telle qu'elle est au moment du run. Un verdict de porte peut donc changer sans qu'une seule ligne du depot ait bouge — c'est la chaine d'approvisionnement de la GARDE elle-meme qui n'est pas tenue. Corollaire MESURE : un correctif de vulnerabilite et une entree d'ignore, tous deux justes, ne peuvent pas rendre ce job vert, puisqu'il echoue AVANT de lire la liste d'ignore. Correctif : epingler l'outil ET la revision de la base, et nommer les deux. | FERMEE par l'epinglage de l'outil en version capable de lire le format CVSS 4.0. TROIS CORRECTIONS AU CONSTAT : l'hypothese que le verrouillage des dependances de l'outil etait en cause est REFUTEE — c'est la VERSION ; un depot epinglait une version trop ancienne et echouait, l'autre n'epinglait RIEN et passait deja, son defaut etant l'absence d'epinglage et non l'echec ; et la base compte 59 entrees dans ce format, le defaut est donc systemique et croissant, pas lie a une advisory. On epingle l'OUTIL, dont on veut un comportement reproductible, PAS la base, dont on veut le contenu frais : l'epingler rendrait la porte aveugle a toute advisory posterieure. Verifie par mutation dans les deux sens, et par contre-epreuve : audite sans sa liste d'ignore, le depot enumere exactement les entrees qu'elle couvre.
| **P8.19-a** *(clé neuve)* | Secrets signales dans l'arbre et l'historique | ✅ | Le controle de fuites signale DEUX trouvailles sur 159 commits scannes, et il echoue depuis le premier run — elles PRECEDENT donc la reecriture des messages. La configuration du controle porte une liste d'exceptions pour des fixtures de test connues : soit ces deux trouvailles sont hors de cette liste et sont reelles, soit la liste a un trou. TANT QUE LES DEUX NE SONT PAS NOMMEES, AUCUNE DES DEUX HYPOTHESES NE VAUT — et l'outil n'etant pas disponible hors integration continue, il faut le journal du job ou une execution locale pour les designer. Priorite haute : depot public. | FERMEE. Les deux trouvailles sont des VALEURS-TEMOINS, tranchees sur le corps des fichiers et non sur leur chemin : une cle de banc employee uniquement sur des bases jetables que le banc fabrique lui-meme, et une chaine qu'un test de confidentialite insere PUIS asserte ABSENTE — c'est la sonde d'une garde. L'allowlist porte sur les deux VALEURS exactes, ligne a ligne : ni fichier, ni regle, ni chemin elargi, une liste trop large etant une garde desarmee. CORRECTION D'UNE MESURE : un premier scan local rendait 10 trouvailles au lieu de 2 ; l'ecart ne venait PAS d'une config differente mais du PERIMETRE — un scan local lit tous les depots distants par defaut, dont un depot interne anterieur a la publication. Rien de ces 8 supplementaires n'est publie. Preuve par mutation dans les deux sens, dont l'insertion de chaines ressemblant a de vrais jetons JUSTE A COTE des lignes allowlistees, toutes retrouvees.
| **P8.20-a** *(clé neuve)* | Reglages de sauvegarde partages entre tests | ✅ | Les reglages de sauvegarde sont relus dans l'environnement PROCESS-global a chaque sauvegarde, et les tests d'un meme binaire partagent ce processus. Des tests les mutaient pendant que d'autres les lisaient : l'un forcait un chemin d'ecriture chez son voisin, un autre faisait REFUSER sa sauvegarde. L'echec etait INTERMITTENT — 2 tirs sur 5 — et se lisait donc comme un alea alors qu'il nommait une course. Corrige par un verrou lecteurs/ecrivain qui n'exclut que la fenetre de mutation, et une garde de portee qui RESTAURE la valeur anterieure meme lorsqu'un test panique : les retraits ecrits en ligne droite etaient sautes par le deroulement de pile et laissaient le reglage en place pour tout le reste du binaire. Ferme par une garde DERIVEE des sources de production plutot que par quatre correctifs — elle a trouve un declencheur qu'une recherche par nom aurait manque. Verifiee par mutation dans les deux sens. |
| **P8.21-a** | Garde des temporaires qui se comptait elle-meme | ✅ | Le job nomme son repertoire temporaire avec le PREFIXE que cette garde recherche, et l'enumeration ne posait pas de profondeur minimale : la recherche rendait donc aussi le repertoire lui-meme, compte comme residu. La garde echouait sur un repertoire VIDE, a chaque execution. CE QUI L'A CACHE EST LE PLUS INSTRUCTIF : son propre CONTROLE POSITIF comptait les correspondances au lieu de chercher son temoin par son NOM, et se trouvait donc satisfait par le meme faux positif — l'instrument se validait a l'aide du defaut qu'il devait exclure, et son « controle positif : OK » ne prouvait rien. Le defaut est en outre reste invisible tant que des echecs anterieurs dans le meme job empechaient d'atteindre cette etape : CORRIGER UN DEFAUT EN REVELE UN AUTRE QUI ATTENDAIT DERRIERE. Corrige par une profondeur minimale sur les quatre enumerations et un controle positif qui exige SON temoin nomme ; verifie dans les deux sens. |
| **P8.22-a** *(clé neuve)* | Destination objet de la sauvegarde | ✅ | L'ordonnanceur de sauvegarde ne savait écrire que sur un système de fichiers : une destination objet était détectée et refusée, et l'orchestration hors du nœud restait à un outil externe. Elle est désormais tenue par le binaire, sous une fonctionnalité de compilation ÉTEINTE PAR DÉFAUT qui n'ajoute **aucune dépendance** — la signature de requête est de l'arithmétique HMAC-SHA256 et SHA-256, déjà présente, et le transport réutilise le client HTTP interne. Les deux bibliothèques candidates ont été MESURÉES avant d'être écartées (2026-08-19) : la trousse complète du fournisseur historique ajoutait 103 caisses au graphe, dont un outillage de compilation que ce dépôt déclare absent de l'hôte visé ; la bibliothèque tierce généraliste en ajoutait 44, dont une seconde pile HTTP et un second jeu de racines. CE QUI FAIT L'OBJET DU LOT n'est pas l'envoi, qui est banal, mais son VERDICT : un dépôt n'est annoncé que si le service a répondu favorablement ET qu'une relecture rend la même taille ; toute autre issue se lit « refusé » (le service a répondu non) ou « impossible » (aucun verdict), jamais succès, et l'archive locale est conservée. Les quatre portes par lesquelles un envoi raté aurait pu ressortir en succès sont exercées contre un service factice local, et la propriété est vérifiée par MUTATION : annoncer le dépôt dès la réponse favorable fait rougir trois tests. LIMITES NOMMÉES : pas de téléversement en plusieurs parties (donc un plafond par archive), pas de rétention côté dépôt distant (règle de cycle de vie du bucket), pas de restauration depuis l'objet, pas d'identifiants par rôle d'instance. |
| **P8.23-a** *(clé neuve)* | Cout d'execution de la suite du tier froid | ✅ | Le job d'integration continue du tier froid durait environ 45 minutes. L'HYPOTHESE DU CACHE DE COMPILATION EST REFUTEE : une compilation totale depuis un cible vide coute 279 s, quand l'EXECUTION des tests en coute 2393, soit 90 % du job — le cout n'etait pas dans le build. Cause reelle : les DEPENDANCES etaient baties sans optimisation, donc le decodage colonnaire, la compression et le moteur chiffre, exactement la ou cette suite passe son temps. Une derogation de profil appliquee aux seules dependances rend 2393 s -> 590 s, mesure en A/B sur le MEME arbre source, les deux verts, avec temoins verifiant que les caisses tierces recoivent l'optimisation et que le binaire de test n'en recoit AUCUNE. Ecartes sur mesure : ne pas rejouer les tests du job par defaut (au mieux 198 s sur 2393, et cela DETRUIRAIT l'assertion de compte que ce job existe pour porter) et le decoupage en tranches (multiplie les minutes sans reduire le travail). LES COMPTEURS SONT INCHANGES : c'est la preuve que la couverture l'est aussi. Ne rend rien plus rapide en production — le profil de livraison est intact. |

---

## P10 — Tenir sous 2 Gio : stockage, index, agrégation

| Clé | Périmètre | État | Ce que la clé désigne |
|---|---|---|---|
| **P10.1-a** | Confidentialité du tri | ✅ | Le déversement des tris sur disque était livré **inconditionnellement**. Il fait exister un plafond de tri, mais il l'obtient en écrivant des valeurs d'événement en clair hors de la base chiffrée : SQLCipher chiffre le fichier, pas les temporaires de SQLite. Le défaut est revenu en mémoire ; le déversement est un échange qui se prend **explicitement**. Reste ouvert : aucun quota ne borne la taille du déversement quand il est activé. |
| **P10.2-a** | Composition de la base | ✅ | Le produit devait tenir sous 2 Gio et ne savait pas dire ce qui remplit sa propre base ; l'outil rendait de surcroît un rapport de **zéros** quand la base était illisible, ce qui se lit « base vide » et non « lecture impossible ». Désormais fail-closed, classification dérivée du schéma, refus de publier si la comptabilité ne ferme pas, et ce qui n'est pas classé est imprimé. |
| **P10.2-b** · **P10.0** | — | ❓ | Numéros réservés ou cités sans définition ; aucun constat ne les porte. |
| **P10.2-c** | Schéma et démarrage | ✅ | Une tâche de fond supprimait deux index que le schéma **recréait à chaque ouverture de connexion d'écriture**, de façon synchrone avant l'ouverture du service : chaque démarrage les reconstruisait sur toute la table, la tâche les supprimait, le démarrage suivant recommençait. Ce qui rendait la boucle invisible est l'affirmation, écrite dans le code, que le schéma ne les déclarait plus. La garde lit les suppressions et vérifie qu'aucune n'est recréée. Volet jumeau : une liste de champs chauds dupliquée dans deux crates sans lien de compilation, dont la divergence est devenue inexprimable. |
| **P10.2-d** | Index inutiles | ✅ | Neuf index que le schéma posait sans qu'aucune requête en ait besoin — doublons exacts d'une contrainte et préfixes stricts —, établis avec l'instrument de SQLite lui-même et prouvés par mutation, le planificateur nommant l'index subsumant. Le résidu associé s'est révélé une **réfutation** : sa prémisse sur les statistiques d'index était fausse, et le filet invoqué pour minorer le risque n'existe pas. |
| **P10.2-e** | Série de composition | ✅ | La composition de la base n'existait qu'en relevé ponctuel, coûteux et donc jamais répété : impossible de dire si une part croît ou si le fichier grossit à composition constante. Elle est désormais écrite périodiquement dans la table de métriques du produit — sans HTTP, sans jeton — et lue par le langage de requête existant. Quand la comptabilité ne ferme pas, la série porte un **trou** nommé, jamais un zéro d'octets. |
| **P10.5-a** | Observabilité du vieillissement | ✅ | Le passage au tier froid était muet en **succès**, donc indiscernable d'un démon arrêté, et plusieurs chemins d'échec n'écrivaient que sur la sortie d'erreur. Trois états sont portés par le type — passe faite avec compteurs même à zéro, passe suspendue avec cause nommée, aucun point du tout — avec un invariant qui refuse de publier plutôt que de publier un compteur faux. Un piège trouvé en chemin : la requête de découverte avalait ses erreurs, et une instrumentation naïve aurait publié « zéro candidat » pour une passe qui n'a rien pu regarder. |
| **P10.6-a** | Sauvegarde en flux | ✅ | La sauvegarde compressée était déjà en flux, contrairement au constat de départ. Ce qui manquait : le balayage des temporaires en clair était devenu du code mort, la ligne opérateur annonçait un format faux, et rien ne prouvait que la zone de préparation reste vide ni que la fidélité survive aux données binaires. Contrepartie nommée : la restauration reconstruit index et index plein texte, ce que le délai de reprise doit provisionner. |
| **P10.6-b** | Reprise après sinistre | ✅ | Le vieillissement retire du chaud les lignes déjà scellées en froid : suivie à la lettre, la procédure de restauration ne rendait que la fenêtre chaude et déclarait le succès. Il n'y avait pas de perte — les fichiers froids sont séquestrés — mais rien dans le chemin de restauration ne le savait. Le verdict nomme désormais sa portée. |
| **P10.6-c** | Fraîcheur des sauvegardes | ✅ | Le test de restauration validait l'artefact le plus récent quel que soit son **âge** : une sauvegarde bloquée aurait fait revalider le même artefact périmé chaque jour, en vert. Le seuil est dérivé de la cadence **observée** des dépôts, le test n'ayant délibérément aucun accès à la configuration. |
| **P10.7-b** | Index plein texte | ✅ | Sortir un événement de la fenêtre chaude faisait **grossir** l'index plein texte : sur une table à contenu externe, la suppression écrit un enregistrement qui s'ajoute, et l'espace n'est rendu qu'à la fusion — qu'aucun code n'appelait jamais. Une compaction incrémentale rend l'espace par passes courtes ; la variante en une seule opération tient le verrou d'écriture bien plus longtemps et gonfle le journal d'écriture anticipée. Les octets sont rendus à la liste libre, pas au système de fichiers. |
| **P10.8-a** | Ordre des leviers | ✅ | L'ordre des leviers d'échelle était dérivé d'un banc et non d'une installation réelle, et aucune série n'était conservée. La composition est désormais mesurée périodiquement, avec un coût borné **par construction** (le sommeil suivant est dérivé de la durée du parcours précédent), et l'ordre des leviers est re-dérivé de parts réelles au lieu d'être promis. |
| **P10.9-a** | Usage des index | 🔵 | On ne savait pas quels index servent, alors que l'index b-tree est un poste majeur du fichier. La mesure est un instrument, pas un verdict : **aucun index n'a été retiré**. Le trou nommé — l'absence de statistiques d'index détaillées — est comblé par les deux bouts. Le rejeu du corpus livré porte désormais un régime où la base est peuplée aux **distributions mesurées** (proportions et taux de nul, pas seulement des cardinalités), analysée pour de vrai, échantillons par index compris, puis remise à l'échelle du volume ; et le produit sait lire le plan **à l'exécution**, là où le planificateur dispose des statistiques réelles, en comptant par index et par classe de consommateur — éteint par défaut, cardinalité plafonnée par construction, et la portée du chiffre écrite dans la série elle-même plutôt que dans un commentaire. Ce que les statistiques détaillées changent est un **résultat, pas un détail** : un index déclaré employé par aucune requête livrée apparaît, un autre déclaré très employé s'effondre — les verdicts pris sans elles n'étaient pas représentatifs. Le candidat unique, lui, reste à zéro sous les quatre régimes. Ce que la mesure ne couvre toujours pas est déclaré : le corpus livré ignore l'ad hoc analyste, et l'observation d'exécution ne vaut que pour la charge observée. Le retirer resterait une décision, jamais une conséquence. |
| **P10.10-a** | Carte des leviers | ✅ | Un défaut de build passager avait été promu en état permanent de conception, et l'ordre des leviers en avait été dérivé : le tier froid n'a jamais été « construit mais éteint » en dehors de cette fenêtre. Mesuré, le vieillissement rend beaucoup d'espace d'un coup — lignes, quote-part d'index et quote-part d'index plein texte partent ensemble — mais l'activité reconsomme la liste libre plus vite qu'il ne la remplit : **il freine la croissance du fichier, il ne l'annule pas**. |
| **P10.11-a** | Latence sous vieillissement | ✅ | L'attente d'une requête était rendue **dans la réponse** de cette requête, donc corrélable par rien. Elle est désormais publiée en série, sur la même échelle de temps que la présence d'une passe de vieillissement — la question « cette requête était-elle lente parce qu'une passe tournait ? » se lit sur deux points de même horodatage. La forme a été choisie **par la mesure** : sur banc, la moyenne sous-estime le pire échantillon d'un facteur 73, et le quantile à 99 % est lui-même **aveugle** au régime le plus proche de l'exploitation — il rend zéro là où un analyste a attendu. La série porte donc des **seaux** et un **maximum**, les deux seules formes qui survivent à l'agrégation horaire. Ce que la série publie n'est plus l'attente du verrou seule mais la **somme** des deux files qu'une requête traverse, permit compris : les deux intervalles sont disjoints par construction, et le terme du permit est le terme majoritaire dès que la borne interactive sature derrière la passe. Ce que la mesure ne dit pas voyage avec elle, dans son `# HELP` : elle reste une **borne inférieure** du coût pour un analyste (seules les routes qui portent l'horloge de requête sont comptées, et un travail simplement ralenti n'est pas de l'attente), la présence de passe qu'elle publie est une borne **supérieure** de la détention du verrou et non une durée de verrou, et son accumulateur est de processus, pas par tenant. |
| **P10.12-a** | Vocabulaire des compteurs | ✅ | Une passe qui ne faisait **rien** était comptée comme une passe qui avait travaillé, tout en coûtant du temps réel et du CPU. Le vocabulaire distingue désormais les deux. Le résidu de lignes chaudes sur des jours déjà scellés est le compromis « traînards », assumé, testé et borné par la purge finale : ces lignes restent visibles et interrogeables, sans perte. |
| **P10.13-a** | Cadence du détecteur | ✅ | Un détecteur horaire balayait la table d'événements entière et gelait la base pour découvrir une poignée de lignes de travail, alors qu'une seule passe par jour peut avoir quelque chose à faire. Le pari d'une réécriture de la requête a été posé, mesuré, puis **perdu** : le coût suit la taille de la table, pas la forme de l'énoncé. Le levier retenu est la cadence — elle ne supprime aucun travail, elle supprime les passes qui n'en avaient aucun, sans changer ce qui est détecté. L'état de cadence vit dans la base, donc il survit à un redémarrage ; la condition de tir est de niveau et non de front, donc un tir sauté retarde l'alarme sans jamais la manquer. |
| **P10.14-a** | Gardes de promesses | ✅ | Deux lots consécutifs avaient livré un chemin d'échec non gardé : ce sont des promesses écrites **quand le code est juste**, donc invisibles au compilateur comme à une relecture. Une garde dérivée exige de chaque cause publiée une citation capable de la **distinguer** ; le seuil a été tranché par mesure, la règle intuitive n'attrapant rien. Limite déclarée : la garde prouve une condition nécessaire, pas que le test exerce le chemin émetteur — la couverture, elle, se mesure par mutation. |
| **P10.15-a** | Fiabilité d'une sonde | ✅ | Une sonde de diagnostic présentait des durées mesurées **à froid** comme le prix de la passe réelle, avec des écarts de plusieurs ordres de grandeur d'une exécution à l'autre selon l'état des caches. Chaque énoncé est désormais mesuré deux fois, seule la mesure à chaud est annoncée comme **plancher** — un majorant qui varie n'est pas un majorant —, et le type interdit au compilateur de republier une durée nue. La sonde consomme de la mémoire, ce qui compte sous 2 Gio, et sa sortie le dit. |
| **P10.16-a** | Empreinte du journal d'écriture | ✅ | La **crête** — ce que le journal atteint PENDANT une rafale — n'est bornable par aucun réglage du moteur : elle ne suit pas le volume écrit mais la durée pendant laquelle un lecteur refuse le point de reprise. Mesurée au banc à **57 % de la taille de la base**, et **inchangée** par la borne posée — elle tombe dans l'écart de deux mesures du témoin entre elles. Ce qui est borné est le **résidu** : sans borne, le fichier garde sa plus haute crête indéfiniment, mesuré à **270 % de la taille de la base** quand un lecteur ne relâche jamais. La borne est **dérivée** — le plus gros lot d'ingest acceptable, plus la réserve d'auto-checkpoint : elle suit ces deux leviers au lieu d'être un nombre rond — configurable, et l'ancien comportement reste accessible. Le levier voisin, abaisser le seuil d'auto-checkpoint, a été mesuré puis **écarté** : +7 % d'appels d'écriture pour aucune réduction, ni de la crête ni du résidu. Ce que la borne rend est du **disque** et du temps de gel aux points de reprise (une droite à ~0,32 ms par Mio de journal), **pas de la mémoire** — l'index du journal pèse ~2 Kio par Mio, et prétendre autre chose serait un chiffre annoncé que rien ne tient. **Ce que le banc n'a pas pu trancher, et qui est publié comme tel** : le débit. Le témoin, mesuré deux fois, s'écarte de lui-même de 17 % ; seul un majorant est donné (au plus +3,6 % de CPU par événement, comparaison des minima), et le travail d'écriture ajouté par la borne est de +0,13 % d'appels système — sous l'écart des témoins entre eux. |
| **P10.17-a** | Point de contrôle du journal | ✅ | Les appels de troncature du journal d'écriture anticipée étaient aveugles à leur propre verdict : ce PRAGMA ne rend pas un code d'erreur mais une **ligne**, et un lecteur concurrent fait échouer la troncature en silence. Voie unique, verdict à trois états nommés, journalisation du refus, garde qui refuse le PRAGMA hors de cette voie. Aucun changement de comportement : on ne réessaie pas et on ne force aucun verrou — la base d'un SOC ne doit pas geler pour une troncature refusée —, on rapporte. |

---

## Constats sans clé encore ouverts

| Réf. | Périmètre | État | Ce que la référence désigne |
|---|---|---|---|
| S4 | Multi-tenant | ✅ | La génération de clé par tenant gardait un repli d'entropie dérivé de l'horloge, du processus et d'une adresse de pile, hachés ensemble : cette clé chiffre la base **entière** d'un tenant, et qui connaît approximativement l'instant de création énumère ce qui reste, pour toute la durée de rétention de la donnée. Le repli **avertissait** — un avertissement se lit une fois, une clé faible reste faible. Le correctif suit celui déjà fermé sur le chemin d'installation : un **producteur unique** à deux sources réelles de l'OS (le périphérique aléatoire, puis l'appel système d'entropie **sans descripteur de fichier**), et aucune troisième voie. **Ce que le repli couvrait n'est pas perdu** : le cas réel visé — arborescence de périphériques absente, en environnement changé de racine ou en conteneur minimal — est exactement celui que la seconde source sert, et les trois modes de déploiement revendiqués l'offrent. Sans entropie, **aucun tenant n'est créé** ; la voie explicite d'une clé pré-approvisionnée, qui ne dépend pas de l'entropie locale, reste ouverte. Recherche **dérivée d'une propriété** plutôt que d'un nom de fonction, mesurée le 2026-08-20 : sur les **onze** fonctions de production qui puisent à une source d'entropie de l'OS et rendent de la matière, **deux** retombaient sur l'horloge — la clé de tenant, et l'identifiant d'engagement, dont le repli horodaté pouvait de surcroît **collisionner** sur sa clé primaire alors que la fonction voisine refusait déjà de servir sans entropie. L'identifiant de plan de contrôle, qui tirait sa matière de la même fonction, hérite du refus. Preuve par mutation dans les deux sens (aucune matière → aucune clé ; source disponible → la clé **est** cette matière), contrôle de structure sur un lot de clés dont l'instrument est éprouvé en dénonçant un générateur à compteur, et garde dérivée dont la partition est la **propriété** ci-dessus, jamais une liste de noms. |
| S5 | Bancs | ✅ | Des références mortes à l'indexation adaptative retirée subsistaient dans les scripts de banc et leur documentation. Relevé le 2026-08-20 : **vingt-cinq lignes sur huit fichiers**, ramenées à quatre — celles qui consignent le retrait au passé. Trois références étaient VIVANTES — le lanceur exportait encore le réglage disparu, et une phase entière générait du trafic puis attendait un tick avant de publier une passe étiquetée « mécanisme actif », étiquette qu'aucun code ne pouvait plus rendre vraie ; la sonde relevait quatre réglages que le daemon ne connaît plus, et rendait donc quatre `null` indiscernables d'un daemon muet. Le rendu chiffré, lui, énumérait **dix** champs indexés là où le code en porte **douze** — `dir` et `risk` y sont entrés avec P6.8-c — et renvoyait à un plafond de réglage qui n'existe pas. Le lanceur et la sonde ne portent plus le mécanisme ; la liste des champs indexés est désormais **lue dans le code au moment du rendu**, qui refuse de produire le document plutôt que de republier une liste périmée. Ce qui documente le RETRAIT est conservé et écrit au passé — y compris la passe de mesure datée qui montre le mécanisme n'indexant rien, et la clé de configuration `autoindex_deny`, renommée d'après ce qu'elle décrit vraiment : des champs à forte cardinalité qu'il ne faut pas envisager d'indexer. |
| S6 | Surveillance d'intégrité | ✅ | Le collecteur d'intégrité **annonçait** surveiller les clés SSH autorisées — un vecteur de persistance, livré comme détection — que le durcissement lui retirait **en silence** : la protection des répertoires personnels est un réglage **scalaire**, et le fragment de durcissement partagé, lu *après* l'unité, écrasait la valeur plus permissive que l'unité posait exprès. Mesuré, capteur exécuté tel quel : la référence d'intégrité perd **exactement une famille**, sans erreur ni avertissement — et les deux installeurs ne s'accordaient même pas, le central couvrant ce que l'agent laissait aveugle. Le réglage appartient désormais à chaque unité, le fragment partagé ne le pose plus pour personne, et une garde **dérivée** de la liste annoncée refuse tout chemin annoncé devenu injoignable. **Ce que le correctif ouvre, dit franchement** : ce collecteur peut lire l'intégralité des répertoires personnels, pas seulement les clés ; l'écriture reste refusée et les répertoires d'exécution de session restent masqués, aucune re-exposition d'un sous-chemin n'étant possible sous la protection stricte (mesuré : aucune des trois directives candidates n'y parvient). Resserrer ce bac à sable reste un arbitrage d'**exploitant** — qui le resserre verra désormais le capteur **avouer** son angle mort dans sa sortie au lieu de se taire. |
| S7 | Alertes de fraîcheur | ✅ | L'alerte de capteur indisponible était **globale** : elle ne faisait basculer l'état d'aucune source, parce que l'imputation cherchait un nom de source **dans le texte de la règle** — et qu'une règle écrite générique, comme le principe vendor-agnostic le demande, n'en porte aucun. Mesuré le 2026-08-20 sur le contenu livré : **onze des quarante-sept règles** ne portent aucun nom de source dans leur texte, dont **quatre actives par défaut**, et ce sont exactement les règles normalisées ; côté capteurs, **les vingt-trois** étaient hors d'atteinte de cette voie, leur détail étant une phrase et non une requête. L'imputation vient désormais de la **donnée** : la colonne source des événements appariés pour une alerte de règle, le descripteur typé de la sonde pour un capteur muet. Elle est **écrite sur l'alerte** au moment où elle est levée, donc la surface de lecture ne devine plus rien. L'alerte globale est **inchangée** — même compte, même titre, même détail, même clé de déduplication : ce qu'elle gagne est une colonne. Une source dont le nom n'est **pas déterminable** le dit et se compte à part, plutôt que de retomber en silence sur l'état global — un inconnu nommé vaut mieux qu'une imputation fausse. Preuve par mutation dans les deux sens : une source muette fait basculer **sa** pastille et aucune autre ; deux sources muettes en font basculer **deux**, sous une seule alerte. Les alertes antérieures à la migration gardent le repli textuel, à l'identique. |
| S16 | Comptage des agrégats | 🔵 | Un sur-comptage sur l'une des routes d'agrégat est **laissé délibérément**, avec sa raison : le corriger reviendrait à effacer de la réponse des événements réels sur le seul chemin qui les compte encore. |
| S24 | Documentation | ✅ | Le document de conception qui porte l'état le plus frais des travaux d'échelle n'était référencé par aucun index — il n'était **nommé** qu'en texte brut, dans la dernière phrase de cet index-ci. Le trou était général et non particulier : mesuré le 2026-08-20, **onze des quarante-cinq documents suivis** n'étaient la cible d'aucun lien, dont l'agent d'endpoint, sa validation, le collecteur Windows, le récepteur syslog, la détection mail, la voie eBPF/Sigma et le harnais de mesure — tous finis, tous exacts, tous introuvables autrement qu'en listant le dépôt. Les huit sont entrés dans l'index de la documentation (dont une section « collecter aux extrémités » qui manquait), et une garde **dérivée** refuse désormais qu'un document suivi soit inatteignable depuis la page d'accueil par un chemin de liens. Elle n'énumère aucun document et une **mention** ne la satisfait pas : c'est précisément la forme sous laquelle le document d'échelle passait pour référencé. Trois fichiers de la racine restent hors de la règle parce qu'une plateforme les ouvre par leur nom, et l'exemption elle-même est vérifiée. |
| S25 | Allegation de bac a sable non reproduite | ✅ | Le code d'installation de service portait, **datée et donc crédible**, une affirmation que rien ne vérifiait : une directive de re-exposition rendrait de nouveau utilisable un chemin placé sous protection du répertoire personnel. **RE-MESURÉE** le 2026-08-20 (systemd 261, unités transitoires, une seule variable à la fois, témoin positif ET négatif) : elle ne se reproduit pas — **aucune** des quatre directives candidates (`ReadWritePaths=`, `BindPaths=`, `BindReadOnlyPaths=`, `ReadOnlyPaths=`) ne ramène un chemin ainsi protégé ; l'exécution échoue à l'identique avec et sans elles (203/EXEC), et ne réussit que lorsqu'aucune protection n'est posée. **La conséquence était pire que celle annoncée** : sur le chemin de données, l'unité **DÉMARRE** — aucun code d'erreur, ni 203 ni 226 — et le service se voit refuser sa **première écriture**. La fonction de décision ne conclut plus sur la foi de l'allégation : l'échappatoire est retirée, et le champ qui la portait est remplacé par le **mode d'accès** dont chaque chemin a besoin — un fait qui est désormais **exercé** plutôt qu'affirmé. Car une table de préfixes écrite d'avance ne peut pas décrire le bac à sable de tous les hôtes : l'installation **monte réellement** ce bac à sable avant d'écrire l'unité et y teste les quatre chemins dans leur mode. Quand cette mesure ne peut pas être faite, elle **le dit** au lieu de valoir feu vert — c'est une limite assumée, pas un silence. Preuve par mutation dans les deux sens : rétablir l'allégation dans la table, ou seulement dans la décision qui l'utilise, fait **rougir** un test qui nomme le chemin laissé passer ; et un témoin montre qu'une spec conventionnelle reste installable. Le pré-vol a été **extrait** pour cela : le prédicat était testé, la décision qui s'en servait ne l'était pas, et c'est là que le défaut vivait. **Recherche dérivée** : 110 affirmations de comportement d'environnement (bac à sable, système de fichiers, noyau, moteur SQL) recensées dans le dépôt, dont 34 datées ; **17 sont tenues par quelque chose qui rougirait**, 32 seulement amorties par un repli ou un aveu, et 61 n'ont que la prose. Elles ne sont **pas** corrigées en masse ici — une correction non mesurée serait le contraire de ce travail. |
| S26 | Reglage de stockage temporaire suppose constant | ✅ | Un commentaire affirmait que la valeur compilée du moteur fait trier EN MÉMOIRE toute connexion qui ne dit rien. L'affirmation était VRAIE — et rien ne la relisait, alors qu'elle porte toute la confidentialité du défaut : le moteur chiffre les pages de la base, PAS ses fichiers temporaires, donc un tri qui déverse écrit des valeurs d'événement en clair. Sous l'autre valeur, la bannière aurait continué d'annoncer « déversement désactivé » pendant la fuite. MESURÉ : `PRAGMA temp_store` ne peut pas répondre à la question — il rend 0 sur une connexion muette dans les DEUX mondes ; seul `PRAGMA compile_options` nomme la valeur compilée. Mesuré aussi : sur les 24 chemins d'ouverture de production qui passent par la porte, UN SEUL posait ce réglage. Le réglage est désormais LU à l'ouverture par une voie unique qui pose ET relit, la porte l'arme pour tous ses appelants, une garde dérivée refuse toute connexion ouverte hors de cette voie en NOMMANT le site, la bannière rapporte la lecture au lieu de la promesse, et le démarrage REFUSE si le moteur ferait déverser ce que personne n'a demandé. |
| S27 | Publication atomique affirmée, durabilité absente | ✅ | La promesse de livraison au moins une fois reposait sur « écrire un temporaire puis renommer ». Ce motif donne l'ATOMICITÉ DU CONTENU — un lecteur voit l'ancien fichier ou le nouveau, jamais un fichier à moitié écrit — et RIEN D'AUTRE : après coupure, l'entrée de répertoire peut manquer alors que le fichier existe. Les octets sont là, leur NOM n'y est pas, et le spool est parcouru PAR NOM : l'événement disparaît, et rien ne le compte, puisque rien ne compte ce qui manque. **LE COMPTE ANNONCÉ ÉTAIT FAUX, et la recherche dérivée du MOTIF le montre** : mesuré le 2026-08-20, non pas trois surfaces mais VINGT-DEUX sur le chemin de livraison — trois dans le binaire d'agent (tampon d'événements, curseurs de source, base de référence d'intégrité), deux dans la bibliothèque de collecteurs, SEPT capteurs qui recopiaient le motif à la main au lieu de l'appeler, un exécutant hors partition, le récepteur syslog, le collecteur mail, et SEPT écritures de spool des récepteurs poussés du central (journal, HEC, OTLP, MinIO ×2, Firehose, Pub/Sub). Cinq autres renommages d'état, hors chemin de livraison, portent le même motif. Aucune de ces surfaces ne synchronisait quoi que ce soit, et aucun test n'en exerçait une seule. **CE QUI EST POSÉ** : une VOIE UNIQUE par langage plutôt que des correctifs site par site — côté agent un module de publication qui écrit, synchronise le contenu, renomme, synchronise le répertoire, et pose les permissions À LA CRÉATION (l'ancienne forme laissait une entrée de spool lisible selon l'umask pendant l'écriture) ; côté capteurs deux fonctions de la bibliothèque, dont une nouvelle pour les sept qui remplissent leur propre temporaire. Deux gardes DÉRIVÉES refusent qu'un site suivant réinvente le motif : un test qui balaie les sources de l'agent, une garde de CI qui refuse toute publication dans le spool écrite hors de la bibliothèque. **ASYMÉTRIE DÉLIBÉRÉE, ET C'EST LE CŒUR DE LA DÉCISION** : l'événement est rendu durable, le FILIGRANE ne l'est pas. Un filigrane perdu fait relire une tranche que le central dédoublonne ; un filigrane qui survivrait à des événements perdus les ferait disparaître définitivement. Rendre le filigrane durable aurait changé une perte improbable en perte certaine — une garde exécutée refuse donc aussi qu'on l'y ajoute. **COÛT MESURÉ** le 2026-08-20 (NVMe, btrfs sur LVM, 300 publications par variante) : chaque synchronisation coûte ~4,7 ms et ce prix est celui de la barrière d'écriture, pas de la taille du fichier (identique de 1 Kio à 64 Kio) ; publication complète 8–9 ms en Rust contre 0,04–0,08 ms sans, +12,7 ms en shell. Payable sur ces surfaces-là — un capteur publie une à trois enveloppes par passage, l'agent une entrée par source et par cycle. **CE QUE LES TESTS PROUVENT, ET CE QU'ILS NE PROUVENT PAS** : que les APPELS sont faits, au bon endroit et DANS LE BON ORDRE — la garde de CI source la bibliothèque avec un `sync` instrumenté et vérifie que la première synchronisation a lieu quand le fichier final n'existe pas encore, la seconde quand il existe. Ils NE PROUVENT PAS la survie à une coupure d'alimentation : cela demanderait de couper le courant d'une machine à l'instant exact. Le défaut réellement fermé est qu'aucun appel n'existait ; un matériel qui ment sur son propre vidage de cache reste hors de portée, et un test qui prétendrait le contraire mentirait. **PREUVE PAR MUTATION**, sept fois : retirer la synchronisation du contenu, puis celle du répertoire, fait rougir un test qui NOMME la propriété perdue, des deux côtés ; ajouter un site qui renomme à la main fait rougir la garde dérivée correspondante, des deux côtés ; rendre le filigrane durable fait rougir la garde d'asymétrie. **LIMITES ÉCRITES LÀ OÙ LA PROMESSE EST FAITE, plutôt que tues** : sous Windows le répertoire n'est pas synchronisable par la bibliothèque standard — la publication y est atomique et son contenu durable, sans garantie sur l'entrée de répertoire. Des capteurs avançaient leur filigrane AVANT de publier leurs événements : c'est l'ORDRE qui était faux, aucune synchronisation n'y remédiait, et c'est précisément pourquoi le filigrane reste non durable. **LE COMPTE ANNONCÉ ICI — cinq — ÉTAIT FAUX** : il venait d'un `grep state_write`, pas du motif ; `S30` en dénombre DIX-SEPT et referme l'ordre. Un exécutant hors partition des capteurs publie un événement de santé sans la voie unique, parce qu'il lui est interdit de sourcer la bibliothèque : son exclusion est nommée, bornée, et re-vérifiée par la garde. **CE QUI RESTE OUVERT, ET C'EST LA SURFACE LA PLUS EXPOSÉE** : les sept écritures de spool des récepteurs poussés du central, plus le récepteur syslog et le collecteur mail. Le central répond ACCEPTÉ puis l'expéditeur EFFACE sa copie : une entrée de répertoire perdue y détruit un événement que plus personne ne détient, et le syslog reçu en UDP n'est rejouable par rien. Ce n'est pas traité ici de propos délibéré : le prix mesuré tombe sur un chemin appelé à CHAQUE REQUÊTE et sur un fil d'exécution asynchrone qu'un `fsync` bloquant fige — l'arbitrage demande de déporter l'écriture, pas seulement d'ajouter deux appels, et il se décide avec son coût sous les yeux. |
| S28 | Plafond memoire dont le chemin de lecture n'est jamais exerce | ✅ | La fonction qui lit la limite mémoire du groupe de contrôle n'était exercée par aucun test, et elle rendait UNE SEULE absence de valeur pour DEUX faits opposés : « l'interface dit qu'il n'y a pas de limite » et « l'interface n'a pas pu être lue ». La bannière de démarrage les rendait donc par la même phrase, NON LISIBLE — elle accusait l'instrument sur l'hôte le plus ordinaire, un service système natif sans MemoryMax=, où la lecture avait parfaitement réussi ; et sur un hôte dont le chemin a changé de forme, elle laissait croire à une propriété du déploiement alors que rien n'avait été mesuré. HUIT FORMES d'interface sont désormais recensées et jouées : hiérarchie unifiée avec limite dans la lignée (le PLUS SERRÉ des ancêtres gagne, un parent tue avant la feuille), le mot max, la racine du montage qui n'expose pas de fichier de limite parce que le noyau n'y pose pas le contrôleur mémoire, le conteneur AVEC espace de noms où la racine visible porte la limite, le conteneur SANS espace de noms où le chemin du processus ne mène à rien sous le montage, la hiérarchie historique et sa sentinelle « illimité », l'hybride où le repli tranche, et l'absence totale d'interface. DEUX FORMES ÉTAIENT PERDUES : la sentinelle écrite sur 64 bits non signés ne rentrait pas dans l'entier signé qui la lisait, donc une absence de limite parfaitement écrite passait pour une panne ; et un fichier présent dont la forme n'est pas reconnue était ignoré en silence. CE QUI CHANGE POUR L'EXPLOITANT : quatre verdicts exclusifs, chacun portant un mot stable et sans espace dans la ligne de démarrage — protege, depasse, sans-limite, illisible — et le mot d'alerte est DÉRIVÉ du verdict, de sorte que seul le cas protégé est calme. La bascule d'une protection annoncée à une protection absente se surveille donc, au lieu de se relire. Un aveu nomme ce qui a été tenté ; une lignée partiellement lisible refuse de conclure sur le minimum des niveaux lus, parce qu'un niveau illisible peut être plus serré. CE QUI SE PASSE QUAND LA LECTURE ÉCHOUE, décidé et justifié : fonctionnement DÉGRADÉ ANNONCÉ, ni refus de démarrage ni défaut prudent. Refuser transformerait en incident un déploiement natif légitimement sans limite ; un défaut prudent INVENTERAIT un nombre, c'est-à-dire prétendrait la mesure qui manque. Le plafond dur du moteur, lui, ne dépend pas de cette lecture : il reste posé et une requête qui le franchit refuse. Ce que la lecture apporte est la CONFRONTATION — savoir si ce plafond est sous ce qui tue — et c'est exactement cela, et rien de plus, qui est annoncé comme non vérifié. PREUVE PAR MUTATION DANS LES DEUX SENS : faire rendre « aucune limite » à une forme non reconnue fait rougir deux tests ; faire rendre toujours « illisible » à la lecture en fait rougir trois — sans ce second sens, une fonction qui ne saurait rien passerait le premier. Une troisième mutation, ne lire que la feuille au lieu de remonter la lignée, annonce la limite large du niveau feuille à la place de la limite serrée du parent. RECHERCHE DÉRIVÉE de la PROPRIÉTÉ et non du nom — « lecture d'un chemin système dont l'absence est traitée comme une valeur » : sept autres sites. Deux la traitent déjà correctement, le champ de mémoire du processus qui devient un trou NOMMÉ plutôt qu'un zéro, et la liste d'autorisation du responder dont l'absence REFUSE tout. Deux la traitent mal et restent ouverts sous S29 : le couple temps processeur et mémoire résidente publié à ZÉRO quand le système de fichiers de processus est illisible, indistinguable d'une vraie valeur nulle, et la profondeur de file d'attente rendue à zéro quand le répertoire n'existe pas — un répertoire disparu s'y lit comme une file vide. Les trois lectures de nom d'hôte retombent sur une valeur de repli et sont sans conséquence. |
| S29 | Allegations d'environnement : recensement, tri par silence, cinq traitees | ⬜ | LE RECENSEMENT A ETE REFAIT AVEC UN CRITERE ECRIT ET REJOUABLE, et il ne retrouve pas le chiffre precedent. Corpus : tout `*.rs` hors `target/` ; unite : le BLOC de commentaire, code et chaines litterales retires par une machine a etats ; retenu : un terme de bac a sable, de systeme de fichiers ou de noyau pris DANS SON SENS OS. Cette derniere condition est le gros de l'ecart : une premiere passe rendait 1 329 blocs, dont l'essentiel etait des faux amis mesures — `kernel` designe ici le noyau VECTORISE de `cold_store` (86 blocs), `atomique` une transaction SQL ou un entier atomique (61), `anti-OOM` une borne de conception, `namespace` un espace de NOMS d'identifiants. Sens desambiguise, l'arbre du 2026-08-20 rend 12 534 blocs de commentaire, 420 candidats et **258 PHRASES qui AFFIRMENT** quelque chose de l'environnement, dont **65 datees (25 %)**. Contre 110 et 31 % annonces : la part datee concorde, le nombre non — et il n'est PAS reconciliable, parce que le balayage d'origine n'a laisse aucun critere derriere lui. C'est le premier constat de la cle : un recensement sans critere ecrit n'est pas une mesure, c'est un souvenir. LE TRI N'EST PAS FAIT SUR LE NOMBRE MAIS SUR LE SILENCE PRODUIT — « si ca cesse d'etre vrai, que rend le systeme, et qui l'apprend ? ». Quatre rangs : BRUYANT (le processus refuse ou plante : on l'apprend le jour meme), AMORTI (un repli ou un aveu nomme absorbe), SILENCE PARTIEL (une ligne de journal que personne n'ouvre), SILENCE COMPLET (le systeme continue et rend une valeur RASSURANTE — zero alerte, un journal d'apparence normale, un service qui demarre). Seul le dernier rang justifie un mecanisme. UNE PARTITION MESUREE COMMANDE CE QUI EST TENABLE : sur les 258, **84 ont pour faiseur de verite l'HOTE** (noyau, groupe de controle, systeme de fichiers) — aucune garde de source ne peut les tenir, elles se MESURENT a l'execution ou cessent d'etre des affirmations ; **24 nomment un fichier de ce depot**, et celles-la se tiennent pour une quinzaine de lignes chacune. CINQ TRAITEES, PAS DAVANTAGE — traiter les 253 autres en masse remplacerait 253 affirmations non verifiees par 253 formulations non verifiees. Quatre sont desormais TENUES par une garde derivee (`daemon/src/tests/allegations_d_environnement.rs`), toutes verifiees encore vraies le 2026-08-20 : (1) « aucun journal de requetes n'est monte » — c'est ce qui rend acceptable qu'une route d'ingestion recoive son secret dans la query-string, et la note s'en remettait a un « A FLAGGER pour la revue », c'est-a-dire a la vigilance ; monter une couche de trace ne casse rien, ne change aucune reponse et ne fait rougir aucun test, la fuite serait dans un fichier d'apparence normale. Trois portes fermees : la fonctionnalite `trace` de la dependance n'est pas compilee, aucun code execute ne monte de couche, aucune ligne executee ne journalise l'URI ni la query. (2) « aucun collecteur legitime n'emet de source dans le prefixe reserve » — un « (verifie) » sans date ; un collecteur qui usurperait le prefixe verrait tous ses evenements RENOMMES sans erreur ni rejet, son panneau resterait vide, indistinguable d'un capteur qui n'a rien a dire. La garde relit les collecteurs livres et les deux amorceurs, y cherche les DEUX formes par lesquelles un nom de source y nait, et lit le prefixe chez la fonction qui renomme au lieu de le recopier. (3) « le capteur d'integrite hache toujours le repertoire d'unites » — c'est le pire silence du recensement : si cette ligne disparaissait, la regle semee « vecteur de persistance ajoute » (T1543) continuerait de s'executer sur zero ligne et ne leverait PLUS JAMAIS d'alerte ; un SOC muet a l'apparence exacte d'un SOC sain. (4) « l'unite du demon ne porte AUCUN `EnvironmentFile` » — affirmee a quatre endroits, elle justifie deux decisions de conception ; or ajouter la directive est le geste ORDINAIRE pour donner sa configuration a une unite, le demon demarrerait et servirait a l'identique, et la seule chose qui changerait est que la cle SQLCipher deviendrait lisible dans l'environnement du processus. La CINQUIEME a ete REECRITE plutot que tenue, parce qu'elle n'etait pas tenable : « `test(1)`, POSIX, donc tenu par le `/bin/sh` de n'importe quel hote » promettait une propriete d'un hote inconnu ; elle dit desormais ce qui est REELLEMENT su — toute sortie qui ne designe pas un chemin devient un aveu nomme, jamais un « tous les chemins sont utilisables ». CHAQUE GARDE VALIDE SON PROPRE INSTRUMENT AVANT DE CONCLURE, parce que le mode de panne de cette famille est de rendre vert en etant aveugle : temoin POSITIF (29 des 88 unites livrees PORTENT `EnvironmentFile`, donc le detecteur sait la voir ; 73 sites d'emission de source reconnus dans les collecteurs) et temoin NEGATIF (le nom de la couche interdite EST ecrit dans le depot, en commentaire, et ne doit pas etre accuse ; une directive COMMENTEE n'est pas une directive). Les deux pieges de cette famille sont fermes par construction : le code est depouille de ses commentaires ET de ses chaines litterales — sans quoi la garde accuserait la prose qui l'explique, ou son propre message d'erreur. PREUVE PAR MUTATION, DANS LES DEUX SENS : une couche de trace posee dans du code non compile est ATTRAPEE ; une source usurpant le prefixe est attrapee a la LIGNE, tandis que la meme chaine en commentaire ne l'est pas ; la ligne de surveillance transformee en COMMENTAIRE qui nomme encore le repertoire rend rouge avec le bon diagnostic (un premier jet echouait la sur l'INSTRUMENT, donc avec le mauvais message : les deux questions sont desormais separees) ; la directive interdite posee rend rouge, la meme COMMENTEE reste verte. Et le depouillement lui-meme, neutralise, fait echouer la garde de l'instrument et refuser de conclure les deux gardes qui en dependent, au lieu de les rendre vertes. CE QUI RESTE OUVERT, ET C'EST LE FOND DU SUJET : 253 allegations, dont 84 sur l'hote qu'aucune garde de source n'atteindra. LA FORME PROPOSEE POUR LA SUITE — et elle est deliberement MINUSCULE : une allegation d'environnement ne merite d'etre ECRITE COMME UNE AFFIRMATION que si l'on peut NOMMER le fichier qui la rend vraie ; si on le peut, elle se tient pour une quinzaine de lignes sur le modele ci-dessus ; si on ne le peut pas, elle doit soit se MESURER a l'execution (le precedent existe : la sonde qui monte le bac a sable de l'unite et regarde), soit cesser d'etre une affirmation. AUCUNE CONVENTION DE MARQUAGE GENERALE N'A ETE INSTALLEE, et le refus est motive : annoter 258 phrases serait une edition de masse dont la garde ne pourrait verifier que la FORME du marqueur, jamais la verite de la phrase — un controle qui rejouerait les allegations datees ne saurait pas davantage rejouer « `ProtectHome` remplace le point de montage » sans un hote sous la main. Une garde qui crie sans rien savoir est desarmee le premier jour. |
| S30 | Filigrane avance AVANT la publication | ✅ | Un capteur incrémental fait deux gestes : il PUBLIE une enveloppe, et il MARQUE sa progression. Marquer d'abord rend le marqueur MENTEUR — une coupure entre les deux perd les événements, et rien ne le signale, puisque le marqueur affirme qu'ils sont acquittés. Défaut DISTINCT de `S27` et plus grave : `S27` rend durable ce qui est publié, cette clé porte ce qui est acquitté sans l'avoir été. **LE COMPTE ANNONCÉ ÉTAIT FAUX, ET DE LA MÊME FAÇON QUE CELUI DE `S27`** : il était dérivé du SYMBOLE `state_write`, pas du MOTIF. Recherche refaite le 2026-08-20 sur le motif « un site qui marque la progression et un site qui publie, dans le même flot » : non pas cinq capteurs mais DIX-SEPT. Les douze manquants marquaient autrement — redirection brute (`falco`, `suricata`, `auditd`, `kube-audit`), `touch` d'un repère daté relu en `find -newer` (`clamav`, `yara`), ajout d'une ligne dans un registre « déjà signalé » (`vuln`, `yara`, `containerd`), renommage d'une base de référence (`integrity`, `imgdrift`), point de reprise avancé par un OUTIL EXTERNE au moment de la lecture (`audit`, via `ausearch --checkpoint`), et un repère d'échantillonnage de métriques (`resources`). Cinq autres capteurs (`cloudflare`, `cloudflare-http`, `web`, `mail`, `dataaccess`) avaient DÉJÀ le bon ordre ; le capteur Windows et le binaire d'agent aussi, chacun par sa propre forme. **CE QUI EST POSÉ** : une voie unique dans `collectors/lib.sh` plutôt que dix-sept correctifs. Un capteur MET EN ATTENTE (`state_stage`, `state_stage_append`, `state_stage_file`) et n'a pas le geste d'écrire ; les seuls points d'écriture sont `spool_write_then_ack` / `spool_publish_then_ack` (publient PUIS écrivent) et `plume_exit_nodata` (rien n'a été publié, donc rien n'est acquitté). L'ordre est INTERNE à ces fonctions, donc un appelant ne peut pas s'y tromper — c'est la forme déjà retenue côté Windows (`Stage-Watermark`/`Complete-Run`) et côté agent (`CursorStore::save`, appelé après ship+ack). **CE QUE LA CORRECTION COÛTE, DIT SANS L'EMBELLIR** : elle échange une perte silencieuse contre un REJEU. Au pire un passage de collecte, borné par le plafond du capteur (4000 lignes pour `auditd`/`kube-audit`, 400 `suricata`, 300 `portscan`/`origin-drop`, 200 `falco`/`pod-logs`, 80 images `imgdrift`) ; deux familles rejouent un LOT et non une enveloppe — `pod-logs` marque un offset PAR FICHIER de pod avant l'unique publication de fin, et les registres « déjà signalé » accumulent une ligne par constat. Le central n'absorbe ce rejeu que là où l'événement PORTE une clé : intégralement pour `journal` (`__CURSOR`), `imgdrift`, `vuln`, `clamav`, `yara`, `cloudflare*` (identité de contenu) ; seulement dans le même seau de temps pour `ufw`, `portscan`, `origin-drop`, `kube-audit` ; PAS DU TOUT pour `falco`, `suricata`, `auditd`, `audit`, `pod-logs`, `integrity`, `containerd`, qui ne portent aucune clé. Pour ces sept-là, l'inversion produit des DOUBLONS VISIBLES là où il y avait une perte muette : c'est l'arbitrage, il est assumé, et la sortie propre — leur donner une clé d'identité — est nommée sans être faite ici. **GARDE DÉRIVÉE** : `.github/scripts/check_watermark_follows_publication.py` n'énumère aucun capteur ; elle refuse toute écriture directe de marqueur hors de la bibliothèque (appel de `state_write`, redirection / `mv` / `cp` / `touch` vers un chemin d'état, et redirection vers un PARAMÈTRE POSITIONNEL — c'est cette dernière forme qui cachait `containerd`), puis SOURCE la bibliothèque et vérifie l'ORDRE à l'exécution. **PREUVE PAR MUTATION, DANS LES DEUX SENS** : remettre l'ordre fautif dans un capteur fait rougir la garde en NOMMANT le fichier et la ligne (vérifié sur `ufw`, forme par redirection, et sur `containerd`, forme par paramètre positionnel) ; inverser les deux gestes DANS la bibliothèque fait rougir la jambe exécutée ; faire écrire `state_stage` tout de suite fait rougir le témoin négatif. **CE QUE LE TEST DE COUPURE PROUVE, ET CE QU'IL NE PROUVE PAS** : le processus est tué entre les deux gestes ; l'enveloppe publiée est là, le marqueur ne l'est pas, donc le passage suivant REJOUE. Le contre-témoin, avec l'ANCIEN ordre, perd l'événement définitivement et sans un mot — c'est ce qui prouve que le test mesure bien le défaut. Il NE PROUVE PAS que le rejeu soit sans conséquence (voir les sept capteurs sans clé), ni la survie à une coupure d'ALIMENTATION : cela demanderait de couper le courant d'une machine à l'instant exact, et un test qui prétendrait le contraire mentirait. **LIMITE NOMMÉE** : pour `audit`, c'est `ausearch` qui écrit le point de reprise ; ce qui est tenu ici est le MOMENT où il devient visible (copie de travail promue après publication), pas l'atomicité de son écriture. **LE CLASSEMENT DU REJEU ANNONCE ICI A ETE REFUTE PAR `S34`** (2026-08-21) : la population qui rejoue n'est pas celle des dix-sept capteurs a l'ordre fautif mais celle des vingt-deux qui publient puis acquittent, et `cloudflare-http` etait range a tort dans l'identite de contenu — sa cle valait un seau de l'instant de collecte. Six / quatre / sept se lisent donc neuf / quatre / huit ; les huit portent desormais une cle. **L'HYPOTHESE ECRITE ICI A ETE REFUTEE PAR `S36`** (2026-08-21) : « sortir par `plume_exit_nodata` veut dire qu'AUCUNE enveloppe n'a ete publiee, donc rien n'est acquitte » n'est vraie que pour un marqueur CALCULE A PARTIR DE CE QUI A ETE LU — un curseur journald tire de la derniere ligne obtenue n'existe pas tant que la lecture n'a pas abouti, donc il n'y a rien a ecrire. Elle est FAUSSE pour un marqueur qui ne doit RIEN a la lecture : un offset pris sur la TAILLE du fichier, un repere date, l'instant du passage. Celui-la est en attente AVANT que la lecture n'aboutisse ; si elle echoue, le capteur n'a effectivement rien a signaler, il sort par la porte propre, et cette sortie ACQUITTE une tranche jamais publiee — la perte SILENCIEUSE que cette cle existait pour empecher, revenue par la porte de sortie. La formulation a reprendre est celle-ci : `plume_exit_nodata` acquitte, et n'a le droit d'etre atteinte que lorsque la source a ete LUE et s'est revelee vide. |
| S31 | Reception poussee sans durabilite, arbitrage a trancher | ⬜ | Neuf surfaces de reception poussee ecrivent leur file sans synchronisation : le central repond ACCEPTE, l'expediteur EFFACE sa copie, et une coupure perd l'evenement des deux cotes. Le correctif de S27 ne s'y applique pas tel quel — une synchronisation bloquante par requete sur un fil d'execution asynchrone demande de DEPORTER l'ecriture, pas d'ajouter deux appels. Mesure disponible : le cout d'une synchronisation est celui de la BARRIERE, pas de la taille, identique de 1 Kio a 64 Kio. ARBITRAGE : accepter la perte possible et l'ECRIRE la ou la promesse est faite, ou deporter l'ecriture. La decision appartient a l'exploitant du produit, pas au correctif. |
| S32 | Lectures d'environnement qui publient zero faute de savoir | ✅ | Deux lectures rendaient une valeur NORMALE quand elles échouaient, au lieu d'avouer : le couple temps processeur / mémoire résidente du processus publiait ZÉRO quand sa source n'était pas lisible — indiscernable d'un vrai repos — et la profondeur de la file d'ingest annonçait une file VIDE quand son répertoire était absent. Le zéro est la valeur la plus CALME de chacune de ces séries : un tableau de bord y lit « rien à signaler » précisément quand la mesure a disparu. **LE DÉCOMPTE ANNONCÉ ÉTAIT FAUX, et c'est la recherche dérivée de la PROPRIÉTÉ — « une lecture d'environnement dont l'échec est converti en valeur normale ET publiée » — qui le montre** : mesuré le 2026-08-20, non pas deux sites mais VINGT ET UN, dont DOUZE alimentent une règle de détection ou une décision de garde et non un simple affichage. Le balayage recense aussi QUATORZE sites qui traitent DÉJÀ le cas correctement (ils donnent la forme) et DIX replis sans conséquence observable. **CE QUI EST CORRIGÉ ICI : les CINQ sites du démon**, soit les deux nommés plus trois de figure identique découverts par le balayage — la taille de la base rendue à zéro quand son fichier n'est pas interrogeable, l'usage disque qui rendait le composant de stockage VERT avec « 0 % utilisé » quand la mesure de volume échouait, et la sonde de chiffrement au repos qui annonçait « aucune base existante, elle sera créée chiffrée d'office » alors qu'une base peut être là et seulement injoignable. **LA FORME EST CELLE DE S28**, reprise pièce par pièce : un type à deux cas sans valeur par défaut (lue, ou illisible avec sa cause), des fonctions PARAMÉTRÉES sur leur racine et sur les constantes de l'hôte — fréquence d'horloge et taille de page comprises — donc exerçables sur une machine sans `/proc` comme sur une machine dont `/proc` répond, et des MOTS STABLES en sortie. **CE QUE LE CONSOMMATEUR VOIT, et pourquoi les deux formes plutôt qu'une** : la série numérique DISPARAÎT (l'absence est un premier concept côté supervision, et ce dépôt s'en sert déjà pour l'âge d'un exercice de restauration jamais fait) ET une jauge de lisibilité paraît à côté, portant la cause — parce qu'une série absente ne distingue pas « la lecture a échoué » d'un relevé manqué, et qu'un indicateur seul aurait laissé le zéro en place. Le JSON du panneau omet le nombre et porte le verdict, la cause et un détail libre ; le panneau affiche un tiret et la cause au lieu de reconstruire un zéro côté client, ce qu'il faisait ; et les composants « ingest » et « stockage » passent au JAUNE au lieu du VERT quand leur mesure manque — jaune et non rouge, les voies qui ne dépendent pas de cette lecture pouvant parfaitement servir. **CARDINALITÉ BORNÉE, pire cas écrit** : l'étiquette de cause ne prend ses valeurs que dans un ensemble FERMÉ de cinq clés, donc au plus cinq séries par jauge sur la vie d'un déploiement et UNE SEULE à la fois, les cas étant exclusifs. Le texte libre — chemin tenté, message du système — n'a aucune borne et ne part JAMAIS en étiquette. **PREUVE PAR MUTATION DANS LES DEUX SENS, et le second témoin est le cœur du lot** : rendre un zéro quand la source est illisible fait rougir quatre tests sur la file et un sur le couple processeur/mémoire ; faire rendre TOUJOURS « illisible » en fait rougir cinq sur la file et deux sur le couple — dont ceux qui exigent qu'une file réellement vide publie bien 0 et qu'un processus réellement au repos publie bien 0 s. Sans ce second sens, une fonction qui ne saurait jamais rien passerait le premier et ferait disparaître le cas nominal. La sonde de chiffrement au repos porte la même paire, avec un instrument VALIDÉ avant d'être cru et choisi pour être indépendant de qui exécute la suite. **CE QUI RESTE OUVERT : S33**, et c'est la part la plus grave du décompte. |
| S33 | Mesures d'hote a zero qui arment des regles de detection | ✅ | Une lecture d'environnement qui échoue et rend la valeur la plus RASSURANTE — zéro, vide, « pas de limite » — fait lire un bon signal là où il n'y a aucun signal. Quand cette valeur ARME UNE RÈGLE À SEUIL, c'est pire : la règle ne peut plus tirer, et une détection devient structurellement impossible sans qu'aucune alerte ne le dise. **LE DÉCOMPTE ANNONCÉ ICI ÉTAIT FAUX, ET DANS LES DEUX SENS.** Il annonçait sept mesures d'hôte plus quatre sites recensés à côté. Recherche refaite le 2026-08-21 sur la PROPRIÉTÉ — « une lecture d'une source d'environnement dont l'échec devient une valeur normale publiée ou consommée » — et non sur un symbole : le capteur de ressources en porte HUIT et non sept, et la huitième n'est pas un zéro mais un défaut PIRE — la charge moyenne lue par un découpage de champ rendait la chaîne VIDE sur une source présente mais vide, l'enveloppe devenait un JSON invalide, et les huit mesures du passage étaient jetées d'un coup sans que rien ne les compte. Deux balayages dérivés de la même propriété rendent par ailleurs, sur l'arbre entier, un ORDRE DE GRANDEUR de cent-soixante-dix sites portant la figure, dont une large majorité hors du démon — là où la donnée ENTRE. Ce chiffre-là n'est PAS vérifié site par site et ne doit pas être cité comme une mesure : il est donné pour ce qu'il est, une borne inférieure de l'ampleur, et c'est `S36` qui le reprend. **CE QUI EST FERMÉ ICI, dans l'ordre du SILENCE PRODUIT.** ① LES HUIT MESURES D'HÔTE. Quatre chemins menaient au même zéro et sont nommés : un bloc terminal d'awk qui imprime 0 quand le motif cherché n'a jamais matché ; le code de retour de la mesure d'occupation disque avalé par le tube vers son décodeur (le pipeline sort 0 en rendant « 0 % ») ; un motif de noms d'interface codé en dur qui ne matche AUCUNE des conventions usuelles, si bien que le débit réseau valait zéro POUR TOUJOURS ; et une variable vide interpolée DANS un programme awk, qui en devenait un fragment de programme et produisait un delta négatif, donc un zéro parfaitement calme. S'y ajoute l'échantillon précédent, qui est une source comme une autre : sans lui aucun taux ne se calcule, et l'ancienne forme publiait « aucune activité » à chaque premier passage et à chaque effacement de l'état. ② LE VERDICT DE COUVERTURE DU CAPTEUR D'INTÉGRITÉ, qui s'aveuglait lui-même : la sonde qui vérifie si le bac à sable retire une famille annoncée rendait DEUX verdicts, et l'échec de lecture de sa propre source tombait du côté « pas masqué », c'est-à-dire « couvert ». Le capteur se déclarait donc intégralement couvert précisément quand il n'avait RIEN pu établir. Un TROISIÈME verdict, `indetermine`, est désormais distinct des deux autres et avoué à son tour ; le verdict « visible » est IMPRIMÉ explicitement plutôt que déduit d'une sortie vide, sans quoi le défaut aurait été reconstruit d'un cran plus bas. ③ L'IDENTITÉ DE L'HÔTE, QUI DÉCIDE D'UNE ACTION et non d'une série — donc traitée avant les files. Le repli rendait un nom d'hôte plausible quand la source n'était pas lisible, et un nom plausible ment mieux qu'un zéro : il est indiscernable d'une lecture réussie, y compris pour qui relit le code. Deux conséquences opposées et toutes deux muettes : les actions de réponse visant le VRAI nom de la machine dormaient indéfiniment — une réponse décidée par un analyste ne s'exécutait jamais — pendant que celles visant une AUTRE machine portant le nom de repli s'exécutaient là. L'arbitrage retenu est écrit : les actions NON CIBLÉES restent exécutées (elles disent « n'importe quel hôte », et cette phrase reste vraie quand on ignore son propre nom), les CIBLÉES sont laissées intactes et restent réclamables par l'agent de l'hôte visé. ④ LA CONTRE-PRESSION DISQUE DU RÉCEPTEUR SYSLOG et ⑤ LA FILE DE L'AGENT D'EXTRÉMITÉ, où le même repli éteignait une GARDE : un répertoire illisible se lisait « sous le budget » et « aucune entrée en attente », donc plus aucun plafond ne mordait — au moment précis où l'on ne savait plus mesurer le disque. Trois replis de plus poussaient dans la même direction À L'INTÉRIEUR d'une mesure réussie (parcours partiel, entrée sautée, interrogation d'attribut ignorée) : un compte plus petit que la file réelle est, pour un plafond, exactement aussi dangereux qu'un zéro. La DIRECTION reste fail-open côté syslog — un récepteur qui jetterait tout parce qu'il ne sait plus lire son répertoire transformerait une panne de MESURE en perte d'événements, dont l'UDP n'est rejouable par personne — mais le cas a désormais un nom, un compteur et une ligne dans le rapport périodique. **LA FORME EST CELLE DE `S28` ET `S32`, REPRISE ET NON DOUBLÉE** : le vocabulaire de causes du shell est celui du démon, aux mêmes mots ; l'aveu passe par le canal d'indisponibilité déjà livré, sur lequel une règle ALERTE déjà, donc une mesure d'hôte perdue lève une alerte au lieu de se lire comme du calme ; aucune métrique nouvelle n'est inventée pour porter l'indicateur, et l'identité de l'hôte publie son VERDICT sans jamais publier sa valeur — un nom en étiquette ferait de chaque machine une série de plus. **PREUVE PAR MUTATION, DANS LES DEUX SENS, ET LE SECOND EST LE CŒUR** : source illisible -> verdict d'échec et AUCUN nombre ; source lisible dont la valeur vaut RÉELLEMENT zéro -> lue, et le zéro est publié. Sans le second, une version qui ne saurait jamais rien passerait le premier sans rien prouver et ferait disparaître le cas nominal — un hôte au repos, une file réellement vide, un système de fichiers réellement à 0 %. **GARDE DÉRIVÉE, QUI N'ÉNUMÈRE AUCUNE MESURE** : le capteur est exécuté TEL QU'IL EST LIVRÉ contre une arborescence fabriquée et une commande d'occupation disque stubée — rien de la machine qui exécute la garde n'entre dans le verdict — et les clés exigées du second témoin sont DÉCOUVERTES dans la sortie du premier. Une neuvième mesure ajoutée demain est donc contrôlée d'office. La garde porte son propre plancher de non-dégénérescence : sous quatre mesures publiées, elle refuse de conclure au lieu de rendre vert en étant aveugle. Elle a été validée par trois mutations — réintroduire le zéro, rendre toujours « illisible », retirer l'aveu — qui la font rougir chacune en nommant ce qui est perdu. **CE QUI RESTE OUVERT ET NOMMÉ** : la mesure d'occupation disque n'a pas de témoin « vraiment zéro » sans stub, aucun système de fichiers réel n'étant à 0 % ; la température reste hors garde parce qu'elle lit des racines que le capteur ne paramètre pas, et c'est assumé — c'est elle qui tenait DÉJÀ la règle dont tout ce lot est dérivé. Et surtout : `S36`, qui porte l'ampleur mesurée ci-dessus. |
| S34 | Rejeu visible mais non absorbe, faute de cle d'identite | ✅ | L'inversion d'ordre posee par `S30` remplace une perte SILENCIEUSE par un rejeu VISIBLE, ce qui est un progres — mais le dedoublonnage ne l'absorbe que si l'evenement porte de quoi etre reconnu. **LE MECANISME, DERIVE PLUTOT QUE RECITE, parce que c'est de lui que la partition depend** : cote central, un evenement est reconnu « deja vu » par UNE chose — la colonne `event.dedup`, index UNIQUE au niveau de la base, toute ecriture etant un `INSERT OR IGNORE`, la cle etant cloisonnee par l'hote au point d'ecriture (`dedup_scoped_by_host`). Une cle ABSENTE vaut NULL, et SQLite tient deux NULL pour DISTINCTS : **sans cle il n'y a pas de dedoublonnage du tout**, jamais un dedoublonnage partiel. **LE DECOMPTE ANNONCE — six / quatre / sept — ETAIT FAUX, ET DE LA MEME FACON QUE CEUX DE `S27` ET `S30`** : il portait sur DIX-SEPT capteurs parce qu'il avait herite de la liste de `S30`, celle des capteurs dont l'ORDRE etait fautif. Or la population qui REJOUE n'est pas celle-la : c'est celle des capteurs qui publient PUIS acquittent, soit les VINGT-DEUX qui appellent `spool_write_then_ack` / `spool_publish_then_ack`. Partition redérivee de la propriete le 2026-08-21, sur 26 sites d'emission : **NEUF absorbes par identite de contenu** et non six (`journal` via `__CURSOR`, `imgdrift`, `vuln`, `clamav`, `yara`, `cloudflare`, plus `web`, `mail` et `dataaccess` — ces trois-la avaient deja le bon ordre, donc `S30` ne les avait pas listes, mais ils rejouent tout autant) ; **QUATRE a seau de temps** (`ufw` horaire, `portscan` 5 min, `origin-drop` 1 min, `kube-audit` empreinte + 10 min) ; **HUIT non absorbes** et non sept — les sept sans aucune cle (`falco`, `suricata`, `auditd`, `audit`, `pod-logs`, `integrity`, `containerd`) plus `cloudflare-http`, que le decompte rangeait a tort dans l'identite de contenu alors que sa cle valait `<instant de collecte>/60` : **une cle qui contient l'instant de publication ne dedoublonne rien**, c'est le piege meme que cette cle-ci devait eviter. `resources` sort de la partition — il publie `kind=metrics`, pas des lignes `event`. **CE QUI EST POSE — une cle prise dans ce que le capteur OBSERVE** : horodatage nanoseconde du record (`falco`, `pod-logs`), microseconde (`suricata`), serial du noyau `audit(<sec>.<msec>:<serial>)` joint a la categorie (`auditd` — un meme evenement noyau peut donner deux events Plume de natures differentes, les confondre effacerait un constat), identifiant d'image ou de conteneur (`containerd`), et (genre, chemin, empreinte, **mtime**) pour `integrity` — la date est ce qui rend un constat RE-SIGNALABLE : sans elle, une cle SSH nettoyee puis replacee a l'identique porterait la cle deja vue et le second constat, pourtant reel, serait efface (c'est la forme deja retenue par `yara`, pour cette raison exacte). Quand le record lui-meme n'offre AUCUNE identite, la cle est la BORNE BASSE DE LA TRANCHE, qui n'avance qu'apres la publication : `audit` (mesure le 2026-08-21 : `ausearch --format text` rend des phrases a la SECONDE, sans serial — deux enregistrements d'une meme seconde y sont indiscernables ; le point de reprise, lui, porte `output=<noeud> <sec>.<msec>:<serial>`) et `cloudflare-http` (borne `since` de la fenetre GraphQL, le groupe agrege n'ayant aucune dimension temporelle). **CE QUI RESTE SANS CLE EST NOMME PLUTOT QUE BRICOLE** : les ports en ecoute d'`integrity` — un port n'a ni empreinte ni date, et son numero se repete legitimement quand un service redemarre ; une cle batie dessus effacerait precisement le constat a voir. De meme tout record prive de son horodatage. Et une limite du premier passage de `cloudflare-http` : sans filigrane, la borne vaut « il y a dix minutes », qui n'est pas reproductible — les passages suivants le sont. **PREUVE PAR MUTATION, DANS LES DEUX SENS, ET LE SECOND TEMOIN EST LE CŒUR DU LOT** : cote capteurs, la garde `.github/scripts/check_replay_is_absorbable.py` LANCE sept capteurs pour de vrai avec une coupure simulee juste apres la publication (la bibliotheque est chargee par un relais qui tue le processus dans l'acquittement) — donner a la cle l'instant de publication fait rougir le temoin d'ABSORPTION, rendre la cle CONSTANTE fait rougir le temoin de DISCRIMINATION, et un enregistrement neuf ne doit jamais reemployer une cle deja vue. Cote central, trois tests font passer les memes formes de cle par le VRAI chemin d'ingestion : meme evenement republie -> 1 ligne (sans cle -> 2, contre-temoin), trois constats distincts -> 3 lignes (cle constante -> 1, donc deux evenements reels detruits), et retirer le cloisonnement cote daemon fait rougir les trois. **UN DEFAUT D'INSTRUMENT A ETE TROUVE ET CORRIGE EN COURS DE ROUTE, il vaut d'etre ecrit** : les deux passes de la garde tombaient dans la MEME SECONDE, si bien qu'une cle a laquelle on ajoutait l'instant de publication passait le temoin d'absorption. L'horloge est desormais relayee, chaque passe voit une seconde differente, et le relais est valide par un temoin positif et un temoin negatif avant d'etre cru. **CE QUE CELA NE FERME PAS** : les quatre cles a seau restent des cles a seau — c'est une AGREGATION VOULUE, mais une coupure qui enjambe la frontiere du seau n'est PAS absorbee, et un test le tient explicitement plutot que de le taire. Un plafond derive refuse que ce regime s'etende par recopie. |
| S35 | Une table verifiee par un test declenche le scan de secrets | ✅ | La table qui porte l'empreinte de chaque unite livree faisait echouer le scan de secrets : deux de ses lignes portent un nom d'unite contenant un mot que la regle cherche, et la valeur voisine est une chaine hexadecimale longue. La convention du depot — n'exclure qu'une VALEUR exacte, jamais un fichier ni une regle — ne s'appliquait pas : chaque empreinte change des que son unite change. L'exception porte donc sur la FORME et le prefixe du nom, et ce qui la rend sure est qu'un AUTRE mecanisme verifie deja chaque entree — le test qui re-hache les fichiers refuse une empreinte perimee, un nom en trop ou manquant. Une valeur etrangere ferait rougir la suite, controle plus fort que le scan. ENSEIGNEMENT D'INSTRUMENT : deux temoins successifs ont ete invalides — un jeton que cette version de l'outil ne detecte pas meme sans configuration, puis une valeur sans le mot qui declenche la regle. Un temoin doit etre derive de CE QUI DECLENCHE la regle, jamais de ce qui ressemble a un secret. |
| S36 | La meme figure hors du demon, la ou la donnee ENTRE | ✅ | `S28`, `S32` et `S33` ont posé la forme — un type à plusieurs cas sans valeur par défaut, des fonctions paramétrées sur leurs sources, un vocabulaire de causes fermé, la valeur ABSENTE plutôt que rassurante, et un indicateur qui l'accompagne. Deux balayages dérivés de la PROPRIÉTÉ, et non d'un symbole, montrent le 2026-08-21 que cette forme n'a jamais franchi la frontière du démon : l'ordre de grandeur est de cent-soixante-dix sites sur l'arbre, et la concentration est nette — l'écrasante majorité de ceux qui arment une détection vit dans le binaire d'agent, dans les deux collecteurs compilés et dans les capteurs shell, c'est-à-dire là où la donnée ENTRE. CE CHIFFRE N'EST PAS UNE MESURE VÉRIFIÉE SITE PAR SITE : c'est une borne inférieure d'ampleur, et le premier travail de cette clé est de la REFAIRE, pas de la reprendre — les quatre décomptes précédents de cette campagne étaient faux, chaque fois pour la même raison. TROIS CONCENTRATIONS SONT DÉJÀ NOMMÉES et méritent d'être traitées comme des familles plutôt que site par site : un mot d'échec routé vers la branche RASSURANTE là où le vocabulaire de l'inconnu existe pourtant déjà dans la donnée ; des enforcers qui échouent OUVERT sur leur propre liste de protection, alors que l'un d'eux fait du sens inverse l'invariant écrit de son en-tête ; et le motif « marqueur mis en attente, lecture qui échoue, sortie propre qui acquitte » — l'hypothèse écrite par `S30` (« sortir par là veut dire qu'aucune enveloppe n'a été publiée, donc rien n'est acquitté ») est vraie pour un curseur calculé APRÈS lecture et fausse pour un offset mis en attente AVANT elle. ORDRE DE TRAITEMENT : ce qui arme une règle, puis ce qui décide d'une action, puis les files, l'affichage en dernier. Et la sortie propre n'est pas un correctif par site : c'est de porter la forme aux trois surfaces qui ne l'ont pas, avec une garde dérivée par surface. **TROISIEME CONCENTRATION FERMEE (2026-08-21) — « marqueur mis en attente, lecture qui echoue, sortie propre qui acquitte ».** LE DECOMPTE A ETE REFAIT SUR LA PROPRIETE, et il ne se lit pas comme le mot de la concentration le laissait croire : la propriete n'est pas « mis en attente avant la lecture » au sens de la position des lignes, c'est « le marqueur ne doit RIEN au succes de la lecture qu'il acquitte ». Sur les VINGT-DEUX capteurs qui mettent un marqueur en attente, NEUF pouvaient acquitter une lecture ratee par la sortie propre — `falco`, `suricata`, `auditd`, `kube-audit` (offset pris sur la taille), `audit` (point de reprise avance par `ausearch` PENDANT la lecture), `clamav` et `yara` (repere date pose avant le scan), `cloudflare-http` (filigrane valant « maintenant moins le lag »), et `integrity`, que le mot de la concentration ne designait pas : sa reference est bien batie sur ce qui a ete lu, mais le DIFF dont elle presume le succes voyait son code de retour jete, si bien qu'un `comm` en echec faisait entrer les constats du passage dans le connu sans les avoir emis. QUATRE AUTRES portent le meme defaut par une route differente — une publication INCONDITIONNELLE (le battement de sante) au lieu de la sortie propre : `ufw`, `portscan`, `origin-drop`, `pod-logs`. CE QUI REND LE DEFAUT INVISIBLE : le mot d'une lecture qui a echoue et celui d'une source calme sont LE MEME MOT — la chaine vide — et un TUBE aggrave la chose : une lecture suivie d'un `grep` rend le verdict du `grep`, pour qui « aucune correspondance » vaut 1, c'est-a-dire le cas NORMAL. **LA DISTINCTION POSEE**, avec le vocabulaire d'indisponibilite deja livre plutot qu'un canal de plus : `plume_lecture_echouee` (jette les marqueurs en attente, AVOUE par le canal ou une regle livree alerte deja, sort 0) et `plume_lecture_partielle` (avoue et CONTINUE, pour un capteur qui a autre chose a publier). Ce n'est PAS un quatrieme cas de la partition : c'est le cas (I) survenu apres la mise en attente, meme `reason`, meme severite, plus une garantie — ne jamais acquitter ce qui n'a pas ete publie. La CAUSE se dit dans l'ensemble ferme de `S32`/`S33` (`source_absente`, `source_refusee`, `source_illisible`, `forme_inconnue`), `plume_cause_lecture` la derivant de l'etat du chemin. **GARDE DERIVEE** : `.github/scripts/check_read_failure_is_not_acknowledged.py` n'enumere aucun capteur — elle decouvre la population sur le motif, exige de chacun l'une de DEUX preuves (nommer le chemin d'echec, ou ne mettre en attente que sur le chemin qui publie), et porte un plancher de non-degenerescence. **PREUVE PAR MUTATION, DANS LES DEUX SENS, ET LE SECOND EST LE CŒUR** : rendre a un capteur la forme fautive fait rougir les deux jambes en NOMMANT la tranche perdue ; faire que la sortie propre n'acquitte PLUS JAMAIS rien fait rougir le temoin inverse sur les trois capteurs exerces — sans lui, une version qui aurait troque la perte silencieuse contre un rejeu PERMANENT passerait pour verte. Ce temoin-la ne se contente pas d'une source inchangee (le marqueur y porte deja la bonne valeur du passage precedent) : il ajoute une ligne que le capteur LIT et n'expedie pas. Retirer l'aveu de la primitive fait rougir la troisieme. **LIMITES ECRITES** : `xargs` ramene a 123 aussi bien « ClamAV a trouve » que « ClamAV a echoue », donc un lot melangeant les deux reste indiscernable — seul le cas « echec sans la moindre ligne FOUND » est conclu ; le code de retour d'`ausearch` ne distingue pas « aucun enregistrement » d'une erreur, si bien que `audit` ne s'en sert PAS et tient sa garantie autrement (le point de reprise n'est mis en attente que sur le chemin qui publie), au prix nomme d'une tranche relue quand elle ne rend aucun texte exploitable ; et la jambe statique exige qu'un chemin d'echec soit NOMME, elle ne verifie pas qu'il est branche sur CHAQUE lecture — cette part tient a la relecture, et elle est dite. **QUATRE LOTS MENES, ET LE DECOMPTE A REFUTE CETTE CLE.** Critere publie et rejouable de part et d'autre. Cote enforcers : 157 sites en balayage strict, 303 en large — l'ordre de grandeur annonce tient, mais **57 % des sites sont DANS le demon**, non hors de lui. L'affirmation « l'ecrasante majorite vit hors du demon » ne survit qu'a un filtre — « qui arme une detection » — qu'aucun critere mecanique ne tranche : elle est RETIREE plutot que confirmee de complaisance. Decomptes par surface : capteurs shell **70 sites** adjuges un par un depuis 251 candidats mecaniques ; surfaces Rust d'entree **69** (agent 40, syslog 18, mail 11). UNE CATEGORIE MANQUAIT au classement : le rang « du BRUIT au lieu du silence » — une lecture ratee qui rend un verdict alarmant a tort — 10 sites cote shell, 7 cote Rust, laisses deliberement car quelqu'un les decouvre. FERME : les deux enforcers echouent desormais FERME, dont celui dont l'en-tete ecrivait l'invariant inverse ; TROIS trous independants sur la liste d'acces d'un port d'ingestion, dont un pair non identifiable qui franchissait un perimetre POSE ; 13 capteurs qui acquittaient une lecture ratee, plus 4 qui portaient le meme defaut par une publication INCONDITIONNELLE ; 10 sites shell de rang 1 dont un qui rendait structurellement inerte une regle livree de severite 4. QUATRE GARDES DERIVEES, une par surface, chacune validant son instrument avant de conclure. CONSTAT D'OUTILLAGE : les suites des deux collecteurs compiles NE TOURNAIENT NULLE PART — leurs gardes n'auraient jamais tourne — et l'un d'eux n'avait AUCUN test ; un job les execute desormais, et il est attendu par l'agregation. RESTE OUVERT et nomme fichier par fichier : 40 sites shell de rang 1, 8 sites d'action du binaire d'agent, et les sites de rang bruyant. **LES TROIS RESTES DE CETTE CLE SONT FERMES (2026-08-21), ET LE PREMIER DECOMPTE EST REFUTE PAR LE SECOND.** LOT SHELL DE RANG 1 : la population n'est plus estimee, elle est MESUREE PAR EXECUTION — chaque capteur armant une regle livree est execute TEL QUE LIVRE contre des bouchons qui font echouer ses lectures, verdict muet ou aveu. Sur 164 candidats mecaniques, **15 capteurs arment une regle livree et 5 restaient muets, soit 16 sites de rang 1** : le reste annonce ici (« 40 sites shell ») etait un decompte de CANDIDATS, pas de sites, et il est RETIRE. **12 sites fermes sur 4 capteurs**, ordonnes par la severite de la regle desarmee : `pe-kube-sa-clusteradmin` (4), `pe-new-suid-added` (4), `lm-conntrack-internal-ssh` (3), `im-ban-storm` (2). DEUX FORMES QUE LE MOT « silence » NE DECRIT PAS : `bans` ne se taisait pas, il AFFIRMAIT `active_bans: 0` sur des backends morts — une valeur rassurante vaut pire qu'une absence ; et dans `integrity`, une enumeration SUID en echec entrait dans la reference PROMUE, c'est-a-dire dans le CONNU, si bien que le passage suivant ne signalait plus rien. **UN AVEU EFFACE PAR UN AVEU** : deux indisponibilites de la MEME source dans un meme passage ecrivaient le meme nom d'enveloppe, la seconde ecrasant la premiere ; le nom porte desormais l'empreinte du contenu. GARDE ETENDUE PAR LA PROPRIETE et non par une liste — « publie une affirmation alors que ses sources n'ont rien rendu », que l'affirmation soit une metrique OU un battement de sante — portee 6 -> 12 capteurs, 4 -> 8 exerces, 7 -> 15 mutations concluantes ; elle NOMME ce qu'elle ne peut pas conclure (capteur non exercable, mutation inconcluante) plutot que de le compter comme vert. Un capteur qui n'emet que ce qu'il a vu reste DEHORS, deliberement : ses deux temoins seraient identiques. LOT DES SITES D'ACTION DU BINAIRE D'AGENT : **11 sites et non 8**, dont neuf DECIDENT et deux informent. Mesure sur systemd 261 : le CODE DE RETOUR ne separe pas « mesure, pas actif » de « pas mesure » — `is-active` sort 4 en disant `inactive` sur une unite ABSENTE — alors que le MOT, lui, les separe ; et deriver `is-enabled` de `UnitFileState` serait faux. `Outcome::observe` passe d'un booleen a un type A TROIS CAS : un booleen n'avait pas de place pour « pas regarde », et le choix commode etait « deja conforme ». L'installation n'execute plus AUCUN demarrage si l'une des trois lectures d'etat echoue. LOT DU RANG BRUYANT — une lecture ratee qui rend un verdict ALARMANT a tort : **13 sites shell et 4 Rust**, adjudication NEUVE et non refutation, le 10/7 annonce plus haut n'ayant jamais publie de liste. Le pire : une sonde d'integrite illisible faisait emettre `fim_event=deleted` en severite 3 par VAGUES, armant la regle livree `im-fim-mass-delete` (severite 4) — un capteur aveugle fabriquait l'incident qu'il est cense detecter. Dans `conntrack`, des ports d'ecoute illisibles faisaient basculer CHAQUE flux en `outbound`. Le champ `proc` est desormais RETIRE plutot que rempli d'une valeur de remplacement, parce que le compilateur GXQL emet `json_extract` SANS `COALESCE` : sur NULL la regle s'ABSTIENT, ce qui est le comportement voulu. Aucun faux calme introduit, verifie site par site. **UNE GARDE DU DEPOT A ATTRAPE LE LOT AU MOMENT DE L'INTEGRATION** : les deux champs neufs emis par `conntrack` (`cause`, `proc_verdict`) n'etaient pas declares a l'inventaire de collecte, et le test le disait avec sa consequence — l'oracle aurait signale INERTES des regles portant sur une donnee reellement collectee. Corrige a l'inventaire. Suites : demon 1155, agent 157, syslog 49, mail 5. |

Les autres constats sans clé sont clos : sécurisation de la route d'installation initiale, capteurs
qui sortaient en succès sans pouvoir fonctionner, trous de couverture des agrégats préconstruits,
marques de troncature, mesures de concurrence et de débit.

---

## Limites connues

**Le budget de 2 Gio est une contrainte de conception, pas une observation.** Tout arbitrage de
stockage, d'index et d'agrégation est tranché contre lui. Au repos, le service en consomme une
fraction ; cela ne dit rien du pire cas, et il ne faut pas le lire comme une garantie.

**Le tri de très gros résultats reste le poste de mémoire dominant.** Le moteur délègue le tri et
l'agrégation à SQLite, dont le trieur n'a pas de budget propre. Une limite de tas dérivée du budget
publié transforme le dépassement en **refus explicite** plutôt qu'en mort du processus : la requête
échoue, le service survit. C'est une fermeture par le refus, pas par la réponse.

**Le déversement des tris sur disque est opt-in, et c'est un choix de confidentialité.** SQLCipher
chiffre le fichier de base, pas les fichiers temporaires de SQLite : activer le déversement fait
exister un plafond de tri au prix de valeurs d'événement écrites en clair hors de la base. Le
réglage existe, il est documenté comme un échange, et il n'est pas le défaut. Aucun quota ne borne
encore la taille du déversement quand il est activé.

**Le tier froid colonnaire est optionnel et n'est pas actif par défaut.** Il se demande à la
compilation puis s'active par configuration ; une image construite sans lui ne peut honorer aucune
de ses variables. Quand il est actif, il retire du chaud les lignes déjà scellées : la fenêtre
chaude ne contient plus tout l'historique, et **toute procédure de restauration doit en tenir
compte** sous peine de rendre une fraction en annonçant un succès.

**Le vieillissement freine la croissance du fichier, il ne l'annule pas.** Il rend beaucoup d'espace
d'un coup, mais cet espace retourne à la liste libre de la base et l'activité le reconsomme.
Réduire durablement la taille demande de réduire les octets **par événement chaud** — c'est le
chantier de compression au repos, conçu et non construit.

**Une passe de vieillissement tient le verrou d'écriture.** L'exposition est rare et prévisible
depuis que le détecteur ne tire qu'une fois par jour, mais franche quand elle a lieu : une très
faible part des requêtes porte la quasi-totalité du temps d'attente, donc ne jamais résumer cet axe
par une moyenne — ni par un quantile, qui est aveugle à une queue plus fine que sa propre marge. La
corrélation est désormais une série, en seaux et en maximum, sur la même échelle de temps que la
présence de la passe (P10.11-a) ; elle reste une borne inférieure, et le dit. **La crête du
journal d'écriture anticipée reste non bornée — et elle n'est pas bornable** : elle ne suit pas le
volume écrit mais la durée pendant laquelle un lecteur refuse le point de reprise (mesurée à 57 % de
la taille de la base). Ce qui a été borné est le **résidu**, l'empreinte que le fichier garde entre
deux rafales et qui, sans borne, reste à sa plus haute crête pour toujours : c'est lui qu'on porte
au budget (P10.16-a).

**Le débit d'ingestion décroît quand le volume croît**, et cette chute se factorise en un coût CPU
par événement qui monte : c'est le volume, pas la machine ni le stockage. La cible affichée n'est
pas atteinte, et elle n'est pas maquillée. Corollaire : **la concurrence de requêtes n'est pas un
réglage de confort** — le sémaphore interactif est ce qui maintient le service dans son budget, et
l'élargir fait baisser le débit, monter la latence de queue, et peut faire tuer le processus sous
charge d'analystes. **Vingt et un gabarits de route le consomment** (décompte dérivé de la source,
2026-08-20, refait à chaque exécution de la suite), et l'exposition sépare désormais l'attente du
permit du travail fait une fois le permit obtenu — sans quoi une saturation ne se distingue pas
d'une requête lente (P7.8-a).

**Vingt et une sondes de fraîcheur gardent une portée « tous hôtes confondus »** (P3.2-a), et cette
portée est désormais **déclarée** : elle se lit dans le panneau au lieu de se déduire du SQL. La
phrase qui figurait ici — « celles qui suivent la fraîcheur du pipeline ne présentent pas ce
risque » — est **fausse** et a été retirée : cet indicateur est lui aussi un maximum sans filtre
d'hôte, donc une seule machine vivante le rend vrai. Les vingt et une masquent, les unes par leur
propre agrégat, les autres par celui du pipeline. Ce qui est fermé, c'est le silence : une machine
qui se tait **entièrement** lève maintenant un signal, rendu comme un compte d'hôtes muets et non
comme une série par hôte. Ce qui reste ouvert est plus étroit et le restera tant que la mesure le
justifiera : un hôte dont **un seul** collecteur meurt pendant que les autres parlent. Le fermer
demanderait une sonde par source **et** par hôte, dont le coût suit le volume d'événements — c'est
exactement le défaut fermé en P3.7-a, qu'il faudrait rouvrir vingt fois sous le verrou d'écriture.

**La sauvegarde est un export logique**, sans contenu d'index, sans tables d'index plein texte et
sans pages libres : tout cela est reconstruit à la restauration, ce qui coûte du CPU et allonge le
délai de reprise. L'archive est donc bien plus petite que le fichier de base, et ce n'est pas une
perte. La **vérification** d'une archive (P8.3-a) restaure et compte ce qui revient quand la clé de
lecture est disponible — c'est le cas du mode par passphrase ; sur le mode d'escrow, elle dégrade en
contrôle structurel, aucune clé privée de séquestre n'étant placée à côté des sauvegardes. **Un
exercice de restauration réel reste donc un geste d'exploitant, hors ligne** — mais il n'est plus
invisible : il laisse une attestation datée que le nœud enregistre, et dont l'absence ou
l'ancienneté se lisent dans la santé par composant et dans les métriques.

**Le mode multi-tenant est désactivé par défaut** et n'est pas destiné à la production en l'état ;
son repli d'entropie de clé est fermé (S4) — sans entropie de l'OS, aucun tenant n'est créé. De même, **une part des variables de configuration
n'est encore lisible que depuis l'environnement** : sur le déploiement natif systemd, un exploitant
qui les écrit dans le fichier de configuration n'obtient rien. Le registre de cette dette ne peut
que rétrécir. Enfin, le retrait du blanket sudo est un geste d'exploitant (P5.8-a) : une entrée
bornée ne le remplace pas tant que le code élevé appartient à l'utilisateur qui l'invoque.

---

## Conçu, non construit

**Agrégation bornée native.** Un accumulateur à état borné — comptage distinct probabiliste, top-N
borné — au lieu de déléguer au trieur de SQLite. Cela fermerait la question mémoire **par la
réponse** (l'agrégat sort, borné et marqué) là où la limite de tas la ferme par le refus. Ne coûte
aucune confidentialité, rien ne touchant le disque, et s'appuie sur des types de réponse déjà en
place. Conçu et ordonné, pas construit.

**Compression au repos des données chaudes.** Les valeurs textuelles et les champs JSON se
compressent fortement ; la variante retenue est **par valeur**, en gardant en clair les colonnes qui
servent les index et les filtres pour que ni l'index ni la clause de sélection ne paient. Le coût de
lecture doit être mesuré avant toute généralisation. La variante **par page**, au niveau de la
couche de fichier virtuelle, est écartée par défaut pour la même raison que le déversement des tris :
elle déplace la frontière de confidentialité.

**Réduction d'index.** Déjà entamée, et il reste peu de gras : ce qui subsiste est nécessaire — la
contrainte d'unicité qui porte la déduplication — ou sert de la détection active. Rien n'y sera
retouché sans mesure d'usage.

**Un chantier plus court, décidé et non livré** : un
index partiel couvrant pour le détecteur de retard de vieillissement (P10.13-a), chiffré à la sonde,
qui supprimerait le balayage au prix d'un coût d'ingestion et d'octets — un index sur le chemin
chaud sous 2 Gio se décide, il ne se glisse pas dans un lot. S'y ajoute une garde permanente sur la
table de décision du retour arrière (P4.4-c), dont les cas sont prouvés par un harnais jetable.

---

*Les états de cet index sont tenus à jour avec le code : une ligne ✅ porte un correctif et une
vérification, une ligne ❓ attend une re-mesure et ne doit être citée comme acquise par personne. Le
document de conception qui détaille les travaux d'échelle est
[DESIGN-P10-echelle-2go.md](DESIGN-P10-echelle-2go.md), également listé dans l'index
[docs/README.md](README.md).*
